# Changelog

Keep a Changelog 형식. 날짜는 로컬(KST) 기준.

## Unreleased

문서를 오가는 비용을 줄이는 데 초점을 둔 릴리스. 크로스링크를 누르지 않고도
내용을 확인하고, 최근 본 문서로 바로 되돌아가고, 어느 화면에서든 퀘스트를
만들 수 있다. 규칙·도서관 문서도 이제 퀘스트처럼 변경 이력이 남는다.
첨부는 1.5 MB 를 넘기면 조용히 실패하던 것을, 스트리밍 업로드로 바꿔 **크기
제한 자체를 없앴다**(진행률·취소·대기 목록 포함).

**주요 변경점**

- **크로스링크로 문서를 오가는 비용 절감** — 링크에 마우스를 올리면 미리보기
  팝업이 뜨고(DEV-256), 최근 본 문서는 검색 팔레트를 그대로 재활용해
  미리보기·새 창 열기까지 쓸 수 있다(DEV-276/294).
- **규칙·도서관 문서의 변경 이력** — 지금까지 퀘스트만 남던 이력이 규칙과
  BOOK 에도 생겼고, 작업기록의 활동 목록·일간 요약에 함께 집계된다.
  CLI(`rule history` / `library history`)와 서버 API 도 지원(DEV-288/290).
- **대용량 첨부 정상화** — 원본 1.5 MB 만 넘겨도 실패하던 것을 고쳤다. 데스크탑은
  파일 경로만 넘기고, 서버(원격/브라우저)도 스트리밍 업로드로 바꿔 **양쪽 모두
  크기 제한이 없다**(200 MB 업로드 실측 0.65초). 진행률·취소·대기 목록까지
  갖췄다 (BUG-168, DEV-298, DEV-321/322/323/337/338).
- **입력 UI 통일** — 긴급도를 드롭다운 대신 pill 로(상태·타입과 동일),
  댓글 입력창은 본문과 같은 마크다운 편집기로 전환 가능(DEV-287/293/289).
- **체감 속도** — 길드 열기는 바뀐 파일만 반영해 4.1초 → 0.02초(퀘스트 651개
  실측), 상태 변경 때 끊기던 것은 자동 백업을 스냅샷 파일 1개로 바꾸고
  백그라운드로 돌려 4.0초 → 0.35초. 스냅샷 용량도 137MB → 1.6MB
  (DEV-246, DEV-299, DEV-306).
- **웹·좁은 화면 지원** — 브라우저 모드에서 접근할 수 없던 검색 팔레트·캠페인·
  작업기록·태그에 진입 경로를 만들고, 375px 급 뷰포트의 가로 스크롤을 정리
  (DEV-271, DEV-257).

### Added
- **첨부 업로드 스트리밍** — 원격/브라우저 업로드가 base64 로 통째로 메모리에
  올라가던 것을 원문 스트리밍으로 바꿨다. 64 MB 상한이 사라졌고(실측 200 MB
  0.65초, sha256 일치) 전송량도 25% 줄었다. 중단되면 조각 파일을 남기지 않는다.
  (DEV-337)
- **첨부 업로드 대기 목록·취소** — 고른 파일 전체를 대기/진행/완료/실패 상태와
  함께 보여주고, 전체 취소와 **항목별 취소**를 지원한다. 취소 시 반쪽 파일을
  지운다. 데스크탑은 동시 3개 병렬(실측 9~52% 단축), 브라우저는 메모리 때문에
  1개씩. (DEV-322, DEV-323, DEV-338)
- **HTTP 응답 gzip 압축** — 한글 JSON·마크다운이라 압축률이 특히 좋다. 퀘스트
  목록 응답 260 KB → 34 KB(7.6배), 최대 JS 청크 963 KB → 317 KB. 원격 접속에서
  체감이 크다. (DEV-332)
- **CLI 도서관·규칙 태그 명령** — `library tag` / `rule tag` (list/add/remove/set).
  코어·서버·GUI 에는 있었는데 CLI 에만 없던 구멍. 로컬·원격 모두 지원.
  (DEV-333)
- **상위(부모) 퀘스트 지정 UI** — 퀘스트 상세의 '연관 추가' 메뉴에서 부모를
  바로 지정할 수 있다. 예전엔 상대 퀘스트로 가서 반대 방향으로 거는 우회로뿐
  이었다. (DEV-339)
- **macOS(Apple Silicon) 지원** — 매 릴리스에 `.dmg` 가 첨부되고, 자동 업데이트도
  Windows 와 동일하게 동작한다. 코드 서명은 하지 않으므로 **첫 실행만** 우클릭 →
  열기(또는 `xattr -dr com.apple.quarantine`)가 필요하다 — 설치 안내는 README 와
  사용 가이드에 있다. dmg 에는 GUI 만 들어 있고, CLI·Server는 Rust/Cargo 없이
  설치할 수 있는 Apple Silicon tar.gz를 함께 제공한다. Intel 맥은 대상이 아니다.
  (DEV-312, DEV-313, DEV-314, DEV-315, DEV-349)
- **최근 본 문서 목록** — 타이틀바 검색 pill 옆(웹은 메뉴바 우측)의 시계
  버튼으로 최근 방문한 퀘스트/캠페인/규칙/도서관 문서를 바로 열 수 있다.
  뒤로가기는 한 칸씩만 가므로 "아까 그 문서"로 점프하는 수단. 목록/보드/
  설정 같은 탐색 화면은 기록하지 않고, 최대 12개까지 세션 단위로 유지.
  전용 드롭다운이 아니라 검색 팔레트를 "최근" 모드로 띄우는 방식이라
  미리보기·새창 열기 등 팔레트의 기능을 그대로 쓴다. (DEV-276, DEV-294)
- **타이틀바 '퀘스트 추가' 버튼** — 검색 pill 왼쪽 ＋ 버튼으로 어느
  화면에서든 퀘스트 생성 모달을 띄운다. 기존엔 퀘스트 목록까지 이동해야
  했다. (DEV-286)
- **댓글 입력창의 마크다운 편집기 토글** — 본문/메모 편집창과 달리 댓글은
  평범한 textarea 라 서식 도구·미리보기가 없었다. 입력창 우측 버튼으로
  본문 편집기와 같은 마크다운 편집기로 전환/복귀할 수 있다(완전 대체가
  아니라 토글). (DEV-289)
- **규칙/BOOK 변경 이력** — 규칙과 도서관 문서도 생성·수정·이름변경·삭제가
  이력으로 남고(파일 사이드카 기록 → `doc_history` 투영), 작업기록 페이지의
  활동 목록·일간 요약에 함께 집계된다. 열람은 GUI 문서 화면과
  `openguild rule history <slug>` / `openguild library history <book-id>`
  로, 원격(`--remote`)·서버 API(`GET /api/rules/{slug}/history`,
  `GET /api/library/{book_id}/history`) 모두 지원. (DEV-288, DEV-290)
- **첨부 업로드 진행 표시** — 대용량 파일은 저장이 끝날 때까지 버튼 라벨만
  '처리 중…' 으로 바뀌어 멈춘 것처럼 보였다. 첨부 섹션에 현재 파일명과
  `n / N` 순번, 진행 바를 표시한다(스크린리더에도 알림). 세 전송 경로 모두
  진행률(%)을 관측할 수 없어 불확정 바 — 실제 %는 청크 업로드 API 가 필요해
  후속. (DEV-298)
- **크로스링크 자동완성 후보의 전체 제목 표시** — 후보 폭에 잘려 어떤
  문서인지 구분이 안 되던 것을, 마우스 호버 **또는 키보드 포커스** 시
  테마를 따르는 팝업으로 전체 제목을 띄운다(OS 기본 툴팁이 아니라 커스텀
  팝업이라 다크 테마에서도 어긋나지 않고 키보드 이동만으로도 보인다).
  (DEV-297)
- **크로스링크 호버 미리보기** — 본문/댓글/규칙/도서관에 렌더된
  `[[DEV-001]]` 류 크로스링크에 마우스를 잠시 올리면 검색 팔레트와 같은
  구성(종류 칩 + 제목 + 본문 미리보기 + 새창/페이지이동 버튼)의 팝업이
  링크 옆에 뜬다. 링크→팝업으로 마우스를 옮겨도 유지, 벗어나면 잠시 후
  닫힘. (DEV-256)
- **메뉴바 overflow 메뉴** — 창 폭이 좁아 메뉴바 링크가 다 안 들어가면
  오른쪽(우선순위 낮은) 항목부터 자동으로 숨기고, 데스크탑은 타이틀바
  ☰ 메뉴 상단(구분선 구획)으로, 웹은 메뉴바의 "⋯" 드롭다운으로 옮긴다.
  창이 넓어지면 자동 복귀, 활성 페이지 하이라이트도 유지 — 브라우저
  툴바의 priority+ navigation 패턴. (DEV-260)
- **알림 동시 다발 처리** — 알림 스택에 표시 상한(5개)을 두고 넘치면
  "+N개 더 보기" 칩으로 축약(클릭 시 전체 펼침). error 알림과 지속형
  카드(업데이트/스키마 경고)는 축약 대상에서 제외, 지속형은 항상 코너쪽에
  고정. 같은 문구의 토스트가 연발하면 새 카드 대신 ×N 뱃지로 갱신 + 수명
  리셋(중복 억제). 디버그 빌드 한정 콘솔 트리거(`__ogNotify`)로 임의 알림
  조합 재현 가능. (DEV-266, BUG-170)

- **작업기록의 댓글 항목을 누르면 그 댓글로 바로 이동** — 이전엔 문서까지만
  열려서 긴 문서에서 해당 댓글을 다시 찾아야 했다. 이제 그 댓글로 스크롤하고
  잠깐 강조한다(접혀 있으면 펼친다). 토론 해결/재개 항목도 동일. (DEV-296)

- **선행/후속 퀘스트도 '새로 만들기'로 추가** — 신규 생성이 서브퀘스트에만
  있어, 아직 없는 선행을 만들려면 목록으로 나갔다 돌아와 연결해야 했다.
  (DEV-278)
- **비어 있는 연관 섹션을 숨기고 '연관 추가' 버튼으로 통합** — 서브퀘스트·선행·
  후속이 비어도 '없음' 문구가 세 줄 자리를 차지해 정작 볼 내용이 밀렸다. 이제
  항목이 있는 섹션만 보이고, 추가는 한 버튼에서 종류(+기존 연결/새로 만들기)를
  고른다. (DEV-279)

- **업데이트 자동 확인 켜기/끄기** — 시작할 때와 주기적으로 무조건 확인하던
  것을 설정에서 끌 수 있다(설정 > 정보). 끈 상태에서도 '업데이트 확인' 버튼은
  그대로 쓸 수 있다. 오프라인·사내망이거나 버전을 고정해 쓰는 경우를 위한 것.
  (DEV-305)

- **백업 스냅샷을 파일 묶음 대신 DB 파일 하나로** — 스냅샷이 `.guild/` 파일
  1,100여 개를 그대로 복사해, 만들 때마다 2초가 걸리고 디스크에는 스냅샷 하나가
  137MB(보관 7개 = 957MB)씩 쌓였다. 저장 방식만 바꿨고 담는 내용(퀘스트·규칙·
  댓글·첨부 등 진리원 파일)은 그대로다. 생성 2,054ms → 460ms, 크기 137MB →
  1.6MB. 이전 방식으로 만든 스냅샷도 계속 목록·복원·삭제할 수 있다. (DEV-306)

### Changed
- **목록 API 가 본문을 싣지 않는다** — `/api/quests?slim=true`. 목록·보드·홈·
  cross-link 인덱스는 제목만 쓰는데 본문까지 실려 응답이 1.13 MB 였다.
  258 KB 로 줄었고, gzip 까지 합치면 34 KB. (BUG-210)
- **보드 최소 zoom 을 화면·내용에 맞춰 계산** — 0.25 고정이라 좁은 화면에서는
  '전체 보기' 조차 전체를 못 보여줬다. 375px 기준 0.0306 까지 축소된다.
  (DEV-334)
- **긴급도 변경을 드롭다운 대신 pill 로** — 상태/타입 변경과 달리 긴급도만
  `<select>` 라 클릭 2회가 필요했다. 퀘스트 상세와 퀘스트 생성 모달 모두
  pill 을 눌러 바로 바꾸도록 통일(생성 모달은 타입/긴급도를 각각 다른 행에
  둬서 서로 헷갈리지 않게). (DEV-287, DEV-293)
- **긴급도 라벨·보드 설정 메뉴 다국어 지원** — 긴급도 라벨이 영어로
  하드코딩돼 있어 한국어에서도 `Critical`/`High` 로 나왔다. 보드 노드의
  긴급도 pill 도 언어 전환 즉시 다시 그려지고(한국어 라벨이 pill 을 꽉
  채우던 문제도 CJK 폭 계산으로 해소), 보드 설정 메뉴의 Arrange/Group/Snap
  등 미번역 항목도 함께 정리. (DEV-295)
- **연관 퀘스트 선택 팝업·검색 팔레트의 기본 정렬을 최근 갱신순으로** —
  생성 역순이라 오래된 퀘스트를 고르려면 목록을 뒤져야 했다. CLI
  `quest list` 의 기본 정렬은 그대로(필요하면 `--sort updated`). (DEV-277)
- **컨텐츠 폭 상한 1800px → 3200px, 최대값은 "전체"(폭 제한 해제)** —
  울트라와이드/4K 에서 양옆 여백이 과하게 남던 문제. 슬라이더를 끝까지
  밀면 모니터 크기와 무관하게 창 전체를 쓴다. (DEV-275)
- **`openguild docs <name>` 기본 출력이 전체 본문 → 목차로 변경** —
  늘 전체를 뱉어 사람은 스크롤, 에이전트는 토큰을 낭비했다. 이제 기본은
  번호가 붙은 목차(제목만), 특정 절은 `--section <번호|제목>`(하위 절
  포함, 제목은 대소문자·공백 무시 부분일치), 전체는 `--full`.
  목차/섹션은 `--json` 으로 구조화 출력도 지원. **주의: 전체 본문을
  기대하던 기존 호출은 `--full` 을 붙여야 한다.** (DEV-274)
- **`openguild comments` / `quest comment list` / `quest comment show` 옵션셋
  통일** — `comments` 에 `--top-only`/`--reply-to`/`--reverse`/`--tree` 추가,
  `quest comment list` 에 `--until`/`--discussion`/`--unresolved`/`--summary`
  추가. `comments` 기본 정렬을 최신순 → 오래된순(대화 흐름)으로 변경해
  `quest comment list` 와 통일(`--reverse` 로 최신순 전환은 그대로 가능).
  `quest comment show`/`campaign comment show` 는 `--id` 없이 실행 시
  기본 최근 20개만 출력(전체는 `--all`)하도록 변경 — 기존엔 무제한 전체
  출력. (DEV-262)

- **길드 열기가 대부분의 경우 즉시 끝난다** — 지금까지는 파일이 하나라도
  추가·삭제되거나 수정되면(git pull, 브랜치 전환, 외부 편집기 저장) 색인 전체를
  다시 만들었다. 이제 추가·삭제·수정을 그 파일들만 반영한다. 퀘스트 651개
  길드에서 열기 4.1초 → **0.02초**(릴리스 빌드 실측, 파일 추가·수정이 섞여 있어도
  0.03초). 색인으로 표현할 수 없는 변화(타입·상태 정의 변경 등)에서는 여전히
  전체 재색인으로 넘어간다 — 안전망은 그대로. (DEV-246)
- **댓글·메모·캠페인 본문을 밖에서 고쳐도 즉시 반영** — 위 개선은 퀘스트 본문만
  해당돼서, 댓글 파일 하나가 `git pull` 로 들어와도 다시 전체 재색인이 돌았다.
  이제 바뀐 파일만 반영한다(댓글 1건 편집 후 열기 4.1초 → 0.07초). 캠페인 본문은
  **파일을 다시 쓰지 않고** 읽기만 한다 — 브랜치 전환처럼 내용이 그대로인 경우
  기록 시각이 통째로 바뀌는 사고를 막기 위해서다. 캠페인 신규/삭제와 도서관·규칙·
  작업기록 문서는 아직 전체 재색인. (DEV-310)
- **남아 있던 컬러 이모지 아이콘 제거** — 보드 노드의 기한·댓글 배지(⏱ ⛺ 💬)와
  일부 화면 제목·버튼 라벨(📁 🕘 🔘 📝), 크로스링크 자동완성 후보(🏷️ 🔗)가
  OS·폰트에 따라 컬러 이모지로 렌더돼 모양·크기·정렬이 환경마다 달랐다. 모두
  테마색을 따르는 SVG 로 교체. 기한 아이콘은 출처에 따라 달라진다(퀘스트 기한
  =시계, 캠페인 기한=텐트). 재발 방지 검사도 마크업뿐 아니라 **문자열로 조립되는
  SVG·라벨**까지 보도록 넓혔다. (DEV-302)

- **첨부 파일이 원래 이름을 유지한다** — 그동안 `.guild/attachments/` 에는
  `18c7f081…-64fb723d.zip` 처럼 알 수 없는 이름으로 저장돼 파일을 직접 찾아보기가
  어려웠다. 이제 `설계 문서 v2-67a707303c76eea5.zip` 처럼 원래 이름 뒤에 중복
  방지용 값만 붙는다(기존 첨부의 이름은 그대로 — 본문 링크가 파일명을 가리키므로
  바꾸면 링크가 깨진다). (DEV-324)
- **첨부 업로드에 실제 진행률(%) 표시** — 그동안은 "돌고 있다"만 알려주는
  움직이는 바여서 얼마나 남았는지 알 수 없었다. 데스크탑은 파일을 옮기는 진행을,
  브라우저·원격은 전송 바이트를 그대로 보여준다(스크린리더에도 숫자가 전달된다).
  브라우저에서 파일을 보내기 전 준비하는 구간은 "준비 중…"으로 따로 표시해
  0%에서 멈춘 것처럼 보이지 않게 했다. (DEV-321)

### Fixed
- **라우트 이동마다 인덱스를 다시 받던 문제** — 문서 페이지에 들어갈 때마다
  cross-link 인덱스를 강제 재적재해, 이동 1회당 1.1 MB 를 다시 받고 있었다.
  변경이 있을 때만 재적재하도록 고쳤다(이동 16회 기준 수신 18.4 MB → 0.4 MB).
  (BUG-210)
- **데스크탑에서 길드가 열리지 않던 문제** — Tauri 로 넘기는 목록 조건의
  bool/정수가 문자열로 전달돼 커맨드가 즉시 실패했다. 기존 필터들도 같은 이유로
  데스크탑에서 동작하지 않고 있었다. (BUG-211)
- **모바일에서 편집기 글자를 길게 눌러 선택할 수 없던 문제** — 편집기가 네이티브
  선택을 숨기고 자체 선택을 그리고 있었다. 터치 환경에서는 네이티브 선택을
  유지한다. (BUG-215)
- **커스텀 스크롤바가 보이지 않던 두 곳** — 댓글 입력창(일반 모드)과 메모 고정
  모드. (BUG-213, BUG-214)
- **규칙·도서관 문서를 고쳐도 작업기록에 안 남던 문제** — 변경 기록 자체는 남고
  있었지만 작업기록 화면이 읽는 캐시가 색인 재구축 때만 채워져서, 실제로는
  아무것도 안 보였다. 이제 문서를 고치는 즉시 타임라인·히트맵에 반영된다.
  이름을 바꾸면 지난 기록도 새 이름을 따라간다. (BUG-189)
- **첨부 업로드 진행 표시에서 파일명이 길면 진행 바가 찌그러지던 문제** — 파일명과
  진행 바를 서로 다른 줄로 분리했다. (BUG-190)
- **1GB 가 넘는 파일을 첨부하면 실패하던 문제 — 첨부는 이제 백업 대상이 아니다**
  — 첨부 파일은 저장할 때 같은 내용을 색인 DB 에도 복사해 백업에 태웠는데, 그
  방식은 약 1GB 를 넘기면 DB 한계에 걸린다(1.5GB 압축파일 실패 보고). 게다가
  실패해도 파일은 이미 저장된 뒤라, 그 뒤로 재색인과 백업이 **계속** 같은 오류로
  실패했다. 이제 첨부는 파일로만 보관하고 백업(스냅샷)에 담지 않는다 — 크기
  제한이 사라지고 백업도 가벼워진다. 대신 **첨부는 백업으로 복원되지 않으므로**
  git 이나 별도 보관이 필요하다(어드민 > 백업 화면에 명시). 첨부 목록 자체는
  그대로 백업되고, 복원해도 기존 첨부 파일은 지워지지 않는다. (BUG-188)
- **편집 중 새로고침하면 경고 없이 내용이 사라지던 문제** — 링크나 뒤로가기로
  나갈 때는 확인 창이 뜨는데 F5 에는 아무것도 없었다(창이 안 닫히는 문제를
  고치면서 같이 빠졌던 부분). 브라우저에서는 표준 이탈 경고를, 데스크탑에서는
  앱의 확인 창을 띄운다. **창 닫기(X)는 예전처럼 즉시 닫힌다** — 종료를 막는
  일은 하지 않는다. (BUG-179)
- **퀘스트를 만들 때마다 "파일이 색인보다 새롭다"는 거짓 경고가 뜨던 문제** —
  퀘스트 생성은 타입 파일의 번호 카운터도 갱신하는데, 그 쓰기를 색인의 mtime
  캐시에 반영하지 않아 바로 뒤에 `check drift` 를 돌리면 매번 변경으로 잡혔다.
  안내대로 `--resync` 를 돌리면 전체 재색인이 도는 탓에 위 DEV-246 의 이득을
  그대로 반납하게 된다. (BUG-177)
- **'토론만' 필터를 켜면 '전체 접기' 가 답글을 접지 않던 문제** — 필터가 접힘
  상태를 덮어써서, 전체 접기를 눌러도 답글이 그대로 펼쳐져 있었다(필터를 끄면
  그때서야 접혀 있었다). 필터는 무엇을 보여줄지, 접기는 사용자가 누른 동작이라
  서로 간섭할 이유가 없다. (BUG-178)
- **길드를 바꾼 뒤 뒤로가기가 엉뚱한 길드를 보여주던 문제** — 길드 전환은
  주소를 바꾸지 않아서, 뒤로가기로 이전 길드의 기록에 가도 화면은 **지금 열린
  길드**가 그려졌다(A → 웰컴 → B 후 뒤로가기가 B). 이제 다른 길드의 기록에
  도달하면 웰컴 화면으로 보내 거기서 직접 고르게 한다. 웰컴 화면에서는
  뒤로가기가 아무 일도 하지 않는다(예전엔 이전 길드 화면이 잠깐 보였다가
  돌아와 깜빡였다). 같은 길드 안에서의 뒤로가기는 그대로다. (BUG-176)
- **대용량 파일을 첨부할 수 없던 문제** — 브라우저(서버) 모드는 axum 기본
  body limit 2 MiB 가 그대로 걸려, base64 팽창(4/3)을 감안하면 **원본 약
  1.5 MB 를 넘는 순간** 실패했다(사진 한 장에도 걸리는 수준). 게다가 오류가
  axum 원문(`Failed to buffer the request body`)으로 노출돼 원인을 알 수
  없었다. bytes 를 받는 `/api/attachments` 에만 한도를 64 MB 로 올리고(다른
  라우트는 기본값 유지), 한도 초과 시 크기를 밝힌 안내를 띄운다.
  데스크탑은 파일 선택 시 bytes 를 IPC 로 보내지 않고 **경로만** 넘겨
  Rust 쪽에서 복사하도록 바꿔(`save_attachment_from_path`) 크기 제한 자체가
  사라지고 속도도 빨라졌다 — 기존 경로는 파일 크기의 5~6배 메모리를 JS/Rust
  양쪽에 동시에 잡았다. 붙여넣기·드래그&드랍은 File 객체(bytes)뿐이라 기존
  방식을 유지하되 같은 64 MB 상한이 적용된다. (BUG-168)
- **자동 백업이 만들어지는 순간 앱이 잠깐 멈추던 문제** — 백업 스냅샷 생성이
  변경 작업과 같은 흐름에서 동기로 실행돼, 임계치에 걸린 작업 하나가 2초 가까이
  멈췄다. 데스크탑·서버에서는 백그라운드로 돌려 화면이 멈추지 않는다(CLI 는
  명령이 끝나면 프로세스가 종료돼 백업이 유실되므로 기존 방식 유지). 연속
  작업으로 스냅샷이 겹쳐 도는 것도 막았다. (DEV-299)
- **브랜치를 전환하면 없어진 퀘스트의 이력 파일이 되살아나던 문제** —
  캐시(index.db)는 브랜치를 따라가지 않는데, 이력 내보내기가 캐시에 남은
  다른 브랜치 퀘스트의 기록까지 파일로 만들어냈다. 그 파일이 다음 브랜치
  전환을 막기도 했다. 현재 브랜치에 실제로 존재하는 퀘스트만 내보낸다.
  (DEV-281)
- **퀘스트 번호 카운터가 다른 브랜치 값으로 덮어써지던 문제** — 캐시의 카운터가
  파일보다 크면 파일에 되쓰고 있었다. 잘못된 브랜치에서 퀘스트를 만들면 그
  번호가 현재 브랜치로 흘러와 ID 가 어긋났다(실제로 발생). 카운터는 이제
  디스크에 실제로 있는 최대 번호만 반영하고, 캐시는 파일에서 다시 만든다.
  삭제된 번호를 재사용하지 않도록 카운터를 낮추지는 않는다. (DEV-282)
- **첨부 자동 복원 판정이 캐시를 보던 문제** — 참조 여부를 캐시 테이블에서
  확인해, 호출 순서에 따라 현재 브랜치에 없는 첨부가 되살아날 수 있는 구조였다.
  판정을 파일(본문·첨부 목록)만 보도록 바꿨다. (DEV-284)
- **존재하는 문서의 크로스링크가 빨간색(없는 문서)으로 표시되던 문제** —
  링크 판정에 쓰는 목록을 앱 실행 중 한 번만 받아오고, 그 과정이 일부라도
  실패하면 조용히 넘어간 채 그대로 굳었다. 그래서 (a) 목록을 받은 뒤에 만든
  문서, (b) 목록 일부를 못 받은 경우의 문서가 계속 빨갛게 보였다. 이제 실패한
  종류만 다시 받아오고, 빨간 링크가 보이면 목록을 한 번 새로 확인해 자동으로
  정정한다(진짜 없는 링크는 그대로). (BUG-173)
- **일부 아이콘이 OS 에 따라 컬러 이모지로 보이던 문제** — 정지/재생, 폴더,
  문서, 댓글, 메모, 삭제, 배너, 고정 등 UI 아이콘이 이모지 코드포인트라
  폰트 폴백에 따라 컬러로 그려지고 크기·정렬도 환경마다 달랐다. 마크업의
  해당 아이콘 21곳을 테마 색을 따르는 SVG 로 교체하고, 재발을 막는 검사를
  CI 에 추가했다. 댓글 반응 이모지는 그대로 유지. (BUG-169)
- **`quest show` 에 후속 퀘스트가 안 보이던 문제** — 부모/자식/선행은 나오는데
  "무엇이 나를 선행으로 잡고 있는지"(후속)는 CLI 로 볼 방법이 없어, 반대방향
  연관 해제 대상을 찾을 수가 없었다. 후속 목록과 해제 명령 힌트를 표시하고,
  `--field successors|prerequisites|sub_quests` 로 스크립트에서도 쓸 수 있게
  했다. (DEV-291)
- **CLI 의 `rule` 명령이 원격(`--remote`)에서 동작하지 않던 문제** — 서버에는
  라우트가 다 있는데 CLI 가 "지원하지 않음"으로 막고 있었다. list/show/set/
  new/delete/rename 전부 원격 지원. 공백이 든 규칙 이름도 정상 동작하도록
  경로 인코딩도 함께 수정. (BUG-166)
- **자동 업데이트 후 HTTP 서버가 구버전으로 남던 문제** — 업데이트는 컴포넌트
  선택 화면을 건너뛰고 기본값을 쓰는데, 서버는 기본이 '해제'라 처음 설치 때
  서버를 선택했던 사용자에게 구버전 서버가 조용히 남았다. 설치 시 선택을
  기록해 업데이트가 그대로 유지하도록 수정. (BUG-151)
- **`openguild campaign` 에 제목/본문 수정 명령이 없던 문제** — 서버·GUI 는
  가능한데 CLI 만 상태 변경밖에 없어, GUI 없이는 캠페인 이름을 바꿀 방법이
  없었다. `campaign update <slug> --title/--description-file/--start/--end`
  추가. (DEV-280)
- **웹(브라우저)의 상단 바를 데스크탑과 같은 배치로** — 검색은 오른쪽 끝의
  작은 돋보기 버튼이라 데스크탑의 중앙 pill 과 완전히 달랐고, 접힌 메뉴(☰)는
  클릭해도 열리지 않았다(nav 의 `overflow: hidden` 에 잘려 보이지 않던 것).
  검색 pill 을 화면 중앙에 고정하고 좌우에 퀘스트 추가·최근 본 문서 버튼을
  두고, 창이 좁아 pill 과 겹치는 링크는 ☰ 로 접는다. pill 에 길드 이름이
  표시돼 웹에서 길드 이름이 안 보이던 것도 함께 해결. (DEV-271)
- **좁은 화면에서 도서관·규칙·설정 페이지가 화면 밖으로 밀리던 문제** —
  좌측 목록/탭이 고정 폭(260/240/160px)이라 375px 급 화면에서 화면의 절반
  이상을 먹고, 남은 열이 더 줄어들 수 없어 컨텐츠가 밀려났다. 그래서 페이지에
  좌우 스크롤이 생기고 상단 메뉴바까지 잘려 보였다. 좁은 화면에서는 위아래로
  쌓고(목록은 자체 스크롤로 높이 제한), 설정 탭은 가로 한 줄로 바꿨다.
  (DEV-257)
- **설정 퀵메뉴의 컨텐츠 폭이 최대값에서도 '3200px' 로 표시되던 문제** —
  설정 페이지에는 '전체'로 나오는데 퀵메뉴만 숫자를 그대로 보여줘서 끝까지
  밀어도 3200 에서 멈춘 것처럼 보였다(실제 폭은 정상). (DEV-275)
- **일부 연관관계를 해제할 수 없던 문제** — 부모→자식, 후속→선행처럼 한쪽
  방향에만 해제 버튼이 있어서, 자식 퀘스트에서 부모를 끊거나 후속에서
  선행을 끊을 수 없었다. 양방향 모두 해제 가능하도록 수정. (BUG-165)
- **데스크탑 앱에서 퀘스트 목록 API 의 정렬/필터 파라미터가 무시되던
  문제** — Tauri 경로가 query string 을 버리고 있었다(서버 모드는 정상).
  (DEV-277 작업 중 발견)
- **최근 본 문서 버튼이 타이틀바 검색 pill 을 밀어내던 문제** — 버튼 유무와
  무관하게 pill 이 항상 화면 중앙에 고정되도록 배치 변경. (BUG-158)
- **최근 본 문서 목록의 제목이 잘못 표시되던 문제** — 화면 h1 을 긁던 방식
  이라 도서관은 모든 문서가 "도서관", 규칙은 라벨과 중복 표시됐다.
  cross-link 인덱스에서 조회하도록 바꿔 정확해지고, 문서 이름을 바꾸면
  목록도 따라간다. (BUG-159)
- **모바일(좁은 화면) 웹 레이아웃 1차 정리** — 375px 급 뷰포트에서 홈
  작업기록 카드와 작업기록 페이지 컨트롤이 가로 스크롤을 유발하던 문제를
  줄바꿈으로 수정, 검색 팔레트가 과도하게 좁아지던 것(62vw)을 모바일에선
  거의 전폭으로. 나머지 페이지는 전수 스캔에서 정상 확인 — 터치 인터랙션
  전환/Tauri 모바일 빌드는 후속. (DEV-257 1차)
- **크로스링크가 인덱스 적재 전에 렌더되면 '존재하지 않음'(빨강)으로
  영구 고착되던 레이스** — 인덱스가 늦게 적재돼도 이미 만들어진 링크를
  다시 resolve 해 정상 표시로 갱신(reindex 후 존재→미존재 전환도 동일
  경로로 반영). (DEV-256 작업 중 발견)
- **웹(브라우저) 배포에서 검색 팔레트/캠페인·작업기록·태그 페이지에
  접근할 방법이 아예 없던 문제** — 이 기능들이 커스텀 타이틀바(Tauri
  데스크탑 전용, `usesCustomTitlebar()`)에만 있었는데 웹은 그 타이틀바
  자체가 없었다. Nav 바에 웹 전용 fallback(검색 버튼 + 캠페인/작업기록/
  태그 링크)을 추가. (DEV-271)
- **`--json` 출력이 `--compact` 여부와 무관하게 늘 한 줄로 나오던 문제
  (rule/tag/worklog/quest/첨부/댓글/메모/도서관 등 다수 명령)** —
  `serde_json::json!(...)` 값을 `println!` 로 바로 Display 하는 경로가
  68곳 있었는데, 이 경로는 pretty/compact 를 결정하는 내부 헬퍼(json_str)
  를 안 거쳐서 항상 compact 로 나왔다. 전부 json_str 경유로 통일하고,
  같은 실수가 재발하지 않도록 소스를 스캔하는 회귀 테스트 추가. (DEV-261)

## 0.4.1-beta — 2026-07-26

0.4.0-beta 이후 발견된 버그 수정만 모은 패치. 기능 추가/동작 변경은
다음 마이너로 미뤘다.

### Fixed
- **길드를 열기만 해도 무관한 퀘스트의 `updated_at` 이 일괄 변경되던
  문제** — 내용이 그대로인데도 파일이 수정된 것으로 잡혀 커밋 노이즈가
  생겼다. 내용 비교에 `updated_at` 자기 자신이 껴 있어 한 번 어긋나면
  계속 재발하는 구조였다. (BUG-145)
- **`openguild init` 이 만드는 `.gitignore` 에 openguild 자체 개발용
  이슈 번호와 한국어 주석이 들어가던 문제** — 사용자 저장소에 생성돼
  팀과 공유되는 파일이라 영어로 교체. (BUG-161)
- **띄어쓰기가 있는 규칙 이름을 cross-link 로 쓸 수 없던 문제** —
  `[[코딩 규칙]]` 처럼 공백이 들어가면 링크로 인식되지 않고 자동완성도
  공백에서 끊겼다. (BUG-156)
- **연관 퀘스트/캠페인 연결 팝업이 바깥 클릭으로 닫히지 않고 너무 좁던
  문제** — 백드롭 클릭 닫기 추가, 폭·목록 높이 확대. (BUG-160)
- **타이틀바 ☰ 메뉴가 타이틀바(빈 영역) 클릭으로 닫히지 않던 문제.**
  (BUG-155)
- **도서관 아이콘 보기에서 긴 문서 제목이 옆 타일 영역을 침범하던
  문제** — 후속으로 제목이 한 줄만 보이던 회귀도 수정(문서 ID 를 아이콘
  위로 옮기고 제목은 3줄까지 표시). (BUG-153)
- **상태 변경·연관 퀘스트 추가/제거·보드 드래그가 1초 가까이 걸리던 문제**
  — 모든 변경마다 백업 스냅샷 폴더 전체를 재귀적으로 훑어 용량을 계산하고
  있었다(스냅샷 수 × 길드 파일 수에 비례해 느려짐). 자동 백업 판단에는
  최신 스냅샷의 시각만 필요하므로 이름만 읽도록 변경 — 실측 0.5초 →
  15밀리초. (BUG-167)
- **cross-link 자동완성에서 ↑/↓ 로 이동할 때 스크롤이 항목 하나가 아니라
  팝업 높이만큼 건너뛰던 문제.** (BUG-163)
- **검색 팔레트 등에서 커스텀 스크롤바가 보이지 않던 문제** — 조상 요소의
  transform 이 고정 위치 기준을 바꿔 스크롤바가 화면 밖에 그려졌다.
  (BUG-157)
- **퀘스트 상세의 부모 관계 라벨이 '부모'/'Parent' 로만 표시되던 문제** —
  다른 섹션(선행/후속/서브 퀘스트)과 톤을 맞춰 '부모 퀘스트'/'Parent
  Quest' 로. (BUG-164)
- **에이전트 스킬 트리거/지침 보강** — `.guild` 가 없는 디렉토리(새 길드
  init 시나리오)에서도 스킬이 트리거되게 수정(BUG-152), 도서관/댓글 등
  "조회"도 CLI 명령을 쓰도록 규칙 추가(BUG-154). 퀘스트 상태 전환 규칙도
  명확히 — 코드를 건드리기 전에 In Progress, 구현이 끝나면 Testing(단
  길드가 해당 상태를 이름 변경/삭제했다면 미적용).


## 0.4.0-beta — 2026-07-19

### Added
- **에이전트용 정식 스킬 패키지** — `docs agents`(AGENTS_OPENGUILD_USAGE.md)
  문서를 대체해 Claude Code plugin marketplace 구조(`skills/`)로 제공.
  Windows installer 가 설치 시점에 바로 `~/.openguild/skill-marketplace/`
  로 복사해둬 앱을 한 번도 안 띄워도 `/plugin marketplace add` 로 바로
  등록 가능(그 외 플랫폼 + 소스 빌드는 앱 최초 실행 시 동기화). 기존
  `openguild docs agents` 는 제거됨(용도가 스킬로 이전). 스킬 자체에도
  작업 전 규칙/도서관 확인 체크리스트, 무엇을 quest/댓글/규칙/도서관으로
  남길지에 대한 판단 기준, 첨부파일 사용법을 추가로 다듬음.
  (DEV-264, DEV-268/269/270)
- **도서관(Library)** — 프로젝트 참고문서/노트 저장소 신설. 파일 진리원 +
  index.db 캐시(core), CLI `library` 명령군 + 서버 `/api/library` 라우트,
  GUI `/library` 페이지까지 전 구간 지원. 폴더 계층, 전문 검색(현재
  폴더+하위 범위, 폴더명 검색), 정렬 옵션(번호/이름/수정순), cross-link
  (`[[BOOK-NNN]]`) 통합 + 자동완성, 태그, 임의 파일 첨부(이미지/동영상
  외 포함, `library attach`), 백업/복원 대상 포함.
  (DEV-215/216/217/218/219/237/238/239/240/243/251)
- **Linux 배포 패키지(deb/rpm/AppImage)** 추가 — 기존 Windows NSIS installer
  외에 리눅스에서도 정식 패키지로 배포. (BUG-142)
- **rule list / tag list `--table` 출력** 추가 — 정렬된 표 형식(사람용).
  (DEV-252)
- **server 설정 파일(`openguild-server.toml`)** — frontend 정적 자산 위치를
  `--frontend-dist` / env 외에 설정 파일로도 지정 가능. API-only 모드로
  떨어졌을 때 원인과 지정 방법을 시동 로그에 안내. (DEV-229)
- **`library attach` 명령** — 도서관 문서에 큰/바이너리 첨부파일(기획 문서
  원본, PDF, zip 등)을 붙일 수 있게 CLI에 추가. core/server/GUI 는 이미
  지원하고 있었는데 CLI만 빠져 있던 걸 `quest attach`/`campaign attach` 와
  동일한 형태(list/add/remove)로 보완. (BUG-150)
- **`openguild docs <name>`** — usage/readme/changelog 번들 문서를 CLI에서
  바로 embed 출력. (DEV-248)
- **길드 전체 tag 관리 top-level 명령** 신설(`tag list/add/update/delete`).
  (DEV-228)

### Changed
- **CLI/서버 메시지 다국어화 확장** — quest/campaign/rule/library/worklog/
  tag/template 등 모든 서브커맨드의 `--help` 가 `locale` 설정(en/ko)을
  그대로 따라가도록 전환. (DEV-254)
- **커스텀 타이틀바 도입(VSCode 식)** — 플랫폼별 네이티브 타이틀바 테마
  어긋남을 원천 해소. 리눅스에서 커스텀 대신 네이티브로 표시되던 문제도
  함께 수정. (DEV-253, BUG-140)
- **창 컨트롤 버튼 — 플랫폼별 실제 네이티브화** — Windows 는 OS 가 실제로
  쓰는 아이콘 폰트(Segoe Fluent Icons/Segoe MDL2 Assets)로 교체하고
  `WM_NCHITTEST` 훅으로 최대화 버튼 호버 시 진짜 OS Snap Layout 이 뜨도록
  복원, macOS 는 `titleBarStyle: overlay` 로 네이티브 traffic light 를
  그대로 유지, Linux 는 실행 중인 GTK 아이콘 테마·`gsettings` 버튼 순서를
  실제로 조회해 렌더링(하드코딩 근사 폐기). (DEV-265)
- **댓글 시스템 대폭 개선** — 길드 전체 검색(`comments`), 기본 출력을
  요약 대신 본문 전체로(`--summary` 로 축약 선택), 핀(고정)/토론 필터,
  접기 상태 localStorage 영속, 답글 depth·부모 포함 출력 옵션(`--tree`,
  `--depth all`, `--with-parents`), 토론 해결/재개 전환의 이력 기록.
  (DEV-213/214/221/230/234/235/236/241/250)
- **CLI 명령 체계 재정리** — type/status/rule top-level 명령을 단수형으로
  통일(복수형은 alias 유지), `rule` 복수형 alias(`rules`) 및 `create`
  alias 완전 제거, quest 본문 `--description-file` 입력 지원.
  (DEV-222/227/231/232)
- **캠페인** — 퀘스트 진행바를 상태별 색 누적 바로 교체, 상태 변경도
  `quest_history` 패턴으로 이력 기록. (DEV-226/233)
- **커스텀 테마 프리셋을 `~/.openguild/themes.json` 파일로 이전**
  (기존 `localStorage`). (DEV-249)
- **사용자 데이터 위치를 `~/.openguild/` 로 일원화** — 설치 시 기존
  `docs`/recents 데이터 마이그레이션. (DEV-247)
- 퀘스트 상세의 '브랜치 이름' 섹션 제거, 검색 팔레트 결과 열기 방식
  선택 옵션(미리보기/자식윈도우/페이지이동) 추가. (DEV-255/258)
- **알림 시스템 통합** — 앱 곳곳에 흩어져 있던 개별 toast/`alert()` 구현과
  레이아웃을 밀어내던 in-flow 배너(SchemaAheadBanner)를 `ToastHost` 하나로
  정리하고, 업데이트 확인 알림과 동일한 우하단 카드 스타일로 통일.
  (DEV-259, BUG-139)
- **커스텀 테마 에디터 다국어 지원.** (DEV-205)
- 좁은 창 폭에서 어드민 페이지 등 UI 가 깨지는 문제(줄바꿈/버튼 구분선
  어긋남) 수정. (BUG-143)
- **메뉴바 상단 로고/길드명 블록 제거** — 타이틀바 pill 과 중복이라 정리
  (모든 플랫폼·웹 공통). (BUG-146)
- **`openguild docs readme` 에서 이 repo 고유 이슈 번호 인용 제거** —
  "복구" 절 제목의 `(BUG-041)` 같은 인용은 다른 길드 사용자가 자기 길드
  얘기로 오인할 수 있어 삭제. (DEV-263)
- **cross-link 는 명시 `[[..]]` 만 인식** — bare `DEV-033`(대괄호 없음) 자동
  링크와 일반 타이핑 중 자동완성 팝업을 제거. 자동완성은 `[[` 를 연
  상태에서만, 빈 `[[` 트리거와 `]]` 중복 방지도 함께 정리. 기존 본문의
  bare 참조는 더 이상 링크되지 않음(의도된 변경). (DEV-220, DEV-223)

### Fixed
- **리눅스 전반 성능 저하** — WebKitGTK 의 DMABUF 렌더러가 일부 GPU 드라이버
  조합에서 소프트웨어 합성으로 떨어져 보드/스크롤이 심하게 느려지는 문제
  완화(`WEBKIT_DISABLE_DMABUF_RENDERER=1`). (BUG-144)
- **도서관 관련 다수 수정** — 도서관/규칙/작업기록 페이지에서 뒤로가기가
  페이지를 그냥 나가버리던 문제, 트리뷰 폴더 접기 누락, 폴더 안 검색 시
  자기 자신이 결과에 뜨던 문제, 검색 범위가 전체 길드로 새던 문제,
  드래그앤드롭 폴더 이동, 뷰모드 토글 중복, 본문 저장/reindex 후 첨부파일
  목록이 사라지거나 재조회 안 되던 문제, JS 색 계산이 OS 테마 전환에
  반응 안 하던 문제.
  (BUG-119/120/121/122/123/124/126/127/128/129/130/133/134)
- **댓글 UI 다수 수정** — cross-link 추천 목록이 스크롤 안 되거나 조상
  overflow 에 잘려 표시되던 문제, 이모지 반응 팝업의 방향/화면밖
  클리핑/스크롤 추종/화면밖 클릭 시 안 닫히던 문제, 답글의 답글 입력창이
  스레드 맨 아래로 튀던 문제, 전역 댓글 검색에서 답글의 부착 위치가 안
  보이던 문제. (BUG-110/114/115/116/125/131/132)
- 보드(Board) grid snap 관련 노드 위치 오류 2건, 다른 길드로 전환해도
  보드/리스트 필터가 남아있던 문제. (BUG-109/112/113)
- 규칙 페이지에서 다른 규칙 `[[링크]]` 클릭 시 이동 안 되던 문제,
  `rule new` 실행이 멈추던 문제(create/new canonical 뒤바뀜),
  debug 빌드가 모든 명령에서 stack overflow 나던 문제(`run()` 단일
  프레임 1MB 초과), 신규 클론에서 dev-frontend API 가 404 나던 문제
  (`VITE_API_URL` 미설정), 웹 dev(SSR) 모드가 SPA 인데 `ssr=false` 를
  안 줘서 크래시 나던 문제, 오버레이 스크롤바가 타이틀바/메뉴바 위에
  그려지던 문제, justfile 빌드 레시피의 의존성 순서 오류, 서버 설정
  테스트가 `~/.openguild` 를 격리하지 않아 실기서 실패하던 문제, snapshot
  디렉토리명이 초 단위로 충돌해 과거 snapshot 을 오염시키던 문제,
  `change_status` 이력 append 타입 오류로 workspace 빌드가 안 되던 문제,
  Welcome 상태에서 설정 진입 시 길드 이름이 placeholder 로 뜨던 문제.
  (BUG-103/104/105/106/107/108/111/117/118/135/136/137/138/147/148)

## 0.3.0-beta — 2026-07-02

### Added
- **단일 origin 웹 배포** — server 가 SPA(`gui/frontend/build`)와 API 를 같은
  origin 으로 서빙, SPA 딥링크 fallback. 실행 위치(repo root / `target/release`)
  와 무관하게 정적 자산을 찾도록 exe 상대경로 탐색까지 보강. (DEV-195)
- **원격 서버 모드(MVP)** — Tauri 데스크탑 GUI 가 로컬 길드 대신 원격
  openguild-server 의 HTTP API 에 접속. 연결/해제는 Welcome 화면에서, 설정
  페이지엔 현재 연결 상태만 읽기 전용 표시. 인증은 범위 밖(신뢰된 네트워크
  전용). (DEV-113)
- **브라우저/원격 모드 첨부 업로드** 지원. (DEV-152)
- **다국어 인프라(한/영 토글)** — 설정 퀵메뉴 + 설정 페이지 모두에 노출.
  GUI 전역 스윕은 후속([DEV-205](.guild/quests/DEV-205.md)). (DEV-015)
- 네이티브 창 테마(Windows 타이틀바)를 앱 테마에 동기화.
- 네트워크 공유 길드(UNC 경로) 열기 — `canonicalize` 의 verbatim prefix
  (`\\?\UNC\`) 정규화로 `.guild` 를 유효 디렉토리로 인식 못 하던 문제 수정
  + index.db journal_mode 자동 분기(WAL→DELETE). (BUG-091)

### Changed
- 보드 줌 — 트랙패드 두손가락 스크롤=pan / Ctrl+스크롤=줌, 마우스 휠=줌.
  (WebView2 가 트랙패드 pinch 를 자체 소비해 DOM 으로 안 보내므로 Ctrl+스크롤로
  대체.) (BUG-090)
- 설정 페이지 '원격 서버' 탭 폐기 — '정보' 탭에 현재 길드 위치(로컬 경로 /
  원격 URL)로 통합. (DEV-207)
- Welcome 화면 길드 열기 — 폴더 선택(생성+열기 겸용)을 주 버튼으로 복원,
  `.guild` 마커 파일 직접 선택은 보조 링크로. (DEV-204)
- 길드 열기 안내 문구 정확화 — 마커는 `이름.guild` 파일.
- 본문 편집기 상단 중복 첨부 버튼 제거.
- MIT 라이선스 적용.

### Fixed
- reindex 후 보드 grid snap 점이 표시되지 않던 stale 캐시 버그. (BUG-092)
- 상세 화면 lazy refresh 가 다른 프로세스의 편집을 놓치던 문제 — 콘텐츠
  항상 다시 읽도록 수정.
- NSIS 설치본에서 `openguild-server` 의 `/` 접속이 항상 404 였던 문제 —
  설치 번들에 frontend 정적 자산(`gui/frontend/build`)이 빠져 있었음.
  설치 시 함께 복사되도록 수정. (DEV-195)
- 브라우저/원격 모드 admin 의 타입/상태 관리가 깨지던 문제 — `/api/admin/types`
  / `/api/admin/statuses` CRUD 라우트가 server 에 없어 SPA fallback(HTML)이
  JSON 자리에 응답됨. Tauri invoke 와 1:1 대응 라우트 추가. (DEV-193)
- 웹 접속 시 탭 아이콘이 Svelte 기본 로고로 표시되던 문제 — 앱 아이콘
  favicon(ico/png) 으로 교체. (BUG-101)
- Welcome 재방문 후 설정 페이지가 이전 길드 컨텍스트를 계속 표시하던 문제 —
  세션 단위 길드 컨텍스트 플래그로 수정. (BUG-099 후속)
- 콜드 스타트 시 Welcome 에서 뒤로가기로 빈 보드에 진입되던 문제 —
  redirect 를 history 교체로 변경. (BUG-100)

### Known issues
- **원격 모드 인증 없음** (JWT 미구현, DEV-021) — 원격 서버는 신뢰된
  네트워크에서만 사용할 것.
- 다국어(한/영)는 인프라 + 일부 화면만 적용 — 전역 문자열 스윕은 후속.
  (DEV-015, DEV-205)
- 터치스크린 핀치-투-줌 미지원 — 보드 줌은 Ctrl+스크롤/마우스 휠. (DEV-208)
- UI polish / server 기능 강화 umbrella 는 계속 진행 중. (DEV-088, DEV-089)

## 0.2.1-beta — 2026-06-23

### Added
- **메모 표시 3-모드**: 접기 / 고정(고정 높이 + 내부 스크롤, 드래그로 크기 조절·영속)
  / 확장(전체 높이). 기본 확장, 고정·확장 선택은 기억(접기는 매번 펼침). (DEV-189)
- **댓글 전체 접기/펼치기** 버튼 — 모든 댓글의 답글·본문을 일괄 토글(섹션 접기와
  별개). (DEV-190)
- 설치 동봉 문서에 `AGENTS_OPENGUILD_USAGE.md` 추가 (README/USAGE/CHANGELOG와 함께).
  (DEV-098)
- **시점 복원(journal replay)** — `backup restore --at <ISO8601-UTC>` / HTTP
  `/api/admin/restore` `at`: 최신 snapshot 을 복원한 뒤 journal(AOF) 을 그 시각까지
  재적용해 "마지막 백업 이후 ~ 임의 시점" 으로 복원. id 는 slug 경유로 재매핑(reindex
  대응). 내용 op(댓글/메모 본문)·type 변경·첨부가 낀 구간은 안전을 위해 거부하고
  full snapshot restore 를 안내(fail-loud). (DEV-022)

### Changed
- 메모 편집기의 '첨부' 버튼 제거(개인 메모). 이미지·동영상은 드래그&드랍 / Ctrl+V
  로 계속 첨부 가능. (DEV-188)
- 상세 점프 버튼(댓글로/메모로)을 양방향으로 — 타겟이 아래뿐 아니라 위로 지나갔을
  때도 표시(메모 영역에서 '댓글로' 노출). 퀘스트·캠페인. (DEV-191)

### Fixed
- 상세 페이지 재진입 시 스크롤이 맨 위로 초기화되던 문제 — 떠날 때 위치를 저장하고
  뒤로가기로 복귀 시 (콘텐츠 로드 후) 애니메이션 없이 복원. 퀘스트·캠페인. (DEV-192)

## 0.2.0-beta — 2026-06-22

v0.1.0-beta 이후의 대규모 개편 — 저장 모델을 "파일 진리원 + index.db 캐시"로
완성하고, 첨부·댓글·캠페인 기능과 CLI/문서를 정리했다.

### Added
- **첨부파일**: quest/campaign 본문 아래 첨부 섹션, drag&drop · 클립보드 paste ·
  버튼 업로드, 미디어 인라인 임베드, 첨부 삭제(orphan 파일/blob GC). CLI
  `quest/campaign attach list/add/remove`. (DEV-069/156/170/175, BUG-084)
- **댓글**: cross-link 자동완성(caret 팝업 + 실재 ID 제안), 이모지 반응,
  토론(discussion) 플래그 + 미해결 시 완료 차단 + 홈 "토론 댓글" 섹션.
  (DEV-171/108/142/148/149/150, BUG-082)
- **캠페인**: 본문 첨부, floating 점프 버튼, 배너 이미지.
- **코드 하이라이팅**: 본문 마크다운 코드블록 syntax highlighting — highlight.js
  로컬 번들(외부 통신 없음), 테마 토큰 매핑으로 다크/라이트 적응. (DEV-183)
- **CLI 확장**: `reindex`, `check drift/counters`, `index rebuild/vacuum`,
  `journal tail`, `info`, `backup new/list/remove`, `restore`, `template new`,
  `migrate-to-files`, `quest comment discussion/resolved`(토론 토글).
  (DEV-095/159/162/164/170/176/177/179/185)
- **호환성**: 길드 `schema_version` + 실행파일 호환 검사 + 안내 배너,
  미저장 변경 경고(라우트 이동). (DEV-064/154/153)
- **자동 업데이트**: Tauri updater 기반 + tag push 릴리즈 워크플로. (DEV-063/071)
- DB 캐시 엔티티(campaigns/types/statuses/tags) 외부편집 반영. (DEV-178)

### Changed
- **`openguild-server` = host 전용** — 중복 정비 서브커맨드 제거, 정비/진단은
  `openguild` CLI 또는 HTTP admin(`/api/admin/*`)로 일원화. (DEV-163/165)
- **CLI 명령 체계 정리**: 생성은 `new`(quest/campaign/template/backup), 하위항목은
  `add`, 삭제는 `remove`(구 `rm`). 정비는 `check`/`index`/`journal` 그룹.
  `reindex` = `index rebuild`. (DEV-176/177/179)
- **백업/복원을 파일 기반(RDB)으로** — index.db binary 사본이 아니라 `.guild/`
  소스 파일 스냅샷 → rules/댓글/메모/첨부까지 복원. (BUG-076)
- 스냅샷 타임스탬프는 UTC로 저장하고 표시할 때만 로컬 변환. (BUG-086)
- 외부 편집 시 `updated_at`을 파일 mtime으로 보정(quest + campaign). (BUG-080)
- 살아있는 문서를 현행 CLI/구조에 맞게 일괄 갱신. (DEV-166)

### Fixed
- drift 오탐(per-row/per-file mtime 비교). (BUG-067/068)
- reindex self-heal이 미참조 orphan 첨부를 부활시키던 문제. (BUG-087)
- `attach remove`가 없는 경로에도 성공 메시지를 내던 문제. (BUG-085)
- GUI 창이 안 닫히던 회귀(닫기/새로고침 가드 제거). (BUG-075)
- 아이콘 투명 배경 — 투명 소스로 아이콘 세트 재생성. (BUG-088)
- IncompatibleGuild 모달의 '업데이트 확인'이 무반응이던 문제 — 결과(확인 중/최신/
  새 버전/실패)를 인라인 표시. (DEV-154)
- 업데이트 확인 에러 분류 — `latest.json` 부재를 네트워크 오류로 잘못 안내하던 것
  수정. (BUG-045)
- 그 외 다수 (보드 필터 edge 디밍, 다크모드 토큰 오용, urgency clamp 경고 등).

### Known issues
- **per-comment 첨부 미구현** (DEV-181, On Hold) — 현재 댓글은 이미지/동영상
  인라인만 가능, 비미디어는 차단.
- `quest_history`가 index.db 전용이라 파일/스냅샷에 백업되지 않음 (DEV-180).
- 자동 업데이트는 릴리즈에 `latest.json` + 서명(`.sig`)이 첨부돼야 동작 —
  GitHub 서명 secret 설정 필요. (release-process.md, BUG-045)

## 0.1.0-beta

최초 베타. (이 CHANGELOG 도입 이전 — 상세 내역은 git 이력 참조.)
