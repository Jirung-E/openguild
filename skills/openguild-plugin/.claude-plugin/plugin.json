{
  "name": "openguild-plugin",
  "description": "Adds two openguild skills — using the openguild CLI/GUI to manage a project (guilds, quests, campaigns, comments), and writing openguild plugins (event hooks that call your own program).",
  "version": "0.6.0"
}
