<!-- og-comment id="1" ts="2026-07-10T11:54:11+09:00" author="claude" -->
구현 완료. 제안대로 진행:

- campaign_history DB 테이블 신설(quest_history 와 유사 스키마, campaign_slug 처음부터 포함).
- .guild/history/{campaign_slug}.jsonl — quest 와 같은 sidecar 디렉토리/repo::history 모듈 재사용(slug 형식 달라 충돌 없음 확인됨, 테스트로 검증).
- reindex 에 6c 단계로 export/rebuild 양방향 동기화 추가(5d 의 campaign 판).
- ops::campaigns::update_campaign 에서 status 변경 감지(no-op 스킵) 시 DB+사이드카 기록.
- CLI: campaign history <slug> (quest history 와 대칭 출력).
- server: GET /api/campaigns/{slug}/history. Tauri: campaign_history 커맨드 + transport.ts 매핑.
- GUI: 캠페인 상세 페이지 하단에 CampaignHistory.svelte 표시(QuestHistory.svelte 와 동일 패턴).

<!-- og-comment id="2" ts="2026-07-11T11:15:19+09:00" author="claude" -->
후속 수정(admin 보고: 캠페인 상태 변경이 worklog 에 안 잡힘): worklog 의 activities/daily_summary 가 quest_history + 댓글 + 생성만 UNION 하고 있어 DEV-226 신설 campaign_history 가 빠져 있었음. 두 쿼리에 campaign_history 브랜치 추가(kind='status', summary="old → new", slug=campaign_slug — campaign_comments 가 campaign_slug 를 싣는 기존 관례와 동일). 회귀 테스트 추가.
