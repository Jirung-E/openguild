<!-- og-comment id="1" ts="2026-06-22T22:08:23+09:00" author="claude" discussion="true" -->
[토론] 구현 방식 — 별도 `.guild/kb/` 신설 vs 기존 `rules` 메커니즘 일반화.

rules 는 이미 `.guild/rules/{slug}.md` 다중파일 CRUD + GUI 목록/편집 + cross-link 자동완성을 갖추고 있다. KB 를 rules 의 일반화(예: rules 에 'category'/디렉토리 구분을 두어 가이드라인 vs 지식 문서를 나눔)로 흡수하면 코드 재사용이 크고, 별도 kb 면 의미 분리가 명확하고 스키마가 단순하다.

논점: (a) rules 와 kb 의 개념 경계가 사용자에게 충분히 다른가? (b) 일반화 시 rules UI/route 가 복잡해지지 않나? (c) 프로젝트 repo 의 docs/ 와의 역할 중복. 어느 쪽으로 갈지 결정 필요.
