+++
quest_id = "DEV-039"
title = "Quest Detail — created_at / updated_at 표시"
status = "done"
urgency = 4
prerequisites = []
created_at = "2026-05-21T13:23:29Z"
updated_at = "2026-05-22T01:09:30+09:00"
deleted = false
+++

Quest Detail 헤더 아래에 생성일 / 최근 변경일 표시.

## 구현 (2026-05-21)

### Util
- `gui/frontend/src/lib/utils/datetime.ts` 신설:
  - `formatTs(s)`: `"2026-05-15 16:13:09"` (SQLite) / `"...T...Z"` (ISO) → `"YYYY-MM-DD HH:mm"` (로컬 시간대).
  - `formatRelative(s, now?)`: "방금" / "5분 전" / "3시간 전" / "2일 전" / 그 이상은 `formatTs` 결과.

### UI
- Quest Detail (`routes/quests/[id]/+page.svelte`) 헤더 뱃지 다음 줄에 `.meta-times` 추가:
  - `생성  2026-05-15 16:13` · `변경  3시간 전`
  - `<time datetime>` 시멘틱 태그, `title` 에 절대값 hover.
- `created_at` 은 절대값 (변하지 않음), `updated_at` 은 상대값 (직관적).

### 자동 테스트 +8 (frontend)
- `datetime.test.ts`:
  - `formatTs` SQLite / ISO / 빈 값 / 파싱 실패.
  - `formatRelative` 방금 / 분 / 시간 / 일 / 절대 / 미래 / 빈 값.

## 회귀

- 기존 페이지 레이아웃 영향 없음 — 헤더 직후 한 줄 추가.
- `QuestDetail` 타입에 `created_at` / `updated_at` 이미 존재 (Quest 상속).

## 검증

- `npm test -- --run` → 74 passed (이전 63 → **74**, +8 + setup용 1 환경).
- `npm run check` → 0 error / 0 warning.
- `npm run build` → success.

## 후속 (별도 quest 후보)

- Quest List 항목에도 updated_at 표시 (`X일 전`) — 정렬 키와 동기화.
- Locale 따라 한국어 / 영어 상대시간 토글 (다국어 quest 와 함께).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
