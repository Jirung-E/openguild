<!-- og-comment id="1" ts="2026-07-03T22:24:57+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03 작성)

**변경 파일**: `cli/src/main.rs` (Restore arm + `--at` doc), `core/src/replay.rs` 또는 CLI 단 전처리.

1. `--at latest` 키워드: CLI 에서 `at == "latest"` (대소문자 무시, `now`/`head` 도 alias 허용 여부는 구현 시 결정 — 일단 `latest` 만) 이면 `9999-12-31T23:59:59Z` 로 치환 후 기존 경로 그대로. core 는 변경 없음(파싱은 CLI 책임 유지 — HTTP admin 라우트도 동일 치환을 server 쪽에 추가할지 여부만 확인).
2. help 문구 갱신: `--at <ISO8601-UTC | latest>` — "latest = 최신 스냅샷 + journal 전체 재적용(최신 상태 복구)". restore-behavior 길드 규칙 문서도 갱신.
3. 출력 개선: latest 복구 시 "✓ 최신 상태로 복구: journal op N 개 재적용" 로 문구 분기(과거 시점 복원의 "이 시점 이후 변경 폐기" 경고와 구분 — latest 는 무손실이므로 그 경고 미출력).
4. 회귀 테스트: cli 단 문자열 치환 함수 분리(`resolve_at_keyword`) + 단위 테스트 (latest→미래 ISO, 일반 ISO passthrough, 대소문자).

**선행 없음 / DEV-212(자동 백업)와 독립** — 같은 restore arm 을 건드리니 순차 작업 권장 (DEV-212 먼저 하면 latest 경로도 자동 백업 혜택).
