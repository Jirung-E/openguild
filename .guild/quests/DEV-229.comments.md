<!-- og-comment id="1" ts="2026-07-10T13:55:15+09:00" author="" -->
구현 완료. 제안대로 진행:

- server/src/main.rs: `openguild-server.toml` 설정 파일 지원 — `--config <PATH>` 명시 또는 exe 옆 → cwd 순 자동 탐색(없으면 조용히 skip, --config 로 명시했는데 없으면 에러).
  ```toml
  [server]
  frontend_dist = "C:/path/to/frontend/build"
  ```
- frontend 위치 우선순위: `--frontend-dist`(신규 CLI 플래그) > `FRONTEND_DIST` env > 설정 파일 > 기존 자동 탐색.
- 시동 로그: static 서빙 중이면 경로 그대로, API-only 면 "frontend 자산 없음" + 지정 방법(3가지 다) 안내. 설정 파일을 실제로 읽었으면 그 경로도 로그에 표시.
