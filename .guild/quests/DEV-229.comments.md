<!-- og-comment id="1" ts="2026-07-10T13:55:15+09:00" author="claude" -->
구현 완료. 제안대로 진행:

- server/src/main.rs: `openguild-server.toml` 설정 파일 지원 — `--config <PATH>` 명시 또는 exe 옆 → cwd 순 자동 탐색(없으면 조용히 skip, --config 로 명시했는데 없으면 에러).
  ```toml
  [server]
  frontend_dist = "C:/path/to/frontend/build"
  ```
- frontend 위치 우선순위: `--frontend-dist`(신규 CLI 플래그) > `FRONTEND_DIST` env > 설정 파일 > 기존 자동 탐색.
- 시동 로그: static 서빙 중이면 경로 그대로, API-only 면 "frontend 자산 없음" + 지정 방법(3가지 다) 안내. 설정 파일을 실제로 읽었으면 그 경로도 로그에 표시.

<!-- og-comment id="2" ts="2026-07-18T01:12:54+09:00" author="claude" -->
재오픈 확인 후 재검증 (2026-07-17, Windows):
- repo root 실행: static = gui/frontend/build 정상 탐색.
- exe 디렉토리(target/debug)에서 실행: exe 조상 탐색으로 정상 발견.
- `--config` 오타 경로: "--config 로 지정한 파일이 없음" 에러 정상.

어떤 시나리오가 실패했는지 알려주세요. 참고: 리눅스에서 "메뉴바가 이전
버전처럼 보임" 증상은 이 탐색이 **오래된 gui/frontend/build 를 정상적으로
서빙**해서일 가능성이 큽니다(빌드 실패로 stale 산출물이 남은 경우) —
`rm -rf gui/frontend/build && just build` 후 재확인 부탁드립니다.
