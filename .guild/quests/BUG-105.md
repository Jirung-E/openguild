+++
quest_id = "BUG-105"
title = "[server/gui] 설치 없이 env 로 server host 시 GUI 확인 불가 — 조사"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-07-05T02:19:03+09:00"
updated_at = "2026-07-05T19:56:58+09:00"
deleted = false
+++

사용자 보고(2026-07-05): 별도 설치 과정 없이 환경변수(GUILD_PATH 등)로 `openguild-server host` 를 열었을 때 GUI 로 확인이 불가능함.

## 조사 필요 (증상 구체화 우선)
어느 경로가 실패하는지 재현 정보 필요:
(a) **브라우저 접속** 시 화면 안 뜸 — 유력: FRONTEND_DIST 미해석. DEV-195 가 exe 조상 디렉토리에서 gui/frontend/build 를 찾지만, **frontend 빌드 산출물이 없는 환경**(fresh clone / exe 만 복사)이면 API-only 로 떨어져 / 가 404. 서버 시동 로그에 static 서빙 여부가 안 보이면 인지 불가 — 최소한 'frontend 자산 없음, API-only 모드' 경고 출력 필요.
(b) **Tauri GUI 원격 연결** 실패 — Welcome 에서 URL 연결 확인/적용 단계 중 어디서 실패하는지.

## 개선 방향(조사 후)
- 서버 시동 시 static 서빙 상태를 명확히 로그(경로 + 미발견 시 안내).
- FRONTEND_DIST env 안내를 에러/시동 메시지에 포함.
- 문서(README/USAGE)에 '소스에서 서버만 띄우는 경우 frontend 빌드 선행' 명시 — DEV-224 와 연계.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
