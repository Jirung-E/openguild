<!-- og-comment id="1" ts="2026-06-15T16:57:53+09:00" author="claude" -->
구현 완료. commands.rs open_err() 가 Store::open 의 anyhow 에러를 downcast 해 AppError::IncompatibleGuild 면 sentinel(INCOMPATIBLE_GUILD_TAG) 로 태깅(두 open 커맨드 적용). welcome 의 handleOpenError() 가 이를 감지해 ConfirmDialog '호환되지 않는 길드' 안내 + '업데이트 확인'(checkForUpdate, DEV-063) 표시. open/init/recents/pickFolder 모든 경로 공통. npm check/clippy(gui) 통과. 검증: 마커의 schema_version 을 임시로 99 로 바꾼 길드를 welcome 에서 열면 전용 모달이 떠야 함(GUI 재빌드 필요).

<!-- og-comment id="2" ts="2026-06-22T21:46:00+09:00" author="admin" reply_to="1" discussion="true" resolved="true" -->
모달 뜨는거 확인. 근데 '업데이트 확인'을 눌러도 아무 반응이 없음

<!-- og-comment id="3" ts="2026-06-22T21:50:39+09:00" author="claude" reply_to="2" -->
원인 찾았습니다. UpdateBanner 가 available/downloading/ready 만 표시하고(DEV-085: checking/uptodate/error 같은 transient 는 전역 배너에서 제외), welcome 모달은 updateState 를 구독하지 않아서 '업데이트 확인'을 눌러도 화면에 아무 변화가 없었습니다. 특히 릴리즈에 latest.json 이 없으면 결과가 error 인데도 무반응이었습니다.

수정(bf352f8): welcome 에서 updateState 를 직접 구독해 결과를 하단 인라인 토스트로 표시 — 확인 중 / 최신 / 새 버전(+지금 업데이트) / 실패(사유). 이제 누르면 즉시 피드백이 뜹니다.

참고: 실제 자동 업데이트가 동작하려면 릴리즈에 latest.json + .sig 가 첨부돼야 합니다(서명 secret 필요). secret 미설정이면 이 토스트가 "업데이트 확인 실패: 릴리즈가 아직 없거나 endpoint URL 이 잘못됐습니다…" 로 뜹니다.
