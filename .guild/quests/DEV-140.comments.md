<!-- og-comment id="1" ts="2026-06-13T19:47:55+09:00" author="claude" -->
## 테스트 스텝 (사용자 GUI)
1. 본문에 [[DEV-001]](실재) 와 [[DEV-999]](미존재) 입력 후 저장.
2. 렌더에서 실재=파란 칩(클릭 시 해당 페이지 이동), 미존재=빨강 확인.
3. 편집기에서 'DEV-0' 타이핑 → 자동완성 '🔗 링크 걸기' 뜨고 선택 시 [[...]] 로 변환.
4. 캠페인 [[C-001]] 도 동일하게 동작.

<!-- og-comment id="2" ts="2026-06-14T21:31:40+09:00" author="admin" -->
다음과 같이 썼을때, 마크다운 표준 링크로 인식되는 문제
```txt
[[DEV-001]](...)    // 뒤에 괄호가 붙는 경우,
```
```txt
[DEV-001]           // (표준 링크로 인식됨)
```
```txt
DEV-001             // 이렇게 openguild의 상세페이지로 인식이 되어야함
```

<!-- og-comment id="3" ts="2026-06-14T21:35:17+09:00" author="admin" reply_to="1" -->
3번이 동작하지 않는다.

<!-- og-comment id="4" ts="2026-06-15T01:29:51+09:00" author="claude" reply_to="3" -->
## #3 '3번 안됨' 재확인 — 내가 오독했음

처음에 '3번'을 댓글 #2의 case3(bare DEV-001 렌더링)으로 봤는데, 이 댓글은 #1(테스트 스텝)에 대한 답글이라 **'3번 = 테스트 스텝 3 = 편집기 자동완성 '🔗 링크 걸기''** 가 맞다. 그걸 안 고쳤다. 미안.

**진짜 원인(자동완성)** → BUG-072:
편집기는 `markdown()`(기본 base=commonmark)인데 자동완성 소스를 GFM `markdownLanguage` 에만 등록해서, 커서 위치의 활성 언어와 불일치 → 소스가 안 잡혀 'DEV-0' 타이핑해도 후보가 안 떴음. commonmark/GFM 양쪽에 등록해 수정.

**정리:**
- 테스트 스텝 3 (자동완성) → BUG-072 (방금 수정).
- 댓글 #2 case3 (bare `DEV-001` 렌더 → 상세 링크) → BUG-071. **실재하는 ID 만** 링크(오탐 방지)이므로 존재하는 ID 로 테스트 필요.
- 댓글 #2 case1/2 (`[[DEV-001]](url)`, `[DEV-001]`): 이건 marked 가 먼저 표준 링크/텍스트로 파싱해버리는 비정상 입력. 의도된 경로는 bare `DEV-001` 또는 트레일링 괄호 없는 `[[DEV-001]]`. case1/2 자체를 openguild 링크로 강제 변환하려면 marked 전처리가 필요한데, 필요하면 별도로 진행.

검증: npm check 통과. 자동완성은 GUI 재빌드 후 'DEV-0' 등 타이핑해 확인 필요.

<!-- og-comment id="5" ts="2026-06-15T19:14:32+09:00" author="claude" reply_to="3" -->
'댓글에서 자동완성 안 됨' → BUG-073 으로 처리(테스트 중). 규칙·메모(CodeMirror)는 crossLinkAutocomplete 추가, 댓글(textarea)은 클릭형 인플로우 제안으로 구현. (렌더링은 기존대로 전역 동작.)
