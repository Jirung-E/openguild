<!-- og-comment id="1" ts="2026-08-12T23:16:25+09:00" author="codex" -->
## macOS CLI·Server tar.gz 실기 검증 완료 (2026-08-12)

Release workflow #14(커밋 `6b4af89d`)의 `openguild-macos-arm64-packages` artifact를 Apple Silicon macOS 26.6.1에서 직접 내려받아 검증했다.

- artifact ZIP SHA-256이 GitHub 표시 digest `ebd09df379235b70bcf7037584f154f21b876f4620a020d41913a5913290ba1d`와 일치했다.
- `openguild_0.5.0-beta_macos_arm64_cli-server.tar.gz.sha256` 검증이 통과했다.
- tar.gz 안에 `openguild`, `openguild-server`, `README.md`가 들어 있고 두 바이너리의 실행 권한(`755`)이 보존됐다.
- 두 바이너리 모두 arm64 Mach-O였고, 임시 `bin` 디렉터리에 README와 같은 `install -m 755` 방식으로 설치했다.
- Rust/Cargo를 사용하지 않고 `openguild --version`, `openguild-server --version`이 모두 `0.5.0-beta`를 출력했다.
- 폐기 가능한 임시 길드를 생성해 `openguild quest list`가 정상 동작했다.
- `openguild-server host --bind local --port 43149`가 정상 기동했고 `GET /api/quests`가 `[]`로 응답했다.

기존 사용자 설치본 보호를 위해 `/usr/local/bin` 대신 임시 `bin`을 사용했으며, 파일 복사 방식과 권한은 README 절차와 동일하게 재현했다.
