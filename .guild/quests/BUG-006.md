+++
quest_id = "BUG-006"
title = "QuestBoard arranging 변수 $state() 누락 — Svelte 5 reactivity 안 됨"
status = "open"
urgency = 3
prerequisites = []
created_at = "2026-05-17 15:52:52"
updated_at = "2026-05-17 15:52:52"
deleted = false
+++

## 증상

`gui/frontend/src/lib/components/QuestBoard.svelte:176` 의 `arranging` 변수가
일반 `let` 선언 — Svelte 5 의 reactivity 시스템이 추적 못 함. 값이 바뀌어도 UI
재렌더 안 트리거.

`svelte-check` 출력:
```
WARNING "src\lib\components\QuestBoard.svelte" 176:6
  "`arranging` is updated, but is not declared with `$state(...)`. Changing its
  value will not correctly trigger updates"
  https://svelte.dev/e/non_reactive_update
```

## 영향

자동 정렬 (`autoArrange`) 등이 실행 중일 때 UI 에 "정렬 중..." / spinner 등 시각
표시가 의도대로 안 뜰 가능성. 동작 자체는 됨 — 사용자 피드백만 미흡.

## 수정

```svelte
// before
let arranging = false;

// after
let arranging = $state(false);
```

`gui/frontend/src/lib/components/QuestBoard.svelte` 의 line 176 (또는 그 근처
선언). 다른 미선언 reactive 변수도 같이 점검 권장 — `svelte-check` 결과는 1건이지만
runes mode 전환 과정에서 비슷한 게 더 있을 수 있음.

## 우선순위

`urgency = 3`. 기능 영향은 작음 (시각 피드백 누락). 그러나 Svelte 5 reactivity
원칙 위반 — 다른 변수 변경 시 사이드이펙트 가능. 짧고 명확한 수정.

## 재현

`cd gui/frontend && npm run check` → 9 warnings 중 첫 항목 (QuestBoard:176).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
