+++
quest_id = "DEV-072"
title = "[chore] 'OpenGuild' → 'openguild' 소문자 통일 (사용자 가시 텍스트 + 문서)"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-05-25T14:20:54+09:00"
updated_at = "2026-05-25T14:25:26+09:00"
deleted = false
+++

사용자 결정 (2026-05-25): 프로젝트 전반의 'OpenGuild' 표기를 전부 소문자 'openguild' 로 통일. 영향: README / docs / quest body / UI 표시 (Nav, welcome, admin title) / Cargo.toml description / tauri.conf.json productName / NSIS template UI 텍스트 등 27개 파일. Rust struct / type 이름 (PascalCase 식별자) 은 영향 없음.

## 구현 (2026-05-25)

### 변경 방식
일괄 `sed -i 's/OpenGuild/openguild/g'`. 사전 검증:
- `(struct|enum|fn|impl|type|trait|const) +OpenGuild` grep → 0개
  (코드 식별자로는 안 쓰임 → 안전한 global replace).
- `.guild/quests/*.md` 제외 (사용자가 직접 관리하는 quest body).
- `target/` / `node_modules/` / `.svelte-kit/` / `build/` / `.claude/` /
  `gen/` 제외 (생성물 / 외부).

### 영향 파일 (27)
- `README.md` / `AGENTS.md` / `CLAUDE.md`.
- `docs/*.md` 7개.
- `gui/{Cargo.toml,README.md,src/lib.rs,tauri.conf.json,capabilities/default.json,nsis/installer.nsi}`.
- `gui/frontend/src/{lib/components/Nav.svelte,routes/admin/+page.svelte,routes/welcome/+page.svelte}`.
- `core/{Cargo.toml,src/lib.rs,src/repo/mod.rs}`.
- `cli/{Cargo.toml,src/main.rs}`.
- `server/{Cargo.toml,src/main.rs}`.

### 사용자 가시 효과
- 윈도우 타이틀바 / 시작 메뉴: "openguild".
- Nav / Welcome 로고: "openguild".
- Installer 마법사 페이지 / Add-Remove Programs entry: "openguild".
- README / GitHub 페이지: 전부 소문자.

### 의식적 결정
- `fileAssociations[].name` 의 "openguild Guild File" 처럼 "Guild" 가
  뒤따르는 경우 있음 — "Guild" 는 일반 용어라 그대로. 사용자 요청은
  브랜드명 "OpenGuild" 의 소문자화.
- Windows 레지스트리 key (예: 옛 설치본의 `Software\openguild\OpenGuild`)
  와 다른 케이스 — 신설은 모두 소문자. 정식 배포 전이라 호환성 영향 없음.

### 검증
- `cargo test --workspace -- --test-threads=1` → 403 passed.
- `cargo clippy --workspace --all-targets -- -D warnings` → 0 errors.
- `npm run check` → 0 errors (1 pre-existing warning).
- `npm test` → 116 passed.
- `npm run build` → ✔.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
