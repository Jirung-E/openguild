+++
quest_id = "DEV-024"
title = "테스트 status 신설 + 워크플로 정착"
status = "done"
urgency = 2
prerequisites = []
created_at = "2026-05-17 07:34:26"
updated_at = "2026-05-17 07:34:27"
deleted = false
+++

in_progress → testing → done 흐름 도입. testing 단계로 보낼 때 quest 본문에 테스트 방법 첨부 필수. AGENTS_OPENGUILD_USAGE.md 에 워크플로 명시 + AGENTS.md 에서 강제 참조.

## 변경 사항

- `.guild/statuses/3-testing.toml` 신설 (`Testing` / `테스트 중` / `#A47AE2`).
- 기존 `done`, `cancelled`, `on_hold` 의 sort_order + 파일명을 4/5/6 으로 재번호.
- `core::repo::seed::default_statuses` 갱신 — 6 statuses 시드.
- 관련 seed/reindex 테스트 갱신 (5 → 6).
- `AGENTS_OPENGUILD_USAGE.md` 2.3 절: 상태 흐름 (**권장** 워크플로) + 자동 테스트
  통과 시 done 허용 / 수동 검증 필요 시 testing + 테스트 방법 첨부 규칙.
- `AGENTS.md` Dogfood 섹션: 📌 AGENTS_OPENGUILD_USAGE.md 참조 표기.
- 본 dogfood 길드 reindex 적용 (6 statuses, 24 quests, 4 dependencies).
- 최근 done 처리됐던 DEV-002, DEV-019 를 testing 으로 되돌리고 테스트 방법 작성.

## 테스트 방법

- `cargo test --workspace` → Rust 191 통과 (core 110 + cli 25 + server 56).
- `cd gui/frontend && npm test -- --run` → 51 통과 (frontend 변경 없음, 회귀 확인).
- `.guild/statuses/` 가 6개 파일: 1-open / 2-in_progress / 3-testing / 4-done / 5-cancelled / 6-on_hold.
- `openguild quest status DEV-XXX testing` → frontmatter `status = "testing"` 으로 갱신, 파일 자동 작성.
- `openguild-server reindex` → statuses 6 / quests 24 / dependencies 4.
- `openguild-server check-drift` → drift 없음 (또는 reindex 후 정상).
- `AGENTS.md` 의 🚨 블록이 명확히 AGENTS_OPENGUILD_USAGE.md 강제 참조.
- 새 빈 길드 init 시 statuses 6개 모두 시드되는지 — `openguild init --name x` (별도 디렉토리).

## 알려진 이슈

- `openguild quest update --description` 에 multi-line 문자열 전달 시 첫 줄만 반영됨 (shell escaping 문제 추정). 본 quest 작업 중 우회로 파일 직접 편집. 별도 quest 로 추적 필요 (DEV-025 후보).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
