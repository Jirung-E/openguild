<!-- og-comment id="1" ts="2026-06-19T00:49:15+09:00" author="claude" -->
# 작업 기록(Worklog) 설계안

## 한 줄 정의
"누가 / 언제 / 무슨 작업을 했나"를 날짜·기간별로 모아 보는 화면.
기록 = ① 자동(시스템이 캡처한 활동) + ② 수동(그날 한 일 메모).

## ① 자동 활동 — 데이터원: quest_history (채택) vs journal.db (탈락)
- **journal.db (AOF)**: snapshot 생성 시 **truncate(절단)** 됨 → 과거가 사라져
  누적 기록용으로 부적합. 디버그/감사 전용으로 유지.
- **quest_history (채택)**: 영구 보존 + `actor`(누가) 보유. 상태 변경/부모 변경 등
  구조화된 활동에 적합.
- 보강: 댓글 = `quest_comments`/`campaign_comments`(author+ts), 생성 = history 의
  create op.
- 결론: **quest_history 주축 + comments 보강**.

## ② 수동 메모 — 저장 위치
- 파일=진리원 + git 추적(공유/어드민 가시) → `.guild/worklog/{user}/{YYYY-MM-DD}.md`
- 사용자별 폴더 격리 · 날짜별 1파일. index.db 에 캐시(빠른 조회), 파일이 진리.

## 유저별 기록 + 어드민 전체 보기
- 모든 활동은 `actor`/`author`/`user_id` 로 사용자에 귀속 → "내 기록" = 현재 사용자 필터.
- **개인 화면**: 본인 활동 + 본인 메모만.
- **어드민 페이지**: 모든 사용자, 사용자별 필터/그룹으로 확인.

### ⚠ 선행 의존성 (중요)
현재 openguild 는 인증/유저 모델이 MVP 스텁(server admin.rs: "인증 없음 — 멀티유저
단계 진입 시 토큰/role 가드"). "현재 사용자" 식별과 "어드민 role" 은 **유저 식별/role
기능이 선행**되어야 한다. (quest_memos 의 `user_id` 컬럼은 그 준비.)
→ 권장: 유저 식별/role 을 **선행 quest** 로 분리. 작업 기록은 처음부터 `user_id`
키로 설계해 단일 사용자(=local)에서 동작시키고, 멀티유저가 붙으면 어드민 전체보기가
자연히 활성화되도록 forward-compatible 하게 구현.

## 범위 분해(제안)
1. (선행) 유저 식별 + role(admin) 모델.
2. worklog 코어: quest_history+comments 집계 쿼리(기간/유저 필터) + 메모 파일 CRUD.
3. GUI: 개인 작업 기록 화면(목업 참조: 기간 세그먼트·주간 요약·날짜별 활동+메모).
4. GUI: 어드민 페이지 '전체 사용자 작업 기록' 뷰.

<!-- og-comment id="2" ts="2026-07-03T22:35:06+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03) — 본문 범위(전역 뷰, 유저 구분 없음) 기준

## 데이터 소스 (본문 확정사항 반영)
- **자동 활동**: quest_history(상태/타입 변경 등 — 주축) + quest/campaign comments(작성 ts). journal 은 truncate 되므로 제외(확정).
  ⚠️ 선행 이슈: quest_history 는 index.db 전용이라 스냅샷/파일 백업이 없음(DEV-180) — worklog 가 이걸 주축으로 쓰면 restore 후 활동 기록이 증발. **DEV-180 을 prereq 로 걸 것 권장.**
- **수동 메모**: `.guild/worklog/{YYYY-MM-DD}.md` — 파일 진리원 + git. frontmatter 불필요(날짜=파일명, 본문만). index 캐시 테이블 worklog_notes(date, content) + reindex/snapshot(SOURCE_SUBDIRS) 포함.

## 구현 단계
1. **core**: worklog repo(파일 IO) + ops(get/set/clear by date) + 캐시/reindex/snapshot. 활동 집계 쿼리 — `activity(from, to)`: quest_history + comments 를 ts 범위로 UNION, (ts, kind, quest_slug, summary) 정렬 반환.
2. **server/CLI**: `GET /api/worklog?from&to`(활동+메모), `PUT /api/worklog/{date}`(메모). CLI `openguild worklog show <date|range>` / `worklog memo <date> --file`.
3. **GUI**: admin 하위(또는 Nav) `/worklog` — 일/주/월 토글, 날짜 네비게이션, 자동 활동 타임라인(quest 링크) + 그 날짜 메모 편집기(DEV-203 MarkdownEditor 재사용 — 순서상 203 먼저 권장). UI 는 세션 목업 참조(본문 언급).
4. 테스트: 집계 쿼리(범위 경계/타임존 — BUG-086 교훈: UTC 저장·로컬 표시), 메모 왕복.

## 선행 제안
- DEV-180(quest_history 파일 백업) — 데이터 내구성.
- DEV-203(MarkdownEditor) — 메모 편집기 재사용.
후속(범위 밖 확정): 유저별 분리 = DEV-168.

<!-- og-comment id="3" ts="2026-07-06T22:56:36+09:00" author="claude" -->
후속 3건 (admin 보고, 2026-07-06):

1. **날짜 선택이 처음 값으로 되돌아오는 버그 수정** — URL(?date=) 동기화
   $effect 가 anchor 도 반응형 의존이라, 날짜를 고르면 goto(비동기)로 URL
   이 갱신되기 전에 effect 가 재실행돼 "옛 URL 값 ≠ anchor" 조건에 걸려
   anchor 를 되돌렸음. lastUrlDate 가드로 URL 값이 실제로 바뀔 때만 반응.
2. **임의 구간 뷰 추가** — 일/주/월 옆 '구간' 토글: 시작/끝 date input
   두 개로 자유 기간 조회 (역순 입력은 자동 스왑). 노트는 기간 내 일별
   나열, 타임라인은 날짜 구분선 그룹핑 (주/월과 동일).
3. **월 뷰 전용 선택기** — 월 뷰에선 date 대신 native `<input type="month">`
   (YYYY-MM 만 고름).
