<!-- og-comment id="1" ts="2026-06-06T14:18:51+09:00" author="claude" -->
## 추가된 server CLI

### `vacuum`
```
$ openguild-server vacuum
VACUUM 실행 중...
✓ VACUUM 완료
  before : 733184 bytes
  after  : 720896 bytes
  saved  : 12288 bytes (1.7%)
```

SQLite VACUUM 명령으로 deleted row 의 공간 회수. snapshot 직전 또는 큰 reindex 후 권장.

### `journal-tail [-n N]`
```
$ openguild-server journal-tail -n 3
journal.db: 18 row(s) total — showing last 3

#    16  2026-06-06T05:06:06Z  add_comment_entry
         args   : {"author":"claude","len":1660,"parent_id":null,"slug":"DEV-093"}

#    17  2026-06-06T05:09:44Z  ...
```

journal.db 의 최근 N row (op + ts + args + result). debug / audit / 변경 추적용. journal.db 가 없으면 명시 안내.

## 테스트
1. 빈 길드 → `journal-tail` → "(journal.db 없음 — ...)" 안내.
2. quest mutation 1~5 회 → `journal-tail` → 같은 수 row 표시.
3. `journal-tail -n 1` → 가장 최근 1 개만.
4. `vacuum` → before/after/saved 출력 + index.db file size 감소.
5. `vacuum` 직후 `journal-tail` → 동일 row count (vacuum 은 index.db 만).

## BUG-016 정책
- doc comment 에 quest_id prefix (DEV-023:) 안 씀 — // 일반 주석으로.

## 향후 (별도 quest)
- `journal-tail` 의 args / result JSON pretty print 옵션.
- 특정 op 만 필터 (`--op create_quest`).
- snapshot 자동 트리거에 vacuum 옵션.

## 자동 검증
- cargo test -p openguild-server 142 pass.
- cargo clippy --workspace -- -D warnings exit 0.
- BUG-016 help_output_has_no_quest_id_leaks 통과.
- 실 동작 (vacuum + journal-tail) 검증 완료.

<!-- og-comment id="2" ts="2026-06-17T02:08:04+09:00" author="admin" -->
이 명령들은 일반 cli에도 있어야 하는거 아닌지?  
또한 gui에서도 확인 가능해야하는거 아닌지?

<!-- og-comment id="3" ts="2026-06-17T02:12:47+09:00" author="claude" reply_to="2" -->
DEV-159(check-drift)와 같은 parity 이슈로 동의합니다 — 둘 다 일반 CLI + GUI 노출이 맞습니다.

- `vacuum`: index.db 는 재생성 가능한 캐시지만 soft-delete 누적 후 공간 회수에 유용 →
  `openguild vacuum` (Local 전용). GUI admin 에 '정리(VACUUM)' 버튼.
- `journal-tail`: journal.db = AOF(op 로그)라 audit/debug 용 →
  `openguild journal-tail [-n]` (Local 전용). GUI admin 에 '최근 작업(저널)' 뷰.
- 원격 모드는 check-drift 와 동일 정책: server 명령 안내.

후속 quest **DEV-162** 로 추적합니다 (prereq: DEV-023).
