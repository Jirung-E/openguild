<!-- og-comment id="1" ts="2026-06-15T16:06:39+09:00" author="claude" -->
구현 완료(백엔드). guild_file: CURRENT_SCHEMA_VERSION(1) + GuildFile.schema_version(default 1) + marker_content() 공용 헬퍼 + schema_compat(). Store::open 이 Newer schema 면 IncompatibleGuild 로 안전하게 거부('앱을 업데이트하세요'), 마커 없으면 통과, Older 는 migration 자리(현재 미발생). error: IncompatibleGuild 추가(server 409). CLI init/GUI create 가 marker_content 로 schema_version 기록. 테스트 2종 + 전체 cargo test/clippy 통과. GUI 전용 안내 페이지+업데이트 버튼은 DEV-154 로 분리(선행 DEV-064).
