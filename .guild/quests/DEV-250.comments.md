<!-- og-comment id="1" ts="2026-07-11T16:48:43+09:00" author="claude" -->
구현 완료 — 6개 항목 전부:

1~3. 메타 배지 공통화: comment_badges()(📌/●미해결/✓해결) + reactions_summary()([👍2 ✅1]) 헬퍼를 list 한 줄/show 헤더/comments 횡단 세 출력이 공유. 횡단 검색은 반응이 DB 캐시에 없어 migration 0027 로 quest_comments/campaign_comments 에 reactions 컬럼 추가(마커 attr 원문 저장, pinned/edited_at 과 동일 패턴 — upsert/reindex 동기화, 토글 경로는 기존 upsert 를 타므로 자동 신선).
4. `comment list --tree`: parent_id 들여쓰기 트리(--reverse 와 배타, 필터로 부모 빠진 답글은 root 표시).
5. `comment show --depth all`: value parser 를 "숫자 또는 all(=usize::MAX)" 로 — select_thread 는 frontier 소진 시 break 라 무제한 안전.
6. 미존재 slug: list/show 에서 결과 0건일 때만 실존 확인(정상 경로 비용 0) — 없으면 명시적 에러.

json 출력에도 pinned/discussion/resolved/reactions 필드 추가(additive). CLI 파싱 테스트 2건 추가, AGENTS_OPENGUILD_USAGE 갱신. 실기: 미존재 slug 에러/트리/depth all/✓해결 배지/횡단 출력 확인.
