<!-- og-comment id="1" ts="2026-06-15T15:11:34+09:00" author="claude" -->
구현 완료.

**공용 가드** (stores/unsaved.ts + +layout):
- 편집기들이 dirty 를 key 로 보고 → 하나라도 dirty 면 이탈 차단.
- 라우트 이동/뒤로·앞으로가기: beforeNavigate → cancel + 공용 ConfirmDialog("편집 중 이동 / 버리고 이동").
- 새로고침/외부 이동: beforeunload → 브라우저 기본 경고.
- 창 닫기(X): Tauri onCloseRequested → preventDefault + 모달(확인 시 close()). capabilities 에 core:window:allow-close 추가.

**보고 연결**: quest 상세 / campaign 상세 / rules / 댓글(작성·편집·답글) / 메모.

**설계 메모 / 한계(검토용)**:
1. dirty 판정은 보수적으로 '편집 모드 진입' 기준 — 실제 변경이 없어도 편집을 열어둔 채 이탈하면 경고함(누락보다 안전 우선). 추후 실제 변경 비교로 정밀화 가능.
2. '버리고 이동' 확정 시 전체 dirty 를 해제(clearUnsaved). 컴포넌트 재사용으로 입력 버퍼가 남는 경우는 다음 변경 전까지 재경고 안 될 수 있음(엣지).
3. NewQuestModal(새 퀘스트 작성 폼)은 모달이라 이번 범위에서 제외 — 필요하면 후속.

검증: npm check / cargo build(gui) 통과. drag/네비/새로고침/창닫기 경고는 GUI 재빌드 후 확인 필요(창 닫기 가드는 새 capability 때문에 재빌드 필수).

<!-- og-comment id="2" ts="2026-06-15T19:34:43+09:00" author="claude" -->
스코프 축소(BUG-075): 창 닫기(onCloseRequested) 가드가 WebView2 에서 창을 못 닫게 하는 회귀가 있어 제거함. beforeunload(새로고침) 가드도 안전상 제거. 따라서 현재 미저장 경고는 'SPA 라우트 이동(beforeNavigate + 공용 모달)' 에만 적용 — 앱 종료/새로고침/창 닫기는 경고 없이 진행됨. 창 닫기 시 경고는 안전한 Tauri 방법(예: 검증된 onCloseRequested+ask dialog) 확보 후 재도입 필요.

<!-- og-comment id="3" ts="2026-06-15T19:36:42+09:00" author="admin" reply_to="2" -->
그렇게 진행 해봐라.
