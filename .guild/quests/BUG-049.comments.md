<!-- og-comment id="1" ts="2026-06-07T23:43:42+09:00" author="" -->
근데 다시시작할때뿐만 아니라 페이지 전환시나 다른 명령이 있을때도 해야하지 않을까? 근데 이때 병목이 발생하면 곤란.

<!-- og-comment id="2" ts="2026-06-07T23:51:09+09:00" author="claude" -->
## 페이지 전환 / 다른 명령 시 reindex 검토 (2026-06-07)

### 비용 분석
`drift::auto_resync` 호출 비용:
1. `drift::detect_drift` — `.guild/` 의 모든 파일 mtime 스캔 + DB updated_at 비교. **모든 파일 stat 호출** — n_files × syscall.
2. drift 있으면 `reindex` — 전체 파일 재파싱 + DB rewrite. **수십~수백 ms** (길드 크기 비례).

### 페이지 전환 시 매번 호출?

#### 단점
- 사용자가 quest list ↔ board ↔ detail 빠르게 이동하면 매 클릭마다 file stat 수십~수백 회 + 필요시 reindex. UI 지연 (특히 detail 진입).
- detail 페이지가 좀 느려진다는 사용자 피드백 가능성.

#### 장점
- CLI / 외부 편집기로 동시 작업 시 즉시 반영.

### 절충안 제안

**A. 드물게 + 조건부**
- 페이지 전환 시 매번 X. **`detect_drift` 만 background tick** (예: 30 초 인터벌).
- drift 발견 시에만 reindex 호출 + UI toast `'외부 변경 감지 — 새로고침됨'`.
- 사용자 액션 (페이지 전환) 과 비동기 — UI block 없음.

**B. mutation 후만 invalidate**
- 자기 GUI 의 mutation (create / update / status change) 후엔 캐시 이미 정합 (ops 가 file+DB 양쪽 갱신).
- 페이지 전환 자체에 reindex 필요 없음 — A 의 background tick 만 의존.

**C. file watcher**
- Tauri 의 `notify` crate 로 `.guild/` 감시. file 변경 이벤트 → drift 즉시.
- 가장 정확하지만 구현 복잡 + 본인 GUI 의 write 도 이벤트 발생 → debounce 필요.

### 권장
B + A 조합 — mutation 은 자체 해결, **외부 변경은 30~60s background drift check** 로 잡음. UI 지연 0 + 자연스러운 갱신. C 는 별도 quest (예: DEV-095 후속) 로 단계적 도입.

확인 후 BUG-049 본 라운드에서 어디까지 할지 알려주면 구현.

<!-- og-comment id="3" ts="2026-06-11T19:29:34+09:00" author="claude" reply_to="1" -->
(#1 질문에 대한 답)

페이지 전환 / 다른 명령 시 reindex 는 **불필요** 결론 — DEV-121 (startup incremental sync) 가 구조적으로 해결.

- 시동 시: `incremental::sync_on_open` 이 변경된 파일만 stat() 비교로 cheap 하게 UPDATE (< 50ms). 우려하셨던 병목이 발생하지 않는 구조.
- 페이지 전환 시: GUI 가 떠 있는 동안의 mutation 은 모두 ops 경로 (DB + 파일 동시 갱신) 라 stale 자체가 안 생김. 외부 편집 (CLI/에디터) 만 예외인데, 이는 DEV-121 Phase 2 (detail 페이지 진입 시 그 파일 하나만 lazy stat 체크 — 비용 ~1ms) 로 커버 예정 (보류 중).
- 즉시 동기화가 필요하면 Nav 의 ⟲ Reindex 버튼 (DEV-095) / admin Reindex (DEV-120, 자동 새로고침 포함).

상세: DEV-121 본문 참조.

<!-- og-comment id="4" ts="2026-06-14T15:00:40+09:00" author="claude" -->
구현돼 있음 — gui/src/lib.rs 가 시동 시 incremental::sync_on_open 호출(자동 동기화). 단 (1) incremental 이 last_indexed_at 을 안 올려 admin drift 표시기가 오탐, (2) quest 파일만 범위라 비-quest 외부수정은 ⟲ 필요. 상세 + 정리 방향은 DEV-121 댓글 참고.

<!-- og-comment id="5" ts="2026-06-14T15:11:24+09:00" author="claude" -->
진짜 원인: sync_on_open 이 초기 길드에서만 돌고 Welcome 으로 연 길드엔 안 돌던 swap 누락 → 고침(fc377b6). 재빌드 후 Welcome 으로 길드 열어 외부 편집 → admin drift clean 인지 확인. 상세는 DEV-121 댓글 참고.

<!-- og-comment id="6" ts="2026-06-14T18:58:06+09:00" author="claude" -->
## 테스트 스텝 (갱신)
1. 앱 실행 → Welcome 로 길드 열기 전/후 외부에서 quest 파일 수정.
2. 길드 진입 직후 ⟲ 안 눌러도 변경 반영 + admin drift clean.
(이전엔 open_guild_in_current_window/init_and_open_guild 의 swap 경로에서 sync_on_open 이 빠져 stale 가 남았음 — fc377b6 수정. 초기 길드만 sync 되던 게 원인.)
