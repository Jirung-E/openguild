<!-- og-comment id="1" ts="2026-08-02T18:33:58+09:00" author="claude" -->
﻿원인: DEV-288 은 사이드카(.guild/history/{slug}.jsonl)만 쓰고 doc_history 캐시 투영을 **reindex 에만** 맡겼다. 평소 reindex 를 돌 일이 없으니 작업기록에는 아무것도 안 뜬다. 재현: 새 길드에서 rule new / library new 직후 worklog → (no activity), reindex 후 → 표시.

수정: ops/doc_history.rs 신설 — 사이드카 기록과 동시에 doc_history 에 투영(파일→캐시 일방향, BOOK-001 유지). rename 은 캐시의 옛 slug 행도 이동(안 하면 작업기록 항목이 없는 문서를 가리킴), delete 는 캐시 행 정리(reindex 가 dangling 사이드카를 skip 하는 것과 같은 결과). 규칙/도서관 7개 호출부 전환.

검증: 임시 길드에서 rule/book create·update 가 **reindex 없이** 바로 표시(doc changes 3), reindex 후에도 중복 없음, rename 후 지난 항목이 새 slug 를 가리킴. 회귀 테스트 ule_and_book_changes_appear_without_reindex 추가.
