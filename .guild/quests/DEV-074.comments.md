<!-- og-comment id="1" ts="2026-06-06T13:47:43+09:00" author="claude" -->
## 현황 (step 1 — 토큰 + store + 토글)

본 quest 의 spec ("다크/라이트/시스템 선택") 의 backbone 만:

1. CSS variable 토큰 (--bg / --text / --accent 등) 의 dark / light 정의.
2. `<html data-theme="dark|light">` 로 swap.
3. theme store (writable + localStorage 영속).
4. Settings 의 표시 섹션에 [다크 | 라이트 | 시스템] 세그멘티드.
5. OS 의 `prefers-color-scheme` 변경 listener (system 모드만 자동 반영).
6. `color-scheme` CSS property 토큰화 → native input (date picker 등) 도 테마 일관.

## 사용
- Settings → 표시 → 테마 토글.
- 즉시 반영 + 자동 저장 (`openguild.theme`).
- "시스템" 선택 시 OS 의 다크/라이트 설정에 따라 자동.

## 한계 / 향후 (별도 sub-quest 권장)
대부분의 컴포넌트가 hardcoded color (#161b22 / #c9d1d9 등) — 본 라운드에선
global.css 의 html / body 만 var() 화. 점진 마이그레이션 필요:

- Nav / Home / QuestBoard / QuestList / QuestDetail / Campaign* / Settings 의 hardcoded color → var() 로.
- input / button / badge / modal 의 색 토큰화.
- light 테마에서 시각적 일관성 검증.
- color contrast (WCAG) 검증.

→ DEV-074 의 sub-quest 들로 분할 권장. 각 component 단위.

## 테스트
1. Settings → 표시 → "라이트" → background 흰색 / text 검정 (단 일부 component 는 dark 그대로 — 점진 마이그레이션 필요).
2. "시스템" → OS 다크/라이트 변경 시 즉시 반영.
3. 새로고침 → 마지막 선택 유지.
4. localStorage `openguild.theme` 직접 변경 → 다음 새로고침에 반영.

## 자동 검증
- `npm test theme` — 7 vitest (default / 영속 / garbage / setTheme / resolveTheme / applyThemeToDocument).
- `npm run check` 0 errors.

<!-- og-comment id="2" ts="2026-06-06T14:49:42+09:00" author="claude" -->
## DEV-074 마이그레이션 라운드 (backbone 후속)

본 라운드 25+ file 의 hardcoded color → var() 일괄. light theme 전환 시 주요 페이지 대부분 시각 일관.

## 마이그레이션 진척

### 완료 (이 라운드)
| Commit | Component / Page | Hardcoded → var() |
|--------|------------------|-------------------|
| 1 | Nav.svelte | 19 색 + nav-bg / nav-border / nav-hover-bg token 신설 |
| 2 | Settings | 42 색 |
| 3 | Home / QuestList / QuestListFilter / QuestListItem / Quest Detail / Campaign Detail / CampaignCard / MarkdownView / UpdateBanner / SchemaAheadBanner | 247 색 |
| 4 | QuestBoard / QuestCombobox / CampaignCarousel / CampaignCombobox / CampaignConveyor / NewQuestModal / QuestHistory / QuestNodeConveyor / QuestCommentsSection / QuestNoteSection / AdminStatusesSection / routes/admin / routes/welcome | 302 색 |

총 ~610 색 매핑 (대소문자 무시 regex 일괄).

## 매핑 표 (사용한 PowerShell regex)

```
#0d1117 → var(--bg)
#161b22 → var(--bg-elevated)
#1c2128 / #21262d → var(--bg-subtle)
#30363d → var(--border)
#e6edf3 → var(--text-strong)
#c9d1d9 → var(--text)
#8b949e → var(--text-muted)
#6e7681 / #484f58 → var(--text-faint)
#58a6ff / #4a9eff → var(--accent)
#56d364 / #3fb950 / #7ee787 → var(--success)
#2ea043 / #238636 → var(--success-strong)
#f85149 / #e94f4f → var(--danger)
#f5a623 → var(--warning)
```

## 잔여 (별도 sub-quest)
- Custom 색 (특정 component 의 unique 색 — gradient / rgba / 보라/주황 톤 등).
- WCAG color contrast 검증 (light theme).
- 일부 file 의 잔여 hardcoded color (`grep -r "#[0-9a-fA-F]\{6\}" gui/frontend/src/lib/components/`).

## 사용
Settings → 표시 → 테마 [다크 | 라이트 | 시스템].

- 다크: 기존 동작.
- 라이트: GitHub light 스타일 (흰 bg + 검정 text + light gray border / nav).
- 시스템: OS 설정 따라 자동.

## 자동 검증
- `npm run check` 0 errors (기존 QuestBoard.sorted warning 만 — DEV-074 무관).

<!-- og-comment id="3" ts="2026-06-06T17:46:27+09:00" author="" -->
노드가 다크모드/라이트모드 관계 없이 항상 회색으로 표시되는데 매우 불편.

또한 배경도 항상 검은색으로 출력되는 경우가 있다.

이외에도 더 있을것으로 예상.

이 작업을 시작하기 전의 테마가 기본적으로 다크였으니 라이트모드 테마만 만들어서 바꾸면 되는거 아닌지?

<!-- og-comment id="4" ts="2026-06-06T18:15:51+09:00" author="claude" -->
## 사용자 보고 (2026-06-06)
"노드가 다크/라이트모드 관계 없이 항상 회색으로 표시. 배경도 항상 검은색. 이외 더 있을 것."
"이 작업을 시작하기 전의 테마가 기본적으로 다크였으니 라이트모드 테마만 만들어서 바꾸면 되는거 아닌지?"

## 원인
Cytoscape 의 노드 background 와 SVG 안 fill 색은 CSS var() 를 컴퓨팅 못 함 (Cytoscape 의 style 시스템 + SVG data URL 의 분리된 doc context 둘 다).

- URGENCY_BG: 4 가지 dark 톤 hardcoded.
- makeSvgUrl 의 SVG: `fill="var(--text)"` — data URL 에서 var() inert.
- 결과: theme 무관 같은 색.

## 수정

### 1. URGENCY_BG_LIGHT 추가 + urgencyBgFor(urgency, theme) 헬퍼
`gui/frontend/src/lib/types/index.ts`:
- URGENCY_BG_LIGHT (옅은 빨강 / 주황 / 노랑 / 회색).
- urgencyBgFor 가 theme 인자로 dark/light 매핑.

### 2. QuestBoard 의 makeSvgUrl 가 theme 별 hardcoded 색
- textFill: dark `#c9d1d9` / light `#1f2328`.
- dueMutedFill: dark `#8b949e` / light `#59636e`.
- dangerFill: dark `#f85149` / light `#cf222e`.
- 노드 data 의 urgencyBg 도 urgencyBgFor 사용.

### 3. quest-node-svg.ts (Home / Conveyor 의 SVG 미리보기) 도 theme 인자
- `makeQuestNodeSvgUrl(quest, overlayColor, theme = 'dark')` 시그니처 변경.
- bgFill / titleFill / defaultDueColor 가 theme 별.

### 4. theme 전환 시 모든 노드 재생성
QuestBoard 가 theme store subscribe + refreshNodeBgForTheme:
- 각 node 의 urgencyBg data + nodeBg (SVG) data 재생성.
- cy.style().update() 트리거.

## 테스트
1. Settings → 테마 라이트.
2. Quest Board 진입.
3. 노드: 흰 배경 / 검정 텍스트 + 옅은 urgency 색 좌측.
4. 다시 다크 → 검정 배경 / 회색 텍스트.
5. system → OS 따라.
6. Home / Conveyor 의 노드 미리보기 (makeQuestNodeSvgUrl 사용) — 본 라운드는 default 'dark' 라 동작 변화 없음. 호출자 별도 갱신 시 theme 전달.

## 남은 (별도 라운드)
- Home / CampaignCarousel / QuestNodeConveyor 등 makeQuestNodeSvgUrl 호출처가 theme 전달하도록 + theme 전환 시 갱신.
- WCAG color contrast 검증.

## 자동 검증
- npm run check 0 errors 0 warnings.

<!-- og-comment id="5" ts="2026-06-07T01:14:34+09:00" author="claude" -->
## fix2~10 (2026-06-07): 라이트모드 전반 마무리

### fix2 — 전역 커스텀 스크롤바 + Admin/Campaigns/Rules sweep (b0abaaf)
- `* { scrollbar-width: thin; scrollbar-color: var(--scrollbar-thumb) transparent }` + `::-webkit-scrollbar` 커스텀. `--scrollbar-thumb` / `--scrollbar-thumb-hover` 토큰.
- `html { scrollbar-gutter: stable }` 로 layout shift 방지.
- Admin Types/Statuses/TagDefs, Admin / Campaigns / Rules 페이지의 `#1a1a2e / #2a2a4a / #1f6feb` 등 hex → `var(--bg-elevated|border|accent-strong)`.
- CampaignCarousel / Conveyor / QuestNodeConveyor / QuestCommentsSection 의 hover bg, reply ribbon 등 토큰화.

### fix3 — Cytoscape stylesheet theme별 hex + Conveyor theme (224f0a2)
- Cytoscape style 시스템이 `var()` 컴퓨팅 못 함 → 모든 node / edge 색을 theme 별 hex 로 명시. `buildCyStyle(eff)` 함수가 stylesheet 배열 반환.
- highlight (pre/sub/next/parent/selected/flash) 의 dark hex → light tint variant 도 정의.
- `refreshNodeBgForTheme` 이 `cy.style().fromJson(buildCyStyle(eff)).update()` 호출.
- `QuestNodeConveyor` 가 `theme` store subscribe + `makeQuestNodeSvgUrl(q, overlay, effectiveTheme)` 호출.

### fix4 — `<main>` bg + CodeMirror 조건부 + 잔여 hex sweep (8e1803e)
- 가장 큰 누락: `<main>` 의 `background: #0d1117` hardcoded → `var(--bg)`.
- Admin toast / Welcome / Rules / Campaigns 잔여 dark hex → semantic 토큰 또는 `color-mix(in srgb, var(--accent|success|danger|warning) Y%, transparent)`.
- QuestList / Quest 상세 tag·note 라벨 색 → `var(--warning)`.
- CodeMirror oneDark: `theme === 'dark'` 일 때만 spread → light 면 기본 light. `$effect` 가 theme 변경 시 편집 중 에디터 재생성 (내용 보존). Quest 상세 + QuestNoteSection 둘 다.

### fix5 — 라이트 모드 New Quest / 복원 버튼 명도 ↑ (b33c4e5)
- 임시: `:global([data-theme='light']) .btn { … }` 직접 override.
- 사용자 지적: 다른 곳의 토큰 패턴과 어긋남.

### fix6 — primary / warning 버튼 토큰화 sweep (a7b6b70)
- 새 토큰: `--btn-primary-bg / -bg-hover / -border / -border-hover / -text`, `--btn-warning-bg / -bg-hover / -border / -text`.
- dark: primary = `--success-strong` / light: primary = `--success` (한 단계 ↑) / light hover = `color-mix(--success 85%, white)`.
- warning light: `#fff8d6 / #d4a72c / #6f5400` (옅은 노랑 카드).
- primary 버튼 11곳 sweep: Board / List New Quest, Home 캠페인 추가, Campaigns 새 캠페인, Campaigns 신규/상세 저장, NewQuestModal 생성, Quest 상세 저장, Comments/Memo 저장, Rules 저장, Settings 업데이트, UpdateBanner 다운로드, QuestBoard dialog-ok.
- fix5 의 `[data-theme='light']` override 제거 — 일관성 회복.

### fix7 — 캠페인 카드 overdue / completed 그라데이션 (98e10f9)
- `.card.overdue` / `.card.completed` 의 그라데이션 윗쪽 `#2a1010 / #102a18` → `color-mix(in srgb, var(--danger|success) 22%, var(--bg-elevated))`.

### fix8 — 카드 highlight 토큰화 (2fb426c)
- `--card-hl-strength / --card-hl-base / --card-hl-fade` 도입.
- light: tint 10% + base `var(--bg)` (흰) + fade 70%.

### fix9 — 카드 light tint 10% → 20% (b73296f)
- 사용자 피드백: 10% 가 너무 옅음. base 는 흰색 유지하면서 tint 만 ↑.

### fix10 — 보드 설정 모달 체크박스 (0eaa0a6)
- `accent-color` 는 checked 만 적용 → unchecked 의 native UA 회색 카드 가 라이트모드에서 어둡게 보임.
- `appearance: none` + 1rem 사각형 + `border: var(--border)` + `background: var(--bg)`. checked 시 accent + after pseudo ✓.

### 자동 검증
- npm run check 0 errors / 0 warnings (47 commits).
- vitest 153 pass (+ 회귀 1 added in DEV-068 fix2).
