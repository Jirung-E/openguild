<!-- og-comment id="1" ts="2026-07-23T00:54:42+09:00" author="claude" -->
실기 테스트 후속: 팝업이 뷰포트와 무관하게 고정 max-width(30/56rem)라 창을
줄이면 화면에 꽉 차 보였다. `.modal-sm`/`.modal-combo` 의 max-width 에 vw
상한 추가(`min(계산값, 92vw)`) — 창보다 크면 창을 따라 줄어든다. 세로도
`max-height:92vh; overflow-y:auto` 로 뷰포트 넘침 방지. quests/[id],
campaigns/[slug] 두 페이지 모두 적용.
