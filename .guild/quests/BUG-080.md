+++
quest_id = "BUG-080"
title = "외부 편집 시 '최근 편집 시간'(updated_at) 미반영 — 파일 mtime 으로 보정"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-06-18T23:33:43+09:00"
updated_at = "2026-06-19T00:49:14+09:00"
deleted = false
+++

reindex 가 quests.updated_at 을 frontmatter.updated_at 에서만 채움(reindex.rs:259). 외부 편집기로 본문만 바꾸면 frontmatter updated_at 이 그대로라 '최근 편집 시간'이 안 바뀜. fs mtime 은 cached_mtime(reindex.rs:283, drift 용)으로 이미 캡처됨. 수정안: reindex/sync 시 fs mtime > frontmatter updated_at 이면 updated_at 을 fs mtime 으로 보정. 설계 결정: (A) index 만 max(frontmatter, fs mtime) 로 표시(파일 불변) vs (B) 파일 진리원 원칙대로 frontmatter updated_at 을 fs mtime 으로 write-back. 권장 B(파일=진리원), 단 git diff churn 고려.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
