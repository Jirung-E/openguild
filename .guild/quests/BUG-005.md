+++
quest_id = "BUG-005"
title = '노드 제목 2번째 줄이 회색 + close tag 오타 (\text → TAB)'
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-05-17 15:20:51"
updated_at = "2026-05-18 13:43:18"
deleted = false
+++

## 증상

Quest Board 노드의 제목이 2줄 (36자 초과) 일 때:
- 두번째 줄이 회색 (`#8b949e`) — 1줄과 색이 달라 가독성 떨어짐.
- 추가 결함 의심: close tag `<\text>` (JS template literal 의 `\t` = TAB).

## 조사 결과 (2026-05-18)

`gui/frontend/src/lib/components/QuestBoard.svelte::makeSvgUrl()` 검토:
- 회색 fill: **확인** — line 58 `fill="#8b949e" font-size="11"`.
- close tag: **현재 코드는 정상 `</text>`**. 보고 시점에 `<\text>` 였을 가능성
  (그 사이 다른 commit 에서 우연히 수정됐을 수 있음) 또는 보고 자체가 오기.
  현재 working tree 검증 — 정상.

## 수정 (2026-05-18)

`gui/frontend/src/lib/components/QuestBoard.svelte::makeSvgUrl()`:
```diff
- ${line2 ? `<text x="10" y="${titleY + 16}" fill="#8b949e" font-size="11"
+ ${line2 ? `<text x="10" y="${titleY + 16}" fill="#c9d1d9" font-size="12"
```

- fill: `#8b949e` (회색) → `#c9d1d9` (1줄과 동일).
- font-size: `11` → `12` (1줄과 동일) — 시각적 통일감.
- close tag 는 이미 정상이라 그대로.

## 테스트 방법

### 자동 (완료)
- `npm test -- --run` → 63 통과 (회귀 없음).
- `npm run check` → 0 errors, 9 a11y warnings (기존, DEV-025 별도 처리).
- `npm run build` → adapter-static 정상.

### 사용자 검증 필요 (수동)
1. `cd gui/frontend && npm run build` (이미 했으면 skip).
2. `cargo run -p openguild-gui` 또는 `openguild-server host` + 브라우저.
3. 36자 넘는 제목의 quest 노드 찾기 (예: BUG-001
   "openguild quest update --description multi-line 시 첫 줄만 저장됨").
4. **2번째 줄이 1번째 줄과 같은 명도 / 같은 크기**.
5. 회귀: 짧은 제목 (1줄) 은 그대로 정상.

## 우선순위

`urgency = 3`. 가독성 영향. 1줄 안에 들어가는 제목은 영향 없음 (긴 제목만).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
