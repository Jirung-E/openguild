+++
quest_id = "DEV-038"
title = "GUI Quest 변경이력 섹션"
status = "testing"
urgency = 3
prerequisites = ["DEV-013"]
created_at = "2026-05-21T13:23:26Z"
updated_at = "2026-05-22T22:35:08+09:00"
deleted = false
+++

Quest Detail 화면에 변경이력 섹션 추가. ts / op / old → new 표시.

## 구현 (2026-05-21)

### 컴포넌트
- `gui/frontend/src/lib/components/QuestHistory.svelte` 신설:
  - props: `questId: number`, `statuses?: QuestStatus[]`.
  - `$effect` 로 `questId` 바뀔 때마다 `questsApi.listHistory(id)` 호출.
  - `status_id` 문자열 (DB 저장 형식) → 부모에서 받은 `statuses` 메타로 이름 매핑.
    - 매핑 없으면 `#N` fallback (상태 삭제된 경우 / 미동기화).
  - `change_status` op 외 — `old → new` 그대로 표시 (후속 op 대비).
  - `<time datetime>` + `formatRelative` (DEV-039 util 재사용).

### Quest Detail 통합
- `routes/quests/[id]/+page.svelte`:
  - import + Prerequisites 섹션 뒤에 `<QuestHistory>` 마운트.
  - `historyVersion` $state — `changeStatus` 후 +1 → `{#key}` 로 컴포넌트 리마운트하여
    새 행 즉시 반영.

### 자동 테스트 +7 (frontend)
- `QuestHistory.test.ts`: `vi.mock('$lib/api/quests')`:
  - 빈 이력 / change_status 매핑 / 알 수 없는 status_id / null old_value
  - API 에러 / count 뱃지 / 알 수 없는 op fallback.

## 회귀

- 기존 페이지 로직 영향 없음 — 새 섹션 추가 + status 변경 후 +1.
- API endpoint `/api/quests/{id}/history` 는 DEV-013 에서 이미 노출.

## 검증

- `npm test -- --run` → **81 passed** (74 → 81).
- `npm run check` → 0 error / 0 warning.
- `npm run build` → success.

## 후속 (2026-05-22, GUI vs CLI 일관성)

테스트 중 user 가 "GUI 와 CLI 의 변경이력이 다르다" 보고. 실제 데이터는
동일하지만 표시 형식 (op 라벨 / 시각 표현) 이 달라 다른 사건처럼 보임:

| 비교 | 변경 전 CLI | GUI | 변경 후 CLI |
|---|---|---|---|
| 시간 | `2026-05-22T22:26:51+09:00` | `방금` | `2026-05-22T22:26:51+09:00  3분 전` |
| op 라벨 | `change_status` (raw) | `상태` | `상태` |
| 상태 | colored name_en | colored pill | (그대로) |

수정:
- `core::time::format_relative(ts)` 신설 — frontend `formatRelative` 와
  동일 규칙 (`방금` / `X분 전` / ... / 7일 초과 또는 미래 시각 → `None`).
- CLI `quest history` 출력에 relative 시각 컬럼 추가 (절대 ts 도 유지 — script
  친화).
- op 라벨 번역 (`change_status` → `상태`), 알 수 없는 op 는 raw 그대로.

자동 테스트 +4 (core::time): format_relative 의 recent / old / future /
legacy 형식 입력.

라이브:
```
$ openguild quest history DEV-038
2026-05-22T22:26:51+09:00  3분 전       상태             Returned → In Progress
2026-05-22T22:26:42+09:00  4분 전       상태             Testing → Returned
```

## 후속 (별도 quest 후보)

- 다른 mutation op (update_title / change_parent / add_prereq / delete) 도
  기록 + UI 에서 op 별 customized 표시.
- 이력 페이지네이션 / 무한 스크롤 — 현재는 전체 fetch.
- actor 표기 — 멀티유저 quest 와 함께.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- [DEV-013](DEV-013.md) — Quest History (상태 변경 이력 자동 기록)
<!-- openguild:auto-end -->
