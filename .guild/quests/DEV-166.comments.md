<!-- og-comment id="1" ts="2026-06-21T17:36:32+09:00" author="claude" -->
DEV-177 정비 명령 그룹화도 문서 sweep 대상에 포함:
- check-drift → check drift / check-counters → check counters
- vacuum → index vacuum, journal-tail → journal tail
- reindex 는 top-level 유지(= index rebuild)
대상: storage-design.md, AGENTS_OPENGUILD_USAGE.md, architecture.md, dev-plan.md, AGENTS.md, README.md, .guild/rules/test-data-script.md

<!-- og-comment id="2" ts="2026-06-23T00:02:45+09:00" author="claude" -->
완료 (commit c7fe09f). 살아있는 문서 일괄 갱신: README / architecture / AGENTS / AGENTS_OPENGUILD_USAGE / storage-design / dev-plan / guild-rules / USAGE / rules/test-data-script.

반영: openguild-server <정비> → openguild CLI (또는 HTTP admin /api/admin/*), server=host 전용(DEV-163), backup new/list/remove(DEV-176/179), check/index/journal 그룹 + reindex=index rebuild(DEV-177), template new(DEV-179), rm→remove. (남은 openguild-server 언급은 host 명령/바이너리명/remote URL 로 정상. commit-message 예시의 [server] 태그는 과거 기록이라 유지.)
