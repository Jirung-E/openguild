<!-- og-comment id="1" ts="2026-08-14T00:01:10+09:00" author="codex" edited_at="2026-08-14T00:03:32+09:00" -->
## 2026-08-14 자동/실제 검증 결과

- 자동 테스트: CLI 115, core 398, server 206 — 총 719개 통과.
- 격리 길드 2개와 localhost 서버를 사용해 quest/campaign 댓글·메모, quest 태그, quest/campaign/library 첨부, type/status CRUD 및 rename cascade, template list/show/new와 template 기반 quest 생성, 전역 comments, tag list --used, reindex/index rebuild, drift --resync, vacuum, journal tail, counters, info를 실제 원격 CLI로 확인.
- 댓글 add/reply/edit/remove/react/discussion/resolved/pinned와 메모 set/show/clear 모두 성공.
- 세 첨부 scope 모두 streaming upload → list → remove 후 빈 목록 확인.
- 60,433,560-byte 파일을 원격 업로드할 때 CLI maximum RSS는 18,153,472 bytes로 파일 크기에 비례한 전체 버퍼링이 없음을 확인.
- 로컬/원격 JSON 조회 14개 항목을 기계적으로 비교해 최종 일치 확인.
- 참조 없는 임시 type/status의 원격 삭제 성공과 `../escape` 템플릿 이름의 400 거부도 직접 확인.

검증 중 추가로 발견해 수정한 회귀:

1. soft-deleted quest가 type/status를 참조할 때 사전 카운트에서 제외되어 삭제가 FK 500으로 실패하던 문제. 삭제된 quest도 복원/reindex에 정의가 필요하므로 전체 참조를 세어 400으로 차단하고 회귀 테스트 추가.
2. quest tag 전용 조회에서 로컬은 frontmatter 순서, 원격은 DB 알파벳 순서를 반환하던 문제. 파일 진리원 순서를 공용 core helper로 읽도록 CLI/server/GUI를 통일하고 회귀 테스트 추가.

`migrate-to-files`는 의도적 로컬 전용이며, 원격 거부 직접 실행은 실행 안전 검토기가 로컬 마이그레이션으로 오인해 차단하여 재시도하지 않았다. 코드상 원격 분기는 오프라인 실행 안내와 함께 거부하도록 유지되어 있다.
