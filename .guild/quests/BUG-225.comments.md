<!-- og-comment id="1" ts="2026-08-08T19:55:11+09:00" author="Codex" -->
구현 및 자동·브라우저 검증 결과.

- grid snap을 world SVG data URI에서 viewport-space CSS radial-gradient로 교체했다. visible/non-collapsed lane만 화면 높이+2행 overscan을 만들고 pan은 phase transform으로 추적한다.
- 점 반지름은 화면 px 기준 0.9~2.25px로 유지한다. 고정 worldHeight 의존이 없어 상하 pan 범위 제한도 사라졌다.
- zoom LOD를 detail(>=0.55), compact(>=0.16), overview(<0.16)로 나눴다. overview에서도 외곽 node hit area는 유지하고 내부 pill/icon/text DOM만 제거한다.
- 도구바에 `Hz 성능` HUD를 추가했다. rAF Hz, median/p95, 12.5ms 초과 비율, viewport update/s, zoom, LOD를 1초 단위로 표시하며 꺼진 동안 측정 loop는 없다.

실제 self guild 브라우저 검증:

1. snap on에서 `게시됨` lane 접기 후 첫 grid column이 `display:none`, 펼치면 복원.
2. panY 297→-273 이동 뒤에도 dot rect top 29.35 / bottom 808.61, board viewport top 52 / bottom 720으로 위·아래 전체 overscan 확인.
3. 375×720 fit zoom 0.0253429에서 dot radius 0.9px, overview LOD, node 560개/내부 자식 0개(중간 LOD에서는 1,120개).
4. 최소 zoom 41-point drag 중 HUD: 120Hz, median 8.3ms, p95 9.7ms, >12.5ms 0%, viewport 40/s.

검증 통과: viewport helper 4 tests, frontend 39 files/399 tests, svelte-check 0 errors(기존 dynamic-range-limit 경고 2), production build, release openguild-gui build, git diff --check.

인앱 브라우저의 120Hz는 확인했다. macOS Tauri는 WebKit 자체 cap 여부가 별개이므로 release 앱에서 같은 HUD로 실제 값을 확인해야 한다. 60Hz라면 약 16.7ms/60Hz, 정상 120Hz라면 약 8.3ms/120Hz가 표시된다.

<!-- og-comment id="2" ts="2026-08-08T20:09:57+09:00" author="codex" -->
macOS release 앱 실기 피드백(2026-08-08):

- 주사율 HUD는 일반 릴리즈 UI에 노출하지 않고 디버그 빌드에서만 단축키로 토글할 것.
- MacBook Pro M5의 최대 120Hz 가변주사율 디스플레이에서 HUD가 30fps로 고정되어 표시됨.
- 스냅 점이 레인당 한 줄만 보임. 현재 레인은 3열 정렬 설정이므로 스냅도 3열이어야 함.
- 스냅 점 크기가 지나치게 큼.

이 피드백으로 BUG-225를 Testing에서 In Progress로 되돌려 후속 수정한다.

<!-- og-comment id="3" ts="2026-08-08T20:20:14+09:00" author="codex" -->
후속 피드백을 반영했다.

- 성능 HUD 툴바 버튼과 i18n 라벨을 제거했다. HUD는 Vite dev 또는 Rust debug
  build에서만 `Cmd/Ctrl+Shift+H`로 토글된다. packaged debug는 기존
  `is_debug_build` Tauri command로 판별하며 release에서는 진입 경로가 없다.
- HUD에 `document.visibilityState`/focus 상태를 추가해 30Hz가 background
  throttling인지 foreground WKWebView pacing인지 구분할 수 있게 했다.
- snap의 가로 CSS background repeat를 없애고 레인마다 세 개의 실제 세로 dot
  column을 생성한다. laneCols=3 실화면에서 각 레인 3열 좌표가 모두 표시됨을
  확인했다.
- 점 반지름을 화면 기준 0.9~2.25px에서 0.55~1.35px로 축소했다.
- 보드 코드에는 30fps cap/timer가 없고 wheel 입력은 rAF 한 프레임당 한 번
  합성할 뿐이다. 같은 데이터의 인앱 Chromium에서는 120Hz(8.3ms)가 측정돼,
  macOS Tauri 30Hz는 WKWebView scheduling 경로의 차이로 판단한다.

검증: viewport helper 6 tests 통과, svelte-check 0 errors(기존 CSS warning 2),
production frontend build 통과, Prettier/diff check 통과. 브라우저 실화면에서
3열 snap, 툴바 HUD 버튼 미존재, debug shortcut on/off, 120Hz 측정을 확인했다.

<!-- og-comment id="4" ts="2026-08-08T20:43:55+09:00" author="codex" -->
사용자 지적에 따라 snap 열별 DOM을 제거했다.

- `.lane-grid-col` 아래 만들던 `.lane-dot-column` 자식 1~3개를 모두 삭제했다.
- 이미 존재하는 레인 컨테이너 자체에 CSS `radial-gradient` layer를 1/2/3개
  적용하고, 각 layer의 X 위치를 명시한 뒤 `repeat-y`만 사용한다.
- 레인 컨테이너가 위치·폭·클리핑·점 paint를 모두 담당한다. 스냅 점과 열 때문에
  추가되는 DOM은 0개다.
- 열별 transform/크기/위치 style write도 제거하고 레인당 background size/position
  한 번으로 합쳤다. 이전에 필요했던 viewport overscan용 자식 크기 계산도 삭제했다.

워크플로 규칙에 따라 build/test는 자동 실행하지 않았다. 수동 확인은 기존 테스트
방법대로 1/2/3열 전환, 상하 pan, 접힌 레인 숨김을 보면 된다.

<!-- og-comment id="5" ts="2026-08-08T21:06:33+09:00" author="codex" -->
확대/축소 중 snap 점이 떨리는 후속 현상을 수정했다.

- 원인: 세로 CSS 배경 위상을 매 프레임 타일 크기로 modulo 하여, zoom 경계를 넘을 때 `background-position` 값이 한 타일만큼 순간적으로 되감겼다. 반복 무늬의 논리 위치는 같지만 WebKit의 재 래스터화 프레임에서 점이 튀어 보일 수 있다.
- 수정: 월드 행의 screen-space 위치에서 계산한 연속적인 background offset을 그대로 CSS에 전달한다. 타일 반복은 CSS에 맡기고 JS에서 위상을 되감지 않는다.
- 회귀 확인: snap을 켠 상태에서 트랙패드로 천천히 확대/축소하며 세로 점 행이 순간적으로 위/아래로 튀지 않는지 확인한다. 1/2/3열, 큰 양수/음수 pan, 접힌 레인 숨김은 기존과 같아야 한다.

프로젝 규칙에 따라 명시적 요청이 없어 build/test는 실행하지 않았고 `git diff --check`와 남은 참조 검사만 확인했다.

<!-- og-comment id="6" ts="2026-08-08T21:17:19+09:00" author="codex" -->
사용자 교차 확인에서 snap 표시 떨림은 Chrome에서는 거의 보이지 않고 Safari에서 재현됐다. 따라서 이전에 추정한 JS modulo 경계가 주원인이 아니라, WebKit이 zoom 중 매 프레임 바뀌는 CSS `radial-gradient` 크기/위치를 재 래스터화하는 경로와 저배율 모아레가 핵심으로 판단된다.

후속 수정:

- zoom gesture 시작 순간의 screen-space snap grid를 texture로 고정한다.
- 제스처 중에는 각 레인의 `background-size`/`background-position`을 갱신하지 않고, 초기 pan/zoom과 현재 pan/zoom의 비율을 사용해 `.lane-grid-layer` 부모 `transform` 하나만 갱신한다.
- wheel zoom 입력이 120ms 끊기거나 touch pinch가 끝나면 현재 배율로 gradient를 한 번만 재생성하고 transform을 초기화한다.
- zoom-out 중 texture 가장자리가 비지 않도록 세로로 viewport 1배 overscan한다.
- 최소 배율에서 실측된 세로 점 간격이 약 3.34px였다. 스냅 계산은 모든 행을 유지하되 시각 표시만 2/4/8번째 행으로 LOD해 최소 8px 간격을 확보했다.

기존 dev server의 실화면에서 노드 561개/레인 7개, snap 1/2/3열 CSS layer, 상하 overscan을 확인했고 HMR console warning/error는 0개였다. Chrome은 원래 재현이 약하므로 Safari/WKWebView 트랙패드 실기가 최종 검증 항목이다. 명시적 요청이 없어 별도 build/test 명령은 실행하지 않았다.

<!-- og-comment id="7" ts="2026-08-08T21:22:24+09:00" author="codex" -->
사용자 피드백으로 snap 세로 행의 2/4/8 간소화 LOD를 제거했다. 배율 구간에 따라 표시 행 간격이 큰 단위로 바뀌어 스냅 표시의 변화 속도가 느려 보였기 때문이다.

최종 방식은 모든 스냅 행을 원래 간격으로 표시한다. Safari 떨림 방지는 LOD가 아니라 zoom gesture 시작 시 gradient texture를 고정하고 제스처 중 부모 `transform`만 갱신하는 방식만 담당한다. 입력 종료 후 현재 배율로 한 번 재생성하는 동작은 유지한다.

명시적 요청이 없어 별도 build/test 명령은 실행하지 않았고, LOD 참조 전수 제거와 `git diff --check`를 확인했다.

<!-- og-comment id="8" ts="2026-08-08T21:28:25+09:00" author="codex" -->
후속 실기에서 zoom gesture가 끝난 후 snap gradient가 제스처 중 texture와 다른 위치에 재생성되는 현상이 확인됐다. 원인은 제스처 texture는 기존 screen-space 타일을 비율로 확대했지만, 종료 후에는 새 screen-space 타일 원점을 다시 계산해 재생성 전/후의 기준 좌표계가 달랐던 것이다.

수정:

- snap 열 중심 X, 첫 행 중심 Y, 세로 간격을 화면 px이 아닌 노드와 같은 world 좌표로 고정했다.
- `.lane-grid-layer`에는 `.board-world-viewport`와 동일한 `translate3d(pan) scale(zoom)`을 적용한다.
- zoom gesture 중에는 gradient texture를 고정하고 부모 transform만 갱신한다.
- 종료 후에는 점 중심/간격은 건드리지 않고, 현재 zoom에서 화면 굵기를 유지하기 위한 world 반지름과 viewport overscan 범위만 재계산한다.
- radial-gradient의 anti-alias feather도 `0.55 / zoom` world px로 변환해 화면에서 0.55px로 유지한다.

기존 dev server 실화면에서 첫 행의 world Y를 검증했다. `gridTop(-37380.5) + backgroundY(37434.5) + cellH/2(54) = 108`로, overscan top이 바뀌어도 항상 snap baseY 108에 남는 구조다. 노드 561개/레인 7개 화면에서 HMR console warning/error는 0개였다. Safari/WKWebView 트랙패드 실기로 종료 시 위치 jump와 제스처 중 떨림을 최종 확인한다.

<!-- og-comment id="9" ts="2026-08-08T21:47:13+09:00" author="codex" -->
사용자 실기에서 release build가 debug build보다 zoom gesture가 더 버벅이고, 최대 확대 시 노드가 저해상도로 보이는 차이가 확인됐다.

조사 결과:

- `build-debug`도 `npm run build`로 production frontend를 embed하므로 Svelte dev/prod 번들 차이가 아니다.
- Rust release 최적화는 보드의 프레임별 DOM/CSS rendering에 관여하지 않는다.
- macOS Wry 0.55.1의 실질적인 debug/release WKWebView 차이는 debug에서만 `setInspectable(true)`와 private `developerExtrasEnabled`를 켜는 것이다. release에 이 private 기능을 켜는 것은 App Store 배포 안정성 문제가 있어 해결책으로 사용하지 않는다.
- WebKit bug 160661은 transform된 sibling layer가 scale 콘텐츠를 흐리게 만드는 현상과, scale 대상이 직접 layer가 되지 않도록 부모에 layer를 두는 우회를 기록한다. bug 264954도 scale context 안의 child layer가 부족한 backing pixel로 흐리게 보일 수 있음을 기록한다.

기존 구조는 `.lane-grid-layer`와 `.board-world-viewport`가 각각 transform되는 sibling compositor layer였다. 이를 다음처럼 변경했다.

```text
board-wrap
└─ board-world-viewport  ← pan/zoom transform 1회
   ├─ lane-grid-layer
   └─ board-world
      ├─ SVG edges
      └─ DOM nodes
```

- grid의 별도 transform / `will-change` layer를 제거했다.
- snap/node/edge가 부모 transform 하나를 공유하므로 제스처 프레임당 compositor update도 2회에서 1회로 줄었다.
- grid texture는 zoom gesture 중 고정하고 종료 시 굵기/overscan만 재생성하는 동작을 유지한다.

기존 dev server 실화면에서 grid 7개/node 561개가 모두 같은 transform 부모의 자식임을 확인했고 HMR console warning/error는 0개였다. release WKWebView에서 최대 확대 노드 선명도와 gesture 버벅임을 최종 확인한다.

<!-- og-comment id="10" ts="2026-08-08T21:52:07+09:00" author="codex" -->
단일 compositor parent로 합친 후에도 release WKWebView의 최대 확대 노드가 흐리게 보였다. sibling layer는 악화 요인이었지만 원인의 전부가 아니었다. 남은 문제는 제스처 종료 후에도 월드 전체가 CSS `transform: scale(finalZoom)` 상태에 남아 WebKit이 저배율 backing texture를 확대해 쓸 수 있다는 점이다. WebKit bug 27684는 composited element를 transform scale-up할 때 기존 작은 rendering이 재사용돼 pixelated될 수 있는 현상을 기록한다.

최종 render zoom 구조:

```text
board-world-viewport
  transform: translate(pan) scale(logicalZoom / committedZoom)
  └─ board-render-scale
       CSS zoom: committedZoom
       ├─ snap grid
       └─ edge + nodes
```

- gesture 시작 전 `committedZoom`은 현재 zoom이다.
- gesture 중에는 committedZoom을 고정하고 외부 transform ratio만 갱신한다. 따라서 hot path는 여전히 compositor transform 1회다.
- wheel 입력이 120ms 끊기거나 touch pinch가 끝나면 `committedZoom = logicalZoom`으로 갱신하고 외부 transform scale은 1이 된다.
- CSS zoom은 최종 배율을 layout/raster에 반영하므로 DOM 폰트/pill, SVG edge, snap이 해당 배율에서 다시 그려진다.
- fit/저장 viewport 복원처럼 gesture가 아닌 zoom 변경은 즉시 CSS zoom으로 commit한다.

기존 Vite dev server가 종료돼 HMR 실화면 검증은 진행하지 못했다. 명시적 요청이 없어 새 server/build/test는 실행하지 않았고, 남은 참조 검사와 `git diff --check`를 확인했다. `just build`후 release WKWebView에서 최대 확대 선명도, 제스처 중 버벅임, 종료 순간 위치 불변을 확인한다.

<!-- og-comment id="11" ts="2026-08-08T21:56:19+09:00" author="codex" -->
사용자 피드백: 최종 해상도로 재 rasterize하기 위해 도입한 CSS `zoom` 방식이 폰트 layout metric과 반올림을 바꿔 글자 크기가 변하는 회귀를 만들었다. 글자/pill/노드 크기 변경은 요청 범위가 아니므로 해당 방식을 전부 제거했다.

정정 내용:

- `.board-render-scale` wrapper, `renderScaleEl`, `committedRenderZoom`, CSS `zoom` 적용을 전부 삭제했다.
- 노드/폰트/pill/SVG는 기존처럼 동일한 world CSS 크기와 논리 zoom을 사용한다.
- zoom gesture 중에는 `translate3d(...) scale(zoom)`으로 compositor 경로를 사용한다.
- 입력 종료 후에는 화면상 pan/zoom 값이 완전히 동일한 `translate(...) scale(zoom)` 2D transform으로 변경한다. 시각 크기는 바꾸지 않고 WebKit의 3D backing layer 재구성만 유도한다.
- snap/grid/edge/node가 transform 부모 하나를 공유하는 구조는 유지한다.

기존 Vite dev server가 종료된 상태라 별도 build/test는 실행하지 않았다. CSS zoom 참조 전수 제거와 `git diff --check`를 확인했다. release WKWebView에서 글자 크기 불변과 최대 확대 선명도를 재확인한다.

<!-- og-comment id="12" ts="2026-08-08T22:46:18+09:00" author="codex" -->
사용자 피드백으로 중간 `compact` LOD에서 제목을 제거하고 추적번호만 크게 표시하도록 변경했다.

- `node-compact-title` DOM과 CSS를 전부 제거했다.
- `node-compact-id`를 카드 전체에 대해 grid center 배치했다.
- font size는 world CSS 32px, weight 800으로 키워 축소 상태에서도 추적번호를 구분하기 쉽게 했다.
- 기존 `overview`는 색상 marker, `detail`은 전체 카드로 그대로 유지했다.
- 작업 전 이미 변경돼 있던 `DETAIL_ZOOM = 0.3`은 사용자 변경으로 보존했다.

명시적 요청이 없어 build/test는 실행하지 않았고, 남은 compact title 참조 제거와 `git diff --check`를 확인했다.
