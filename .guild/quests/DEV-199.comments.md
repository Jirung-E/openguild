<!-- og-comment id="1" ts="2026-07-03T22:35:06+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03)

**변경 파일**: cli/src/main.rs (comment 서브커맨드), Backend trait(Http/Local 두 구현).

1. 명령: `openguild quest comment react <SLUG> <ID> <EMOJI> [--author <NAME>]` — 토글 시맨틱(GUI 의 toggle_reaction 과 동일: 이미 있으면 제거). campaign comment 에도 동일 추가(기존 관례: quest/campaign 대칭).
2. Backend: Local = core 의 기존 reaction 토글 ops 재사용(GUI invoke 가 쓰는 함수 — comments ops), Http = 기존 `/api/.../reactions` 라우트 호출(HTTP 는 이미 존재 — DEV-199 본문 확인). **--author 필수 규칙**(AGENTS_OPENGUILD_USAGE 🚨)을 react 에도 적용: 미지정 시 에러 권장(반응은 author 단위 토글이라 필수).
3. `comment list` 출력에 반응 표시: 각 entry 요약줄 뒤에 `👍2 ✅1` 형태 집계 표시(+`--json` 은 raw reactions 배열 이미 포함 확인).
4. 이모지 검증: GUI 고정 4종(👍✅❓❌) 외 임의 이모지 허용 여부 — 데이터 포맷은 임의 문자열 허용이므로 CLI 도 제한 없이 통과 권장(DEV-132 커스텀 이모지와 정합).
5. 테스트: cli integration(react 토글 왕복, author 별 집계) + list 표시.

예상 규모: 소형~중형. 선행 없음.
