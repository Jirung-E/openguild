<!-- og-comment id="1" ts="2026-07-11T21:18:00+09:00" author="claude" -->
구현 완료:

- Tauri 커맨드 load_custom_themes/save_custom_themes — ~/.openguild/themes.json raw IO(atomic write). 파싱/검증은 frontend 담당.
- customThemes.ts: Tauri 모드에서 파일이 진리원 — initCustomTheme(async 화)이 시동 시 파일 로드(있으면 store 교체 — **외부에서 파일 직접 편집한 경우도 시동 로드로 자연 반영**), 없고 localStorage 에 프리셋 있으면 1회 마이그레이션 기록. store 변경은 debounce 파일 저장(시동 로드 완료 전 쓰기 방지 게이트 포함). 브라우저(remote) 모드는 localStorage fallback 그대로.
- 파일 포맷: { "presets": [...], "active": "이름" } — pretty JSON 이라 직접 편집/백업/공유 가능.
- 기존 vitest 는 jsdom(비 Tauri) 경로라 localStorage 동작 그대로 통과(async 전환분 await 만 갱신).
