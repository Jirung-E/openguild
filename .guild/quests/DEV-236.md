+++
quest_id = "DEV-236"
title = "[core] 토론 해결/재개 전환을 quest_history 에 기록 — worklog 표출"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-07-06T21:49:20+09:00"
updated_at = "2026-07-08T21:49:04+09:00"
deleted = false
+++

질문(admin): '토론 해결(resolved)' 전환도 worklog 에 기록되어야 하는데
그렇게 설계되어 있는지? — **확인 결과: 안 됨 (설계 공백).**

## 현재 구조
- worklog 활동 소스(DEV-167): quest_history(상태/타입 변경) +
  quest/campaign 댓글의 **작성 시각(ts)** + quests.created_at.
- `toggle_comment_discussion` / `toggle_comment_resolved`(core
  ops/comments.rs)는 journal 에만 기록(감사용) — quest_history 에 안
  남고, 댓글 ts 도 안 바뀌므로 worklog 타임라인 어디에도 안 나타남.
  journal 은 snapshot 시 truncate 라 worklog 소스로 못 씀(기존 결정).

## 개선안 (설계 결정 필요)
(a) **quest_history 에 이벤트 추가**: op='discussion_resolved'/
    'discussion_reopened'/'discussion_marked' 로 INSERT + DEV-180
    사이드카 append — worklog 는 quest_history 를 이미 소스로 쓰므로
    kind 매핑만 추가하면 자동 표출. 권장.
(b) worklog 쿼리에서 comments 의 discussion/resolved 컬럼을 별도
    스캔 — 전환 "시각"이 저장 안 돼 있어 불가능(현재 스키마).
→ (a)로 가야 함. quest_history 스키마는 op 가 TEXT 라 확장 무변경.

## 함께 검토
- 같은 논리로 댓글 수정/삭제, due 변경, 태그 변경 등도 worklog 대상으로
  넣을지 범위 결정 (과기록은 노이즈 — 우선 resolve 전환만).

## 구현 (2026-07-07)
- (a)안 채택, 범위는 제목 그대로 "resolve/reopen 전환만" — discussion
  최초 표시(marked)는 기록 안 함(과기록 방지, "함께 검토" 절 결정 유지).
- `ops::comments::toggle_comment_resolved` 가 resolved 토글 후
  `record_discussion_history()` 호출 — `quest_history` INSERT
  (op="discussion_resolved"/"discussion_reopened", new_value=댓글 id) +
  DEV-180 사이드카 append. quest 가 drift 상태(캐시에 없음)면 다른 history
  기록과 동일하게 silently skip(다음 reindex 가 일관).
- `ops::worklog::activities()` 의 kind CASE 에 두 op → 'discussion' 매핑
  추가, summary 도 전용 문구("댓글 #N 토론 해결"/"토론 재개(미해결)").
  `ActivityCounts` 에 `discussion_events` 필드 추가.
- reindex/replay 변경 불필요 — quest_history 사이드카 재구축은 op 값에
  무관하게 그대로 replay(기존 인프라 재사용, 설계 문서에서 예상한 대로).
- GUI: `worklog/+page.svelte` 의 `KIND_BADGE` 에 discussion 배지 추가,
  하단 집계에 "N 토론 전환"(0건이면 숨김) — HOME 요약 카드는 공간 제약상
  변경 안 함(과기록 방지 취지 유지).

## 테스트 방법
- `cargo test -p openguild-core toggle_resolved_records_quest_history` /
  `cargo test -p openguild-core activities_includes_discussion_resolve_events`
  / `cargo test -p openguild-server worklog_shows_discussion`.
- `cd gui/frontend && npm run check && npm test -- --run`.
- 실기: quest 댓글을 토론으로 표시 → resolve → 작업 기록 상세에서 그 날짜에
  "토론" 배지가 붙은 활동으로 나오는지, 다시 미해결로 되돌리면 "재개"
  항목도 추가되는지.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
