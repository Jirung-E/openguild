<!-- og-comment id="1" ts="2026-08-08T23:31:47+09:00" author="Codex" -->
DEV-283 구현 완료.

- `writeback_external_edit_ts`와 frontmatter 치환 함수를 제거했다.
- quest 시동 sync/상세 refresh와 campaign 시동 sync/상세 refresh 모두
  `updated_at`을 파일에서 DB로만 투영한다.
- 파일 투영 동일성 비교에 `updated_at`을 포함해 stale DB도 파일 값으로 복구한다.
- 회귀 테스트는 동기화 전후 quest/campaign 파일 바이트가 동일한지 검증하도록
  변경했다.
- BOOK-001의 B1을 “폐지 완료”로 갱신하고 트레이드오프를 기록했다.

정적 검증: 단일 파일 rustfmt, `git diff --check`, 제거된 helper 참조 검색 통과.
저장소 에이전트 규칙에 따라 cargo build/test는 실행하지 않았으며, 실행할
명령과 수동 검증 절차는 퀘스트 본문에 기록했다.
