+++
quest_id = "REQ-009"
title = "강화된 검색 — 댓글/첨부 이름/메모까지 (기본 검색과 별도)"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-08-21T23:21:35+09:00"
updated_at = "2026-08-23T13:27:30+09:00"
deleted = false
+++

## 요청

댓글 / 첨부파일 이름 / 메모 내용 등도 검색할 수 있는 **강화된 검색**.

**기본 검색을 바꾸는 것이 아니라 추가 기능**이다 — 지금의 빠르고 예측 가능한
동작은 그대로 두고, 더 넓게 훑고 싶을 때 쓰는 별도 경로를 만든다.

## 현재 검색이 보는 범위 (조사 완료)

| 경로 | 대상 |
|------|------|
| 서버/CLI `quest search` (`core/src/services/quests.rs:161~179`) | title + description + slug. 공백 split AND, 각 토큰이 셋 중 하나에 `LIKE` |
| 검색 팔레트 (`SearchPalette.svelte`) | 전체 목록을 클라이언트에 적재 후 label/title/tags 필터 |

즉 **댓글·메모·첨부 이름은 어디서도 검색되지 않는다.**

## 대상별 준비 상태 — 여기가 설계의 핵심

같은 "검색 대상" 이라도 데이터가 있는 곳이 제각각이다.

| 대상 | index.db 캐시 | 비고 |
|------|--------------|------|
| 퀘스트 본문/제목 | `quests.title/description` | 이미 검색됨 |
| **댓글** | `quest_comments.body` / `campaign_comments.body` | ✅ 캐시 있음 — 바로 가능 |
| **메모** | `quest_memos.content` | ✅ 캐시 있음. **단, 아래 "비공개" 주의** |
| 도서관 | `library_docs.body` | ✅ |
| **첨부 이름** | ❌ **없음** | 사이드카 `{slug}.attachments.json` 뿐. `attachment_blobs` 는 [[BUG-188]] 로 **폐기**(0029) |
| 규칙 | ❌ 없음 | 전체가 파일 직독 (`file-truth-db-cache` §4) |

### ⚠️ 메모는 비공개 데이터다

`.guild/.gitignore` 가 `quests/*.memo.md` 를 제외한다 — 메모는 **공유되지 않는
개인 기록**이다. 검색 결과에 섞이면:

- 화면 공유 / 데모 중에 사적인 메모가 노출될 수 있다.
- 원격(서버) 모드에서는 **다른 사람의 로컬 메모가 아니라 호스트의 메모**가
  나간다 — 인증이 없는 현재 구조에서는 더 민감하다.

**기본 포함이 아니라 명시적 opt-in**(예: `--include-memo` / 결과 필터 토글)이
맞아 보인다. 결정 필요.

## 제안 방향

1. **별도 진입점** — 기본 검색은 그대로. 예:
   - CLI: `openguild search <query> [--in comments,memo,attachments,rules,library]`
   - HTTP: `GET /api/search?q=…&in=…`
   - 팔레트: 별도 모드(접두 `all:` 같은 것) 또는 "더 넓게 검색" 액션
2. **첨부 이름 검색은 캐시가 필요**하다. 선택지:
   - (a) 사이드카 JSON 을 조회 시점에 스캔 — 문서 수만큼 파일 I/O
   - (b) `attachment_index` 캐시 테이블 신설 + reindex 투영 + 쓰기 시점 증분
     갱신 — [[REQ-008]] 의 `doc_links` 와 **완전히 같은 패턴**이라 그대로 따라갈
     수 있다(BOOK-001 단방향 유지, 언제 지워도 reindex 로 재구축).
   - **이름만** 색인한다 — 파일 내용은 색인하지 않는다(BUG-188 의 대용량 첨부).
3. **결과에 출처를 표시**한다. "어디서 맞았는지"(본문/댓글/메모/첨부)를 안
   보여주면 왜 이 문서가 나왔는지 알 수 없다.

## 결정이 필요한 사항

1. 메모 포함 정책 — 기본 제외 + opt-in 이 맞는가?
2. 첨부 이름 — 캐시 신설(b) 대 조회 시 스캔(a)?
3. 진입점 — CLI/HTTP/팔레트 중 어디까지 이번 범위인가?
4. 매칭 방식 — 지금처럼 `LIKE` 유지인가, SQLite FTS5 도입인가?
   (FTS5 는 한국어 형태소 없이도 부분일치가 까다로워 `LIKE` 유지가 무난할 수
   있다. 다만 대상이 늘면 성능 차이가 생긴다.)

## 테스트 방법

1. 댓글에만 있는 단어로 검색 → 그 퀘스트가 나오고, 출처가 "댓글" 로 표시되는지.
2. 첨부 파일 이름으로 검색 → 해당 문서가 나오는지.
3. 메모에만 있는 단어 → **기본 검색에서는 안 나오고**, opt-in 했을 때만 나오는지.
4. 기존 검색 회귀 — title/description/slug 검색 결과가 이전과 동일한지.

---

## 수정 내용 (2026-08-21)

추천대로 네 가지 방침을 그대로 적용했다 — 메모 기본 제외 / 첨부는 스캔 /
core+HTTP+CLI 범위 / `LIKE` 유지.

### core — `ops/search.rs`

문서별로 검색 대상 텍스트를 모아(`DocText`) Rust 에서 필터한다. 여러 영역에
걸친 AND 매치를 SQL 로 짜면 읽기 어려워지는데, 규모가 수백 건이라 그럴 이유가
없다(퀘스트 607개 기준 본문 전체가 수 MB).

- 대상: 퀘스트/캠페인/도서관 본문 + 규칙(파일 직독) + **댓글** + **메모(opt-in)**
  + **첨부 이름**(사이드카 직독).
- 제목과 slug 는 항상 본다.
- 토큰은 공백 split **AND** — 서로 다른 영역에 흩어져 있어도 된다
  (기존 `--search` 시맨틱과 동일).
- 결과에 **`matched_in`**(어디서 맞았는지)과 **발췌**를 함께 낸다. 댓글에서
  맞았는데 본문 앞부분을 보여주면 왜 나왔는지 알 수 없다.
- 발췌는 **char 단위로 자른다** — 바이트로 자르면 한글이 깨진다.

### 왜 메모를 기본에서 뺐나

`.guild/.gitignore` 가 `quests/*.memo.md` 를 제외한다 — 메모는 공유되지 않는
개인 기록이다. 게다가 이 서버는 **인증이 없고 공개 바인드가 가능하다**(실제로
이 세션에서 `0.0.0.0` 으로 띄웠다). 기본 포함이면 같은 네트워크의 누구나
호스트의 사적 메모를 훑을 수 있다. 기본 끔의 비용은 플래그 하나, 켰을 때의
위험은 되돌릴 수 없는 노출이다.

### 왜 첨부에 캐시를 안 만들었나

첨부 사이드카는 *첨부가 있는 문서만* 갖는다 — 실측으로 **퀘스트 607개에
사이드카 2개, 전부 읽는 데 0.54ms**. 역인덱스를 만들면 마이그레이션 + reindex
단계 + 증분 갱신 훅이 붙고 staleness 버그 여지가 생기는데, [[REQ-008]] 에서
정확히 그 구조 때문에 두 번(댓글 누락 / reindex 전 미반영) 구멍이 났다.
backlink 은 역방향 조회라 인덱스가 **필수**였지만 첨부 이름은 정방향이라
그럴 이유가 없다. 스캔은 덤으로 **항상 최신**이다.

### 진입점

- CLI `openguild search <query> [--in ...] [--limit N] [--json]`
  — `comments` / `worklog` 처럼 최상위 횡단 명령(`cli-command-naming` 의
  "명사 그룹 단수형" 규칙은 CRUD 명사 그룹 대상이라 이 동사형과 무관).
- HTTP `GET /api/search?q=…&in=…&limit=…`
- 둘 다 `Backend::search_enhanced` 로 묶어 **로컬/원격 파리티**를 지킨다.
- 알 수 없는 영역 이름은 **에러**로 알린다 — 조용히 무시하면 "왜 안 찾히지"
  로 이어진다.

**검색 팔레트는 이번 범위 밖**이다. 팔레트는 전체 목록을 클라이언트에 적재해
필터하는 구조라, 댓글까지 넣으려면 서버 왕복 + 디바운스 + stale-async 가드가
딸린 다른 상호작용 모델이 된다 — 별도 퀘스트가 맞다.

## 검증

**단위 테스트 6건 신규** (`cargo test -p openguild-core` 414 passed):
본문 매치 / **댓글에만 있는 단어 + 출처 보고** / **메모 기본 제외 & opt-in** /
영역 넘나드는 AND / **한글 부분일치**(`테마전환` → `다크테마전환`) / 빈 질의.

**CLI 실측** (테스트 길드)

| 질의 | 결과 |
|------|------|
| `search "미해결 토론"` | `quest DEV-007 [comment]` + 발췌가 그 댓글 |
| `search preview.png` | `quest DEV-007 [attachment]` |
| `search "본인 한정"` | `(결과 없음)` — 메모 기본 제외 |
| `search "본인 한정" --in body,comment,memo` | `[memo]` 로 매치 |

**HTTP 실측** — 같은 질의에 CLI 와 동일 결과. `in=bogus` → **400**.

server 210 / cli 115 passed.

## 테스트 방법

1. 댓글에만 있는 단어로 `openguild search <단어>` → 그 문서가 나오고
   `[comment]` 로 표시되는지.
2. 첨부 파일 이름으로 검색 → `[attachment]`.
3. 메모에만 있는 단어 → 기본은 안 나오고, `--in memo` 를 붙여야 나오는지.
4. 기존 검색 회귀 — `openguild quest list --search <단어>` 결과가 이전과
   같은지(이번 변경이 그 경로를 건드리지 않았다).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)

## Successors
- [REQ-010](REQ-010.md)
- [REQ-011](REQ-011.md)
- [REQ-012](REQ-012.md)
- [REQ-013](REQ-013.md)
- [BUG-247](BUG-247.md)
- [BUG-249](BUG-249.md)
<!-- openguild:auto-end -->
