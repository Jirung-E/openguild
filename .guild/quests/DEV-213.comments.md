<!-- og-comment id="1" ts="2026-07-03T22:24:57+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03 작성 — 착수 전 검토용)

**변경 파일**: `gui/frontend/src/lib/components/QuestCommentsSection.svelte` 단일.

1. 상태: `let discussionOnly = $state(false)` + 파생 `discussionCount`(전체 entries 중 `discussion` true 수), `unresolvedCount`.
2. UI: 섹션 헤더(제목 옆)에 필터 토글 버튼 — `💬 토론만 N (미해결 M)` / 활성 시 강조(class:on). 토론 댓글이 0개면 버튼 숨김. scope=campaign 은 discussion 기능이 quest 전용이므로 미노출.
3. 필터링: groups 파생 이후 렌더 단계에서 적용 — root 카드는 **"스레드 안(자신+자손)에 토론이 하나라도 있으면"** 표시(토론 답글의 문맥 보존), 스레드 내부에서는 토론 아닌 entry 를 dimmed(불투명도) 처리로 문맥 유지. 완전 숨김보다 dim 이 대화 흐름 파악에 유리 — 구현 단순(entry 에 class:dimmed).
4. 정렬/접기 상호작용: 필터 on 시 collapsedRoots 무시하고 강제 펼침(접힘 때문에 안 보이면 필터 의미 없음) — DEV-214 와 시너지.
5. 홈의 길드 전체 '토론 댓글' 섹션(DEV-150)과 무관 — 이건 상세 내 필터.

예상 규모: ~60줄. 테스트: 수동(토글 → 토론 스레드만 표시/비토론 dim, 해결/미해결 카운트, 캠페인 미노출).
