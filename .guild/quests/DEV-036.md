+++
quest_id = "DEV-036"
title = "릴리즈 패키지 절차 (CHANGELOG / 태그 / GitHub Release)"
status = "open"
urgency = 3
prerequisites = ["DEV-034", "DEV-035"]
created_at = "2026-05-19 13:47:30"
updated_at = "2026-05-19 13:47:30"
deleted = false
+++

릴리즈 한 번 만들 때 반복되는 종합 절차 정리. 사전 점검 → 버전 동기화 →
CHANGELOG → tag → GitHub Release artifact.

## 사전 점검 체크리스트

- [ ] develop 의 모든 testing quest 가 done 으로 정리.
- [ ] `cargo test --workspace` 통과.
- [ ] `cd gui/frontend && npm test -- --run` 통과.
- [ ] `npm run check` 0 errors.
- [ ] `cargo tauri build` 정상 (3 OS 각각).
- [ ] AGENTS.md / docs/planning.md / docs/architecture-refactor.md 갱신
      (Phase 5 자동 검증 ✅).
- [ ] 알려진 BUG 의 closure 또는 known-issue 명시.

## 버전 동기화

다음 파일들의 version 값을 `X.Y.Z` 로 일관 갱신:
- `core/Cargo.toml`
- `cli/Cargo.toml`
- `server/Cargo.toml`
- `gui/Cargo.toml`
- `gui/tauri.conf.json`
- `gui/frontend/package.json`

후속 quest 후보: 한 곳 (workspace `[workspace.package]`) 에서 관리하도록 통합.

## CHANGELOG

`CHANGELOG.md` 신설 / 갱신 — Keep-a-Changelog 형식:
```
## X.Y.Z — YYYY-MM-DD

### Added
- ...

### Fixed
- ...

### Known issues
- ...
```

## tag 작업 절차

```bash
git checkout develop
# 모든 testing 정리, 버전 업데이트 commit
git commit -m "[chore] X.Y.Z 버전"
git checkout master
git merge develop
git tag vX.Y.Z
git push origin master --tags
```

## GitHub Release artifact

- DEV-034 의 workflow 가 tag push 트리거로 installer 자동 첨부.
- description: CHANGELOG 의 해당 버전 절 그대로.

## 후속

- 사용자 피드백 수집 채널 (GitHub Issues / discussions).
- 메이저 cleanup quest (멀티유저 / JWT / Campaign / 등).
- auto-updater (Tauri updater plugin) 도입 시점 결정.

## 우선순위 사유

`urgency = 3`. 매 릴리즈마다 반복되는 절차. 한 번 문서화하면 이후 사이클이 단순.
DEV-034 (installer) + DEV-035 (icon) 의존.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
