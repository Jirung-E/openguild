<!-- og-comment id="1" ts="2026-08-02T18:33:58+09:00" author="claude" -->
﻿저장 파일명을 {정리한 원본 이름}-{고유값 16hex}.{ext} 로. 원본 이름을 모르면(붙여넣기) 예전처럼 고유값만.

안전장치: 경로 구분자·제어문자·윈도우 예약문자는 _ 치환, 앞뒤 공백/점 제거, 60자 상한, 빈 이름은 ile. Path::file_stem 은 쓰지 않는다 — 윈도우에서 :b.zip 의 : 를 드라이브 접두로 보고 통째로 버린다(테스트로 발견).

경로: 데스크탑은 원본 경로에서 이름을 그대로, 브라우저/CLI 는 name 을 함께 전달(HTTP body + Tauri invoke args 에 optional name 추가 — 예전 클라이언트 호환).

검증: CLI 로 설계 문서 v2.zip 첨부 → .guild/attachments/설계 문서 v2-67a707303c76eea5.zip. 단위 테스트로 traversal·예약문자·중복·길이·빈이름 고정.
