<!-- og-comment id="1" ts="2026-06-06T13:12:37+09:00" author="claude" -->
## 현황 (step 1 — backend 만)

본 quest 의 spec 4 영역 중 backend (모델 + DB) 만 완료. 향후 UI / 토글 / API 노출은 별도.

## 사용 (API 응답 변화)

`CampaignSummary` (Home active carousel, Campaign Detail) 에 새 필드 3개:
```json
{
  "campaign_slug": "C-001",
  "title": "베타 1.0.0",
  "progress": 0.12,           // 기존 — 체크리스트 진행률
  "checklist_total": 17,
  "checklist_checked": 2,
  "quest_total": 18,          // NEW — 링크 quest 수 (alive)
  "quest_done": 3,            // NEW — status.counts_as_done=true 인 수
  "quest_progress": 0.166     // NEW — quest_done / quest_total
}
```

## 구현

### Migration 0012
- `quest_statuses` 에 `counts_as_done INTEGER NOT NULL DEFAULT 0` 추가.
- 기존 길드의 `done` / `cancelled` slug 자동 backfill = 1.

### StatusFile + seed
- frontmatter `counts_as_done: bool` (기본 false, skip_serializing_if).
- 새 길드의 seed: `done` / `cancelled` = true, 나머지 false.

### reindex
- file → DB sync 시 `counts_as_done` 도 binding (file 진리원).

### summarize (services::campaigns)
- 링크된 quest 중 alive + status.counts_as_done=1 인 수 계산.
- `quest_progress = quest_done / quest_total` (0 일 때 0.0).

## 테스트
1. 새 길드 init → done/cancelled status file 의 `counts_as_done = true`.
2. 기존 길드 reindex → DB 의 quest_statuses 의 done/cancelled 가 1.
3. 캠페인에 quest 링크 + status done 으로 → API 응답의 quest_progress 갱신.
4. status 정의 file 의 counts_as_done 직접 toggle → reindex 후 quest_progress 변화.

## 향후 (이 quest 범위 외 — 별도 작업)
- Home / Campaign Detail 의 progress bar 2개 표시 (체크리스트 + 퀘스트).
- Admin → Statuses 의 `counts_as_done` 토글 UI.
- CLI `openguild statuses update <slug> --counts-as-done true|false`.
- Tauri command + HTTP route 의 counts_as_done 필드 노출.

## 검증
- cargo test --workspace 482/482 pass.
- cargo clippy --workspace -D warnings exit 0.

<!-- og-comment id="2" ts="2026-06-06T13:56:37+09:00" author="claude" -->
## DEV-093 step 2 — Home active 카드 progress 2 줄

backend (status.counts_as_done + quest_progress) 완료 후속.

## 변경
- `CampaignSummary` 타입에 quest_total / quest_done / quest_progress 추가 (optional — 서버 미배포 환경 fallback).
- `CampaignCard.svelte` (active mode):
  - 기존 progress-row 에 "체크" 라벨 추가.
  - questTotal > 0 이면 두 번째 progress-row "퀘스트" 추가.
  - 두 번째 row 도 done 상태 시 초록.
- 시각: 한 progress bar 가 두 줄로 나란히, 각 줄에 라벨 + bar + 카운트.

## 사용 확인
1. Home 으로 진입 → active 캠페인 carousel 카드.
2. 캠페인에 quest 가 링크되어 있고 그 status 의 counts_as_done = true 인 quest 가 있으면 두 번째 progress bar 표시.
3. 링크된 quest 가 없으면 두 번째 bar 자동 숨김 (기존 동작 보존).

## 한계 (별도 quest)
- Campaign Detail 페이지의 같은 표시 — 본 라운드 미포함.
- Admin → Statuses 의 counts_as_done 토글 UI — 본 라운드 미포함.
- 기본 status 외에 사용자가 토글하려면 file 의 frontmatter 직접 편집 + reindex 필요.

## 자동 검증
- `npm run check` 0 errors.
- type 변경은 optional 필드 추가만 — 기존 코드 영향 없음.

<!-- og-comment id="3" ts="2026-06-06T14:06:06+09:00" author="claude" -->
## DEV-093 step 3 — Campaign Detail 페이지 + QuestStatus model 확장

Home 카드 (step 2) 후속.

## 변경

### Backend
- `models::meta::QuestStatus` 에 `counts_as_done: bool` 추가 (DB ↔ model sync).
- `models::campaign::CampaignDetail` 에 quest_total / quest_done / quest_progress.
- `ops::campaigns::fetch_detail` 가 SQL 로 done slug fetch 후 client 측 계산
  (linked_quests 의 status_slug 와 매칭).
- `ops::meta::create_status` 의 결과 QuestStatus 에 기본 false.

### Frontend
- types/index.ts 의 QuestStatus / CampaignDetail 에 새 optional 필드.
- Campaign Detail 의 "연결된 퀘스트 (N)" 헤더가 quest_total > 0 이면
  "(done/total, %)" + 100% 시 ✓ 완료 + green progress bar 표시.

## 사용
1. `/campaigns/C-001` 같은 Detail 페이지.
2. 헤더가 "연결된 퀘스트 (3/5, 60%)" 처럼 표시 (counts_as_done 인 status 기준).
3. 100% 시 "✓ 완료" 마크 + bar 초록.
4. linked quest 없음 → 기존 동작.

## 테스트
1. 캠페인에 quest 5 개 링크, 그 중 3 개를 status done 으로.
2. Campaign Detail 페이지 → "연결된 퀘스트 (3/5, 60%)".
3. 나머지 2 개도 done → "(5/5, 100%) ✓ 완료" + bar 초록.
4. cancelled status 도 카운트 (counts_as_done = true backfill).

## 자동 검증
- cargo test --workspace 482/482 pass.
- cargo clippy --workspace -- -D warnings exit 0.
- npm run check 0 errors.
- BUG-016 help_output_has_no_quest_id_leaks 통과.

## 남은 (별도 quest)
- Admin → Statuses 의 counts_as_done 토글 UI.
- CLI `openguild statuses update <slug> --counts-as-done true|false`.
- HTTP / Tauri command 의 update_status 필드 노출.

<!-- og-comment id="4" ts="2026-06-06T14:30:53+09:00" author="claude" -->
## DEV-093 step 4 — Admin Statuses 의 counts_as_done 토글 UI

backend 의 미노출 필드 (counts_as_done) 를 Admin → Statuses 에서 사용자가 토글 가능.

## 변경

### Backend
- `ops::update_status` 에 `counts_as_done: Option<bool>` 인자 (8번째).
  - file (StatusFile.counts_as_done) + DB (UPDATE quest_statuses) 양쪽 sync.
- `models::QuestStatus` 의 `counts_as_done` 이 이미 노출됨 (DEV-093 backend).
- gui::commands::UpdateStatusBody + admin_update_status 가 counts_as_done 전달.

### Frontend
- adminApi.updateStatus body 의 optional `counts_as_done`.
- AdminStatusesSection:
  - table 헤더 "완료" column 추가 (tooltip: "캠페인 진행도 계산 시 '완료' 로 카운트").
  - read mode: ✓ 또는 — 표시.
  - edit mode: 체크박스 (busy 시 disabled).

### CLI
- 본 step 에선 미노출 (CLI 에서 None 으로 호출).
- 향후 `openguild statuses update <slug> --counts-as-done true|false` 추가 가능.

## 사용
1. /admin 진입 → Statuses 섹션.
2. 각 status 의 "완료" column: done / cancelled 가 ✓, 나머지 — (기본).
3. 수정 → 체크박스로 토글 → 저장.
4. Campaign Detail / Home 카드의 quest_progress 가 즉시 반영.

## 테스트
1. "testing" status 의 "수정" 클릭 → 체크박스 체크 → 저장.
2. testing status 의 quest 가 캠페인 progress 의 done 으로 카운트되는지.
3. 다시 체크 해제 → 원복.
4. file (`.guild/statuses/{order}-testing.toml`) 의 `counts_as_done = true` 확인.
5. 다른 quest mutation 후 sync 유지.

## 자동 검증
- cargo test --workspace 482/482 pass.
- cargo clippy --workspace -- -D warnings exit 0 (too_many_arguments allow).
- npm run check 0 errors.

## 남은 (별도 quest)
- CLI `openguild statuses update --counts-as-done` 옵션.
- HTTP route (서버 모드) — frontend 가 Tauri 만 사용하므로 우선순위 낮음.

<!-- og-comment id="5" ts="2026-06-06T17:48:23+09:00" author="" -->
(gui 기준) 연결된 퀘스트 중 done으로 되어있는게 3개나 있음에도 홈에서 봤을때 퀘스트 진척도가 0으로 나온다.

<!-- og-comment id="6" ts="2026-06-06T18:08:25+09:00" author="claude" -->
## 사용자 보고 (2026-06-06)
"(gui 기준) 연결된 퀘스트 중 done 으로 되어있는게 3개나 있음에도 홈에서 봤을때 퀘스트 진척도가 0으로 나온다."

## 원인
기존 길드 (DEV-093 commit 이전부터 존재) 의 `.guild/statuses/{n}-done.toml` / `cancelled.toml` 에 `counts_as_done` 키가 없음 (옛 file 형식).

- migration 0012 가 적용된 시점 SQL backfill (`UPDATE quest_statuses SET counts_as_done=1 WHERE slug IN ('done','cancelled')`) 은 성공.
- 그러나 다음 reindex 시 file → DB sync 가 file 의 `counts_as_done` 키 누락 → TOML default false → backfill 덮어씀.
- 결과: 기존 길드의 done/cancelled status 의 counts_as_done 이 0 → quest_progress = 0.

검증: `sqlite3 .guild/index.db "SELECT slug, counts_as_done FROM quest_statuses"` 모든 row 가 0.

## 수정
`core/src/reindex.rs` 의 status 적재:
```rust
let counts_as_done = s.counts_as_done
    || matches!(slug, "done" | "cancelled");
```

slug 가 done/cancelled 인 경우 file 키 누락도 자동 true. 직관 일치 — "완료된 상태는 진행도 카운트".

trade-off: file 에 명시 `counts_as_done = false` 와 키 누락이 TOML default 로 둘 다 false → 구분 불가. done/cancelled slug 에 명시 false 의도는 best-effort 로 덮임 (드물 케이스).

## 검증
- 본 길드 `openguild-server reindex` 후 `campaign show C-001` 의 `quest_done=3` (이전 0).
- cargo test --workspace 482/482 pass.
- cargo clippy --workspace -- -D warnings exit 0.

## 향후
- file 에 키 명시 vs 누락 구분 (TOML 의 Option<bool> 사용 — `Option<bool>` 으로 변경 후 None 일 때만 backfill default).
- 본 길드의 `.guild/statuses/4-done.toml` + `6-cancelled.toml` 에 `counts_as_done = true` 추가도 가능 — 단 fix 가 적용된 후엔 의미 약함.

<!-- og-comment id="7" ts="2026-06-06T19:49:33+09:00" author="" -->
홈 페이지에 표시되는 '진행중인 캠페인'에서, 연결된 퀘스트는 아직 다 완료되지 않았는데 체크리스트만 100%가 돼도 캠페인 완료로 표시됨

<!-- og-comment id="8" ts="2026-06-07T01:14:36+09:00" author="claude" -->
## fix2 (2026-06-07): 캠페인 완료 판정 — 체크리스트 + 연결 quest 양쪽 100%

이전엔 `CampaignCard.completed` 가 `checklist_total > 0 && checklist_checked === checklist_total` 만 봐서, 체크리스트 100% 달성했지만 연결된 quest 가 아직 done 이 아닌 캠페인이 "✓ 완료" 로 표시되고 overdue 필터에서도 사라지던 문제.

새 규칙 (`CampaignCard.completed` + `Home.overdueCampaigns`):
- 체크리스트 + 연결 quest 둘 다 있음 → 둘 다 100% 일 때만 완료.
- 체크리스트만 있음 → 체크리스트 100% 일 때 완료.
- 연결 quest 만 있음 → quest 100% 일 때 완료.
- 둘 다 없음 → 완료 판정 안 함 (정보 부족).

수정:
- `gui/frontend/src/lib/components/CampaignCard.svelte`: `completed` derived.by 재작성.
- `gui/frontend/src/lib/components/Home.svelte`: overdue 필터 동일 로직.

commit: d234637
