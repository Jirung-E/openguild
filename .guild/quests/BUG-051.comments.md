<!-- og-comment id="1" ts="2026-06-14T20:20:00+09:00" author="claude" -->
[결정] 파일이 진리 — 이번에 guild rule(file-truth-db-cache)로 명문화됨. 그러므로 옵션 (a): file 의 testing 이 맞고, reindex(시동 자동 BUG-049 / 상세 lazy DEV-137 / ⟲)로 DB 를 file 에 맞춘다. DB 의 done 은 stale 였던 것.

CLI 의 idempotent skip(DB 가 이미 목표 상태면 file 재기록 생략)도 그대로 둠 — file 진리 원칙을 깨지 않음(ops 는 항상 file+DB 동시; skip 은 '바뀐 게 없을 때' 재기록만 생략). BUG-067 에서 ops 가 cached_mtime 도 동기화하게 해, 이런 잔류 drift 는 reindex/sync 로 일관 회복됨.

→ 코드 변경 없음(정책+BUG-049/067 으로 해소). 종결.
