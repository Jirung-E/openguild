<!-- og-comment id="1" ts="2026-08-03T08:49:11+09:00" author="claude" -->
수정:

- `QuestListItem.svelte` 에 `flat` prop 추가. 리스트 뷰에서는 `.toggle` 을 아예
  렌더하지 않는다(`visibility:hidden` 이 아니라 미렌더 — 20px + gap 자리 자체를
  없앰). `QuestList.svelte` 가 `flat={viewMode === 'list'}` 전달.
- 2줄(모바일) 배치에서 `.title { padding-left: var(--title-indent) }`.
  `--title-indent` = 트리 뷰 `calc(20px + 0.6rem)`, 리스트 뷰 `0px`.
  PC(단일 행)는 미디어 쿼리 밖이라 영향 없음.

검증 (375px, DOM 측정):

- 트리 뷰: SLUG 배지 left 45.6px, 제목 텍스트 시작 45.6px (Range.getClientRects
  로 측정 — padding-left 라 rect.left 는 16px 이지만 실제 글자는 45.6px).
- 리스트 뷰: 토글 없음, SLUG left 16px, 제목 left 16px — 둘 다 왼쪽 끝.
- 가로 스크롤 없음 (scrollWidth 375 = viewport).
- `npm run check`: 724 files, 0 errors.
