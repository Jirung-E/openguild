<!-- og-comment id="1" ts="2026-08-14T21:41:16+09:00" author="codex" -->
구현 시점 재대조 결과, 퀘스트 등록 당시의 11번 항목 중 "전역 comments는 Local 전용" 부분은 이후 [[DEV-349]]에서 원격 `/api/comments`와 CLI HTTP 경로가 구현되어 더 이상 현행 불일치가 아니었다. 따라서 레퍼런스의 local/remote 지원 설명은 유지하고, 배포 SKILL의 `cross-project` 표현만 "선택한 길드 내부 quest+campaign 검색"으로 수정했다.

추가로 CLI 자체에서 두 건을 발견해 함께 수정했다.

- 백업 목록이 비었을 때 존재하지 않는 `openguild backup create`를 안내하던 문구 → `backup new`
- snapshot 복원 성공 후 파일 시스템이 자동 갱신되지 않는다고 출력하던 과거 문구 → 파일 진리원과 `index.db`가 복원되고 journal은 보존된다는 실제 동작
