<!-- og-comment id="1" ts="2026-06-28T14:15:44+09:00" author="claude" -->
## E2E 스모크 결과 — 격리된 temp 길드 + 실제 서버 바이너리

연결된 Chrome 브라우저가 없어 실제 GUI 클릭 자동화는 불가. 대신 격리된 temp 길드(`%TEMP%/og-e2e-smoke`, 실 저장소 밖)에 `openguild init` → `openguild-server.exe host`(FRONTEND_DIST=빌드된 build/, DEV-195 단일 origin) 실제 기동 후 curl로 18단계 전부 실행·확인. 단위테스트(DEV-196)와 달리 진짜 프로세스+디스크+포트로 검증.

| # | 단계 | 결과 |
|---|---|---|
| 1 | SPA 루트 `/` | 200 |
| 2-4 | quest 생성 → 목록 → status 변경(in_progress) | OK |
| 5-6 | 댓글 추가, 메모 설정 | OK |
| 7-11 | campaign 생성 → quest 링크 → checklist 추가/체크 → detail(진행도) | OK |
| 12-14 | admin snapshot / drift / reindex | OK |
| 15 | SPA 딥링크 `/quests/DEV-001` (새로고침 시나리오) | 200 (DEV-195 fallback 정상) |
| 16 | 존재하지 않는 quest 조회 | 404 (에러 처리 정상) |
| 17-18 | admin restore(최신 snapshot) + 데이터 무결성 | OK |

**첨부(attachment)**: `AttachmentSection.svelte`에서 `isTauri` 게이트로 브라우저 모드는 명시적으로 차단(`"첨부 추가는 데스크탑 앱에서만 지원됩니다"`) — **버그 아님, 의도된 데스크탑 전용 제약**으로 확인.

## 작업 중 발견·수정한 사고 (기록)
이 검증 도중 `openguild init --guild .e2e-tmp`를 저장소 루트에서 실행했다가 `--guild` 플래그가 무시되고(또는 init이 cwd 고정) 실 저장소의 길드 마커 `openguild.guild`(git tracked)가 덮어써졌음. 즉시 `git checkout -- openguild.guild`로 복구, 이후 모든 검증은 `cd`로 격리된 절대경로 temp 디렉토리에서만 수행. **별도 갭 발견**: `openguild init` 이 `--guild` 옵션을 cwd 대신 인식하지 않는 듯함 — 후속 확인 필요(별도 이슈로 분리할지는 사용자 판단).

## GUI 시각 확인 — 수동 체크리스트 (사용자용)
자동화 불가 항목 — 실제 브라우저(:5173 dev 또는 단일 origin 빌드)에서 1회 확인 권장:
- [ ] 퀘스트 목록/보드 페이지 렌더링, 상태 변경 버튼 클릭 동작
- [ ] 퀘스트 상세 페이지 딥링크 새로고침 시 정상 렌더링(DEV-195 SPA fallback 시각 확인)
- [ ] 댓글/메모 작성 UI 동작
- [ ] 캠페인 페이지, 체크리스트 토글 UI
- [ ] Settings/Admin 페이지에서 백업·reindex 버튼 동작
- [ ] 브라우저 모드에서 첨부 버튼이 "데스크탑 앱에서만 지원" 안내로 막히는지(의도된 동작 확인)

<!-- og-comment id="2" ts="2026-06-29T22:41:49+09:00" author="claude" -->
## 재검증 (2026-06-29) — DEV-113/DEV-193/BUG-093 등 그간 변경분 포함

Chrome 확장 미연결로 다시 curl 기반(격리된 temp 길드, 실제 서버 바이너리,
직전 사고 재발 방지 위해 `cd` 후 `init` 으로 안전하게 격리). 기존 18단계
전부 재확인 + 그 사이 추가된 항목 신규 검증 — 전부 통과:

| 항목 | 결과 |
|---|---|
| 기존 18단계(quest/comment/memo/campaign/checklist/admin snapshot·drift·reindex/딥링크/404/restore) | 전부 OK (재확인) |
| `GET /api/guild-info` (DEV-113) | OK — `{"name":"og-e2e-smoke2"}` |
| `GET/POST/PATCH/DELETE /api/admin/types` (DEV-193) | 전부 OK |
| `GET/POST/PATCH/DELETE /api/admin/statuses` (DEV-193) | 전부 OK (한글 `name_ko` 포함 — curl shell 인코딩 문제였고 서버는 정상) |
| reindex 시 parent/prerequisite 가 unknown status 인 quest 를 가리키는 경우 (BUG-093) | FK 에러 없이 skip 처리됨 — `{"reason":"unknown status slug: ..."}`, dependencies_loaded:0, 정상 |
| admin restore 후 데이터 일관성 | OK — snapshot 시점 quest 개수로 정확히 복원 |

결론: 그간 추가된 변경분 포함해서 server 경로 전체 재검증 완료, 회귀 없음.
GUI 시각 확인 체크리스트(브라우저 클릭)는 Chrome 확장 연결 시 별도 권장
— 자동화 도구로는 여전히 미실시.
