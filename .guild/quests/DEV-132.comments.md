<!-- og-comment id="1" ts="2026-07-03T22:40:03+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03)

본문 확인대로 **backend 변경 불필요**(reactions 는 임의 문자열) — UI 전용, 소형.

1. **(b) picker 확장 우선**: reaction '+' popup(REACTION_SET 4종)을 두 줄로 — 상단 고정 4종 + '최근 사용' 몇 개 + `직접 입력` 필드(이모지 1~2자 입력 → 적용). OS 이모지 피커(Win+.)로 입력 가능하니 자체 전체 피커 라이브러리 도입은 **불필요**(번들 비대 방지).
2. **(a) 사용자 세트 편집**: localStorage `customReactions: string[]` — 설정 퀵메뉴 아님, picker popup 안에서 '최근 사용'이 자연 축적(최대 8개 LRU). 별도 설정 UI 는 생략 가능 — 착수 시 사용자에게 (a) 필요 여부 확인.
3. 유효성: grapheme 단위 1개(이모지 조합 고려 — Intl.Segmenter)로 제한, 공백/':'/'|' 금지(reactions 직렬화 포맷 충돌 방지 — `emoji:author|author` 구분자).
4. CLI(DEV-199 react 명령)와 정합 — 동일하게 임의 이모지 허용.
5. 변경 파일: QuestCommentsSection.svelte(picker 부분) + 신규 store 소형.

예상 규모: 소형(~80줄). 선행 없음.
