<!-- og-comment id="1" ts="2026-06-07T21:24:37+09:00" author="" -->
접은 상태가 저장이 되는거같은데, 저장할 필요 없음. 재접속시 그냥 펼쳐져있는상태를 기본으로 해도 된다.

댓글에 답글이 달렸을때, 답글들만 접을 수 있는 기능도 필요(접힌 답글의 하위 답글들도 전부 접혀야함)

<!-- og-comment id="2" ts="2026-06-07T23:06:10+09:00" author="claude" -->
## fix1 (2026-06-07): 사용자 피드백 반영 — 영속 제거 + 답글 단위 접기

요구 (id=1):
1. 접은 상태 localStorage 영속 X → 매 진입 시 펼침 기본.
2. 답글 (한 root 의 자식 entries) 단위로 접기 가능 — 하위 답글도 포함.

수정 (`QuestCommentsSection.svelte` / `QuestNoteSection.svelte`):
- `loadCollapsed` / `COLLAPSE_KEY` / `localStorage.setItem` 제거. `let collapsed = $state(false)` 만.
- `collapsedRoots: Set<number>` 추가 (Comments 전용).
- root 댓글 아래 답글이 있으면 토글 라인 (`답글 N개 접기/펼치기`) 추가.
- 1-level threading 이므로 root 접으면 모든 자손 답글 자동 hidden.

commit: 6029293
