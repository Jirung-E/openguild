<!-- og-comment id="1" ts="2026-06-25T00:53:12+09:00" author="claude" discussion="true" -->
⚠️ 전제 재검증 필요. 이 퀘스트는 "frontend가 호출하는 types/statuses 조회 라우트가 HTTP에 없음"이 전제인데, 실제 server/src/routes/mod.rs 에는 `/api/quest-types`(meta::list_quest_types)·`/api/quest-statuses`(meta::list_quest_statuses)가 등록돼 있고 tests.rs 에도 통과 테스트가 있음. 즉 **읽기 라우트는 존재**.

진짜 갭 후보 (재범위):
- types/statuses **쓰기/admin**(생성·수정·정렬)은 HTTP에 없음 — tag-defs 만 upsert/delete 있음. GUI에서 types/statuses 편집 UI가 있다면 브라우저 모드에서 그 부분만 깨짐.
- 또는 DEV-161 작성 시점 이후 이미 닫힌 갭.

조치: GUI(admin 등)에서 types/statuses 를 편집하는 경로가 있는지 확인 → 있으면 그 쓰기 라우트 추가, 없으면 본 퀘스트 close/축소. (읽기 파리티는 이미 충족.)
