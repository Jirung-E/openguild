+++
quest_id = "DEV-178"
title = "DB 캐시 엔티티 외부편집 반영 통일 — campaigns/types/statuses/tags"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-06-21T18:39:55+09:00"
updated_at = "2026-06-24T01:11:26+09:00"
deleted = false
+++

DB 캐시로 읽는 엔티티 중 외부편집 반영 경로가 없는 것들을 quest 와 대칭으로 통일.

대상: campaigns(본문), types, statuses, tag defs — 현재 services/meta.rs + camp get 이 DB 캐시만 읽고, drift.rs 는 quest 본문 + 댓글/메모 sibling 만 검사해 이들 외부편집은 재시작해도 미반영(수동 reindex 만).

작업:
1. refresh_campaign_if_stale 추가 + get_campaign 에서 호출 (상세 lazy, DEV-137 대칭).
2. drift 에 campaigns/types/statuses/tags 본문 검사 추가 (file_mtime_cache 재사용, 스키마 변경 없이) → 시동 sync 반영.
3. 규칙 .guild/rules/file-truth-db-cache.md §4 sync 지점 표 + docs/storage-design.md 커버리지 표 갱신.

rules/templates 는 파일 직독이라 제외.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
