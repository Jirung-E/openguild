+++
quest_id = "DEV-090"
title = "[docs] architecture / storage-design 갱신 — gui workspace + Campaign + Quest due 반영"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-05-31T14:12:48+09:00"
updated_at = "2026-05-31T14:19:12+09:00"
deleted = false
+++

audit 결과 우선순위 높음 2건: 1) architecture.md 의 Cargo workspace members 에 gui 누락 + gui/ 내부 구조 (Tauri Rust src/, capabilities/, icons/, nsis/, tauri.conf.json, build.rs) 누락 + server routes (campaigns.rs) + core repo (campaign.rs) + 신규 quest endpoint (due/type/campaigns) 누락. 2) storage-design.md 에 Campaign (DEV-011) entity / 파일 형식 / 4 테이블 + Quest due (DEV-076) 컬럼 / migration 0008·0009 전혀 미반영.

## 구현 (2026-05-31)

### docs/architecture.md
- **Cargo Workspace 구조** 트리 갱신:
  - members 에 `gui` 명시.
  - `core/src/`: `repo/campaign.rs`, `services/campaigns.rs`, `ops/campaigns.rs`,
    `recents.rs` 등 누락분 추가.
  - `server/src/routes/campaigns.rs` 추가.
  - `gui/` 전체 구조 (Tauri Rust + Svelte) — `src/`, `capabilities/`, `icons/`,
    `nsis/`, `tauri.conf.json`, `build.rs`, `gen/`, `frontend/` 세분화.
  - `scripts/seed-test-data.ps1` (DEV-075) 추가.
- **변경 이력**에 gui crate / Campaign (DEV-011) / 멀티 컴포넌트 NSIS / Quest
  due (DEV-076) / Tauri 자동 업데이트 (DEV-063) / 설정 페이지 (DEV-084) 추가.
- **API 엔드포인트** Quest 도메인에 `/history` (DEV-013), `/type` (DEV-055),
  `/due` (DEV-076), `/campaigns` (DEV-011) 추가.
- **Campaign** 신규 섹션 — 11개 endpoint (CRUD + 체크리스트 + 링크 +
  active-summaries).
- **데이터 모델** 진리원 표에 Campaign 파일 추가. Quest frontmatter 필드에
  desired_due / required_due. Campaign frontmatter 필드 명시.
- **index.db 테이블 표**에 status_slug (DEV-046), quest_slug (DEV-049),
  quest_history (DEV-013), campaigns / campaign_checklists / campaign_quests
  / campaign_counters (DEV-011), desired_due / required_due (DEV-076) 추가.

### docs/storage-design.md
- 디렉토리 트리에 `.guild/campaigns/` 추가. git tracked 목록 갱신.
- Quest frontmatter 필드 표에 `desired_due` / `required_due` (DEV-076).
- **Campaign 파일 형식 (B3) 섹션 신설**:
  - frontmatter 예시 + 9개 필드 표 (campaign_id / title / status / period /
    linked_quests / display_order / 시각 / deleted).
  - 본문 체크리스트 단방향 sync (file → DB) 설명.
  - linked_quests 다대다 + Quest Detail 역참조.
  - campaign_counters 단일 row 카운터.
- `index.db (캐시)` 섹션의 스키마 설명 갱신 — quest_history + campaign 4
  테이블 + due 컬럼 / migration 0001~0009 명시.

### 변경 없음
- AGENTS.md, README.md, dev-plan.md, AGENTS_OPENGUILD_USAGE.md, planning.md,
  guild-rules.md, architecture-refactor.md — 본 quest 범위 밖 (audit 의 중·낮
  우선순위 항목, 별도 처리).

### 검증
- `cargo build` (코드 변경 없음, sanity only) → 동일.
- `git diff --stat` → 2 files, +164 / -21.
- keyword: `campaign` 28+13 (이전 0+0), `due` 6+2 (이전 0+0).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
