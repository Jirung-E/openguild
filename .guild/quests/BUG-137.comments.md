<!-- og-comment id="1" ts="2026-07-18T01:12:46+09:00" author="claude" -->
재오픈 확인 후 재검증 (2026-07-17, Windows dev 환경):
- `vite dev` 로 /, /welcome, /admin, /worklog, /library 전부 200 (500 재현 안 됨).
- ssr=false 유지 확인, 앱 클라이언트 부팅 정상.
- 브라우저에서 본문에 "Failed to fetch" 가 뜨는 건 백엔드(:3000) 미실행 시
  데이터 로드 실패 표시 — SSR 크래시가 아니라 정상 에러 상태. `just dev`
  (서버 포함)로 띄우면 사라짐.
- cold start 직후 vite 의 의존성 재최적화로 "Failed to fetch dynamically
  imported module" 콘솔 에러가 1회 스쳤다가 자동 리로드로 복구되는 것도
  관찰 — 새로고침하면 정상인 일시 현상.

어떤 명령/URL 에서 무엇이 떴는지(500? 빈 화면? 콘솔 에러?) 알려주시면
그 경로로 다시 파겠습니다.

<!-- og-comment id="2" ts="2026-07-18T14:10:46+09:00" author="claude" -->
재확인 결과 이미 해결되어 있습니다.

퀘스트 설명의 해결책(`gui/frontend/src/routes/+layout.ts`에
`export const ssr = false;` + `export const prerender = false;`)이
코드에 이미 적용돼 있고(BUG-137 참조 주석 포함), 방금 fresh `vite dev`로
직접 재검증했습니다:

- `/`, `/welcome`, `/admin`, `/worklog`, `/library`, `/settings`,
  `/rules`, `/quests/DEV-254` 전부 200 (500 없음).
- `/` 응답 본문에 `ReferenceError`/`window is not defined` 등 SSR 크래시
  흔적 없음.

원래 재현 시도(2026-07-17)에서도 같은 결과였고, 이번에 다시 한 번
fresh 서버로 확인해 재현 안 됨을 재확인했습니다. testing으로 이동합니다.
