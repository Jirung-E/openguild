<!-- og-comment id="1" ts="2026-07-27T21:32:21+09:00" author="claude" -->
## CI 확인 결과

`fffe576` 실행에서 `upload-artifact@v7` 이 정상 동작 —
`Upload notes preview -> success`, artifact `release-notes-preview` 1877 bytes
(v4 때와 동일 크기).

경고 자체가 사라졌는지는 **로그로 확인하지 못했다** — Node 20 deprecation 은
check-run annotation 이 아니라 로그/실행 요약에만 남고, 로그 다운로드는 인증이
필요하다(비인증 403).

다만 근거는 확정적이다: 두 워크플로가 쓰는 모든 액션의 `action.yml` `runs.using`
을 pinned ref 로 직접 읽어 node20 이 남아 있지 않음을 확인했다
(checkout/setup-node/cache v5 = node24, rust-cache v2 = node24,
upload-artifact v7 = node24, gh-release v3 = node24, rust-toolchain = composite).
경고는 node20 을 target 하는 액션에 대해서만 발생하므로 발생 조건 자체가 없다.

`gh-release@v3` 은 릴리스 잡에서만 실행되므로 다음 태그 push 때 최종 확인.
