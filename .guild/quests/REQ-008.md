+++
quest_id = "REQ-008"
title = "문서 하단 '이 문서를 참조하는 문서' 섹션 (cross-link backlink)"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-08-19T23:57:43+09:00"
updated_at = "2026-08-22T20:01:39+09:00"
deleted = false
+++

## 요청

문서 하단에 **'이 문서를 참조하는 문서'** 섹션을 추가한다.
해당 문서로 cross-link(`[[...]]`)를 건 문서들을 역방향으로 표시한다.

## 배경

현재 cross-link 는 **단방향**이다 — A 가 `[[B]]` 를 걸면 A 에서 B 로는 갈 수
있지만, B 를 보고 있을 때 "누가 나를 참조하는가" 를 알 수 없다. 설계 결정이
여러 퀘스트에 흩어져 있을 때 역방향 추적이 안 되면 맥락을 놓친다.

## 기대 동작

- 문서(퀘스트/캠페인/규칙/도서관) 상세 하단에 backlink 섹션
- 그 문서를 `[[...]]` 로 참조하는 모든 문서를 나열(제목 + 링크)
- 참조가 없으면 섹션 자체를 숨기거나 "없음" 표시

## 구현 고려사항

- **역인덱스가 필요하다.** 전체 문서 본문을 매번 스캔하는 것은 문서 수가 늘면
  비용이 크다. index.db 에 backlink 테이블을 두는 방식이 유력하나,
  [[BOOK-001]] 불변식상 **index.db 는 파일의 폐기가능 투영**이므로 반드시
  reindex 로 100% 재구축 가능해야 한다(DB→파일 역류 금지).
- cross-link 문법의 접두 별칭(`quest`/`q`, `campaign`/`c`, `rule`/`rules`/`r`,
  `book`/`library`/`lib`)과 접두 생략형을 모두 파싱해야 한다.
- 규칙 slug 는 공백을 포함할 수 있다(BUG-156, 예: `[[코딩 규칙]]`).
- 존재하지 않는 대상을 가리키는 깨진 링크는 backlink 집계에서 제외.

---

## 수정 내용 (2026-08-20)

결정대로 **index.db 역인덱스**.

### 색인 (파일 → DB 단방향)

- `core/migrations/0030_doc_links.sql` — `doc_links(src_kind, src_id,
  dst_kind, dst_id)`, `(dst_kind, dst_id)` 인덱스.
- `core/src/repo/crosslink.rs` — `[[...]]` 토큰 추출. **프론트 문법 미러**
  (`questIndex.ts` + `MarkdownView.svelte`): 대괄호/개행 제외 1~64자,
  `kind:` 별칭(quest/q, campaign/c, rule/rules/r, book/library/lib),
  BUG-156 의 공백 포함 규칙 slug 허용.
- `core/src/reindex.rs` **6e 단계** — 모든 문서 적재가 끝난 뒤에 돈다.
  링크를 **색인 시점에 해석**해 확정된 것만 담는다(깨진 링크 제외).
  그래서 조회 측이 우선순위(quest > campaign > book > rule)를 다시 구현하지
  않아도 되고, DEV-219 의 네임스페이스 충돌도 여기서 한 번만 정리된다.
  자기 자신 참조는 제외. 1단계 wipe 에 `DELETE FROM doc_links` 추가 —
  [[BOOK-001]] 대로 언제 지워도 reindex 로 100% 재구축된다.

### 조회

- `core/src/ops/backlinks.rs` — 읽기 전용. 제목은 각 캐시 테이블에서
  `COALESCE` 로 끌어오고, index.db 캐시가 없는 규칙은 slug 를 제목으로 쓴다.
- `server`: `GET /api/backlinks/{kind}/{id}`. 알 수 없는 kind 는 400 —
  조용히 빈 목록을 주면 "backlink 가 없는 문서" 와 구분이 안 된다.
- `gui`: Tauri `list_backlinks` 커맨드 + `transport.ts` 매핑. **데스크톱은
  HTTP 가 아니라 invoke 를 타므로 이걸 빠뜨리면 런타임에 throw 한다**
  (transport 가 그렇게 알려준다).

### UI

`BacklinkSection.svelte` — 퀘스트 / 캠페인 / 규칙 / 도서관 상세 4곳의 변경이력
바로 위에 배치. **참조가 없으면 섹션 자체를 렌더하지 않는다** — 대부분 문서엔
backlink 가 없고 빈 섹션이 쌓이면 소음이다.

[[REQ-004]] 에서 지적된 stale-async 문제를 처음부터 막았다 — 결과를 반영하기
전에 `wantKind/wantId` 가 아직 현재 대상인지 재확인한다. reindex 후에는
`reindexBump` 로 다시 읽는다.

## 검증

- `cargo test -p openguild-core` **408 passed** (crosslink 파서 7건 신규)
- `cargo test -p openguild-server` **210 passed**
- 프론트 **417 passed** (42 파일)
- 실 데이터 reindex → `doc_links` **77건** 색인.
  종류별 분포: quest→quest 51, quest→rule 9, quest→book 7, book→quest 4,
  quest→campaign 2, rule→book 2, rule→rule 2.

API 실측:

| 요청 | 결과 |
|------|------|
| `/api/backlinks/book/BOOK-001` | 8건 (DEV-218·292·355, REQ-003·005·008, release-process, test-data-script) |
| `/api/backlinks/quest/dev-033` (소문자) | 3건 — 대소문자 무시 정상 |
| `/api/backlinks/bogus/X-1` | 400 |
| `/api/backlinks/quest/REQ-999` | `[]` |

UI 실측 — BOOK-001 상세에서 섹션 제목 "이 문서를 참조하는 문서 8", 항목 8개,
링크 경로가 종류별로 정확(규칙 `?slug=`, 퀘스트 `/quests/`). DEV-033 에서
3건(DEV-140/173/256) 확인.

**시각 확인 한계**: Claude 브라우저 패널이 hidden 이라 스크롤된 영역이 다시
칠해지지 않아 섹션 스크린샷은 비어 나온다. DOM/계산값으로만 확인했다.

## 부수 수정 — REQ-007 이 깨뜨린 테스트

전체 스위트를 돌리다 `QuestHistory.test.ts` 8건 실패를 발견했다. [[REQ-007]]
로 변경이력이 **기본 접힘**이 되면서 목록이 DOM 에 없어진 탓인데, 그 퀘스트를
머지할 때 컴포넌트 테스트를 돌리지 않아 놓쳤다.

테스트가 내용을 검증하기 전에 펼치도록 `renderExpanded()` 헬퍼를 넣었고,
**기본 접힘 계약 자체를 검증하는 테스트를 새로 추가**했다(접힌 동안 항목 0개
+ `aria-expanded=false` → 클릭 후 1개 + `true`).

## 테스트 방법

1. `openguild reindex` 후 BOOK-001 도서관 문서를 연다 — 하단에 "이 문서를
   참조하는 문서" 8건이 보이는지.
2. 목록의 규칙/퀘스트 링크가 각각 올바른 화면으로 가는지.
3. 아무도 참조하지 않는 문서에서는 섹션이 **아예 없는지**.
4. 퀘스트 본문에 `[[BOOK-001]]` 을 새로 추가 → `reindex` → 그 퀘스트가
   BOOK-001 의 목록에 나타나는지.
5. 데스크톱(Tauri) 앱에서도 동일하게 뜨는지 — HTTP 가 아니라 invoke 경로.

---

## 후속 수정 (2026-08-21) — 사용자 테스트에서 드러난 두 구멍

테스트 길드에서 "BOOK-001 하단에 참조 문서가 안 보인다" 는 보고로 확인.
**첫 구현은 실사용에서 사실상 동작하지 않는 상태였다.**

### 1. 댓글을 색인하지 않았다

본문(description/body/content)만 훑고 **댓글은 빼먹었다.** 그런데 참조는
댓글에서 더 자주 걸린다 — 설계 논의가 댓글에서 오간다. 시드 스크립트조차
"댓글의 `[[BOOK-001]]` 참조" 를 도서관 cross-link 검증 대상으로 명시해 뒀는데,
바로 그 케이스가 안 잡혔다.

reindex 6e 단계에서 `quest_comments` / `campaign_comments` 캐시도 함께 읽는다.
댓글은 독립 문서가 아니라 딸린 것이므로 **src 는 소속 문서**로 기록한다.

### 2. reindex 때만 갱신됐다 — 실사용에서 치명적

`doc_links` 를 reindex 가 통째로 다시 만들기만 했다. 사용자는 reindex 를 상시
돌리지 않으므로, 방금 단 cross-link 가 backlink 에 나오지 않았다. 실측으로
확인: 댓글에 `[[BOOK-002]]` 를 달아도 조회 결과가 `[]`, reindex 후에야 나타남.

문서 본문/댓글이 바뀔 때 **그 문서가 내보내는 링크만** 다시 계산하도록
`ops::backlinks::refresh_for(store, kind, id)` 를 추가하고 쓰기 경로에 연결했다:

- 퀘스트 댓글 add / update / delete
- 캠페인 댓글 add / update / delete
- 퀘스트 본문 `update_quest`
- 캠페인 본문 `update_campaign`
- 규칙 `set_rule`
- 도서관 `update_book`

`BUG-189` 가 `doc_history` 를 즉시 투영하도록 고친 것과 **같은 이유·같은
방식**이다("예전엔 reindex 를 돌기 전까지 작업기록에 안 떴다").

여전히 파일 → DB 단방향이다([[BOOK-001]]). 이 테이블은 파생물이고 언제 지워도
reindex 로 100% 재구축된다 — 증분 갱신은 그 사이를 메울 뿐이다. 색인 실패는
본 작업(댓글 작성 등)을 실패시키지 않는다.

## 검증 (테스트 길드)

| 확인 | 결과 |
|------|------|
| 댓글의 `[[BOOK-001]]` 색인 | `DEV-007 → BOOK-001` 잡힘 |
| **reindex 없이** 새 댓글 `[[BOOK-003]]` | 즉시 `[('quest','DEV-007')]` |
| 그 댓글 삭제 | 즉시 `[]` (다른 링크 BOOK-001 은 보존) |
| UI (BOOK-001 상세) | "이 문서를 참조하는 문서 1" + DEV-007 표시 |
| API 양방향 | `book/BOOK-001` ↔ `quest/DEV-007` 정상 |

`cargo test -p openguild-core` **408 passed**, 워크스페이스 빌드 정상.

### 조사 중 두 번 밟은 함정 (기록)

`cargo build -p openguild-core` 만 돌리고 CLI/서버를 다시 안 빌드해, **옛
바이너리로 검증하다 "수정이 안 먹는다"고 오판**했다. 이 저장소에서 core 를
고쳤으면 `--workspace` 로(또는 해당 크레이트를 명시해) 다시 빌드해야 한다.

---

## 후속 — 형제 섹션과 모양 맞추기 (2026-08-21)

같은 상세 페이지에 나란히 있는 **변경 이력 섹션과 세 곳이 달랐다**(사용자 지적).

### 1. 접을 수 없었다

[[REQ-007]] 이 변경 이력에 접기를 넣었는데 backlink 섹션에는 없었다. 같은 정책을
적용한다 — **기본 접힘**, 상태는 **영속하지 않음**(`QuestNoteSection` 의
`collapsed` 와 동일). 마크업도 `.section-toggle` + `.toggle-icon` 으로 같게.

접힌 동안 목록은 `{#if !collapsed}` 로 **DOM 에 아예 없다**(숨김이 아님).

### 2. 개수 표시 모양이 달랐다

`.bl-count` 가 맨 텍스트였는데 `.qh-count` 는 알약 뱃지였다:

| | 이전 | 지금(= `.qh-count`) |
|---|---|---|
| padding | 없음 | `0.05rem 0.4rem` |
| border-radius | 없음 | `10px` |
| background | 없음 | `var(--bg-subtle)` |

두 섹션이 세로로 나란히 놓여 카운트 모양이 다르면 바로 눈에 띈다.

### 3. slug 이 pill 이 아니었다

`.bl-id` 가 맨 monospace 텍스트였다. 같은 식별자가 화면마다 다르게 읽히는
[[DEV-362]] 와 같은 문제다. **보드 노드(`.node-pill.mono`) 공식**을 적용했다 —
높이 17px / radius 9px / 10px·600 / monospace / 배경 16% / 테두리 55%.

종류 색은 항목(`li.bl-item.{kind}`)에서 `--bl-c` 로 **한 번만** 정하고 종류 칩과
slug pill 이 함께 쓴다. 같은 색을 두 곳에 따로 적으면 갈라진다(DEV-362 에서
실제로 겪은 문제).

## 검증 (계산값 대조)

| 항목 | 결과 |
|------|------|
| 접기 | 기본 `aria-expanded=false` + 목록 미렌더 → 클릭 시 렌더 |
| 카운트 | backlink 와 변경 이력이 **모든 값 동일** (11.52px / `0.8px 6.4px` / 10px / 같은 배경·글자색) |
| slug pill | 17px / 9px / 10px / 600 / 1px / monospace — 노드 공식 일치 |

프론트 418 passed, `svelte-check` 0 errors.

> 참고: pill 공식이 화면마다 갈라져 있는 근본 문제는 [[DEV-364]] 에서 전수
> 조사·통일한다. 여기서는 **현재 기준(노드 공식)** 에 맞춰 두었다.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)

## Successors
- (없음)
<!-- openguild:auto-end -->
