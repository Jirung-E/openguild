<!-- og-comment id="1" ts="2026-08-04T18:52:00+09:00" author="claude" -->
수정 — `QuestNoteSection.svelte`:

- 고정 모드 본문(`.memo-body.fixed`)에 `OverlayScrollbar` 부착.
- native 스크롤바 숨김(`scrollbar-width: none` + `::-webkit-scrollbar`)
  — 앱의 다른 스크롤 영역과 같은 처리.

## 검증 (실측, rAF shim 적용)

임시 메모(80줄)를 넣고 고정 모드로 전환 후 박스 높이를 140px 로 줄여 스크롤을
만든 상태:

- `scrollbar-width: none` 적용 확인 (native 숨김).
- 내부 스크롤 발생(scrollHeight 342 / clientHeight 138)에서 overlay thumb
  **1개** 생성, 높이 55.7px.
- 스크롤 시 `visible` 클래스 부착.
- thumb 좌표가 메모 박스 안(top 3324, 박스 3275~3415).

검증용 메모는 확인 후 `quest memo clear` 로 삭제했다(메모는 gitignored).
