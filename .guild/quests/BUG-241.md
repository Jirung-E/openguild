+++
quest_id = "BUG-241"
title = "첨부 '전체 다운로드' 가 브라우저 모드에서 1개만 저장됨"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-08-21T12:49:30+09:00"
updated_at = "2026-08-22T11:12:17+09:00"
deleted = false
+++

## 증상

첨부 '전체 다운로드' 를 눌렀는데 **파일이 하나만 저장된다**(브라우저 모드).

## 원인 (계측으로 확정)

우리 코드는 정상이다. `AttachmentSection.svelte` 의 `downloadAll()` 은
브라우저 모드에서 첨부마다 `<a download>` 를 만들어 200ms 간격으로 클릭한다.

첨부 3개짜리 퀘스트에서 anchor 클릭을 가로채 계측한 결과 **3건 모두 정확한
이름과 URL 로 시도**됐다:

```
seed-note.md  → /api/guild-files/attachments/seed-note-….md
preview.png   → /api/guild-files/attachments/preview-….png
config.json   → /api/guild-files/attachments/config-….json
```

즉 **브라우저가 같은 출처의 연속 자동 다운로드를 2번째부터 차단**하는 것이다
(Chrome 계열의 "여러 파일 다운로드" 정책 — 주소창에 허용 프롬프트가 뜨거나
조용히 막힌다). 지연을 늘려도 근본 해결이 안 된다.

Tauri(데스크톱) 모드는 폴더를 고른 뒤 `copy_guild_file` 로 직접 복사하므로
이 문제가 없다 — **브라우저 모드 한정**이다.

## 왜 차단되는가 — 우리가 자초한 구조 (추가 조사)

브라우저는 **사용자 제스처와 붙어 있는 다운로드**만 허용한다. 그런데 현재
루프는 파일마다 `await` 를 두 번 건다:

```js
for (const a of list) {
    browserDownload(await guildFileUrl(a.path), downloadName(a));
    await new Promise((r) => setTimeout(r, 200));
}
```

`guildFileUrl` 은 브라우저 모드에서 **실제로 기다리는 게 없다** — `isTauriLocal()`
분기를 지나 URL 문자열을 조립해 돌려줄 뿐이다(`utils/banner.ts:55~64`).
그런데도 `async` 라 `await` 가 걸리고, 여기에 200ms 타이머까지 더해져 2번째
파일부터는 제스처 컨텍스트에서 떨어져 나간다. 그 상태의 자동 다운로드가 바로
브라우저가 막는 대상이다.

즉 "브라우저 정책 때문에 어쩔 수 없다" 가 아니라, **우리 코드가 차단 조건을
만들고 있었다.**

## 수정 방향 (zip 은 채택하지 않음)

처음엔 서버 zip 스트리밍을 1순위로 적었으나 철회한다 — **사용자가 압축을 풀어야
한다**(사용자 지적). 파일 3개 받자고 압축 해제를 강요하는 건 퇴보이고, [[BUG-188]]
때문에 스트리밍 구현까지 필요해 비용도 가장 크다. 첨부가 수십 개일 때만 이득인데
그건 예외적 상황이다.

### 1순위 — 폴더 선택 후 직접 쓰기 (File System Access API)

`showDirectoryPicker()` 로 폴더를 받고 각 파일을 `createWritable()` 로 쓴다.
**데스크톱(Tauri) 경로와 똑같은 흐름**이 되어 코드 모양도 수렴한다.
압축 해제 없음, 권한 팝업 한 번, 파일명 그대로.

Chrome/Edge 지원. Safari/Firefox 미지원 → 아래 폴백.

### 2순위 폴백 — 제스처 안에서 한 번에 발사

URL 을 **미리 전부 계산해 두고**(브라우저 모드에선 동기로 가능), 클릭을
`await` 없이 연속 실행한다. 그러면 브라우저가 "여러 파일 다운로드" 를 **한 번**
묻고, 허용하면 N개가 그대로 받아진다. 폰(iOS Safari)에서 접속하는 경우가 이
경로를 탄다.

폴백에서도 안 되는 환경이 있으면, 개별 다운로드로 유도하는 안내를 띄운다.

## 테스트 방법

브라우저 모드(서버 접속)에서 첨부가 2개 이상인 퀘스트를 열고 '전체 다운로드'
를 누른다 — 첨부 개수만큼 파일이 저장되는지 확인. 데스크톱 앱에서도 폴더 선택
후 전부 복사되는지 회귀 확인.

---

## 수정 내용 (2026-08-21)

`AttachmentSection.svelte` 의 브라우저 경로를 `downloadAllInBrowser()` 로 분리.

### 공통 — URL 을 먼저 다 만든다

클릭 사이에 `await` 가 끼면 제스처가 끊긴다. `guildFileUrl` 을
`Promise.all` 로 **미리 전부 해결**한 뒤에야 저장을 시작한다.

### 1순위 — 폴더 선택 후 직접 쓰기

`showDirectoryPicker()` → 각 파일 `createFileHandle` + `createWritable`.
**데스크톱(Tauri) 경로와 같은 흐름**이라 압축 해제도, 다운로드 팝업도 없다.

`res.body.pipeTo(writable)` 로 **스트리밍**한다 — `arrayBuffer()` 로 받으면
[[BUG-188]] 의 1.5GB 첨부가 메모리에 통째로 올라간다. [[REQ-005]] 가 지적한
서버 측 버퍼링과 같은 함정을 클라이언트에서 반복하지 않는다.

폴더 선택 **취소(`AbortError`)는 오류가 아니다** — 조용히 끝낸다.

### 2순위 폴백 — 제스처 안에서 연속 발사

폴더 쓰기를 지원하지 않는 환경(Safari/iOS — 폰 접속이 이 경로)에서는
`await` 없이 앵커 클릭을 연속 실행한다. 그러면 브라우저가 허용을 **한 번**
묻고, 허용하면 전부 받아진다.

허용 프롬프트를 놓치면 여전히 1개만 받고 끝나므로 안내를 띄우되,
**"차단됐다" 고 단정하지 않는다** — 우리는 결과를 알 수 없고 대개는 정상
저장된다. 그래서 빨간 오류가 아니라 **info 토스트**로 알린다.

## 검증 (테스트 길드 DEV-007, 첨부 3개)

| 경로 | 결과 |
|------|------|
| 폴더 쓰기 (가짜 핸들 주입) | `seed-note.md` 86B / `preview.png` 70B / `config.json` 49B **실제 기록**, 앵커 클릭 0건, 오류 없음 |
| 폴백 (`showDirectoryPicker` 제거) | 클릭 3건, **첫→마지막 간격 0ms** (기존엔 파일당 200ms), info 토스트 1건, 빨간 오류 없음 |
| 폴더 선택 취소 (`AbortError`) | 오류 없음, 폴백으로도 안 넘어감 |
| 진짜 실패 | "전체 다운로드 실패: …" 오류 표시 |

`svelte-check` 0 errors, 프론트 **418 passed**.

## 테스트 방법

1. 브라우저(Chrome/Edge)에서 첨부 2개 이상인 퀘스트 → '전체 다운로드' →
   폴더를 고르면 **첨부 개수만큼** 그 폴더에 저장되는지.
2. 폴더 선택을 취소했을 때 오류가 안 뜨는지.
3. Safari/iOS 에서 → 허용 프롬프트가 뜨고, 허용하면 전부 받아지는지.
4. **데스크톱 앱 회귀** — 폴더 선택 후 일괄 복사가 그대로 동작하는지
   (이 경로는 건드리지 않았다).

---

## 최종 수정 — 폴백을 zip 으로 (2026-08-21, 모바일 검증 후)

앞선 폴백("제스처 안에서 연속 클릭")은 **모바일에서 동작하지 않았다.** 사용자가
폰으로 확인: 안내 토스트는 떴지만 여전히 1개만 저장.

### 왜 모바일에서는 1순위도 폴백도 안 되는가

- 폰은 `http://<LAN IP>:3100` 으로 접속한다. **평문 HTTP + IP 라 보안 컨텍스트가
  아니다** — `showDirectoryPicker` 는 `[SecureContext]` 라 아예 정의되지 않는다.
  (실측: `http://localhost` 는 `isSecureContext: true`, 폴더 API 존재)
- 게다가 iOS Safari / 안드로이드 Chrome 은 File System Access 를 **지원하지도
  않는다** — 주소가 HTTPS 여도 마찬가지다.
- 모바일 브라우저는 제스처당 다운로드를 사실상 1건만 처리한다.

즉 그 환경에서 첨부를 전부 받게 하는 방법은 **다운로드를 1건으로 만드는 것**
뿐이다. **zip 을 배제했던 앞선 판단은 데스크톱만 보고 내린 것이라 틀렸다.**

### 환경 판별이 아니라 기능 판별

`typeof window.showDirectoryPicker === 'function'` **한 줄**로 갈린다. UA 파싱은
하지 않는다 — 알아야 할 것은 기기 종류가 아니라 "여기서 폴더에 쓸 수 있는가"
이고, 이 API 의 존재 자체가 *보안 컨텍스트 + 브라우저 지원* 을 모두 함축한다.

```
1. isTauri                   → 기존 폴더 복사 (데스크톱 앱, 손 안 댐)
2. showDirectoryPicker 있음  → 폴더에 직접 쓰기 (압축 해제 없음)
3. 그 외 / 2번이 실패        → zip 1건
```

2번이 **호출 중 실패**(권한 거부 등)해도 3번으로 넘어간다 — 어떤 경로로도 못
받는 상황을 남기지 않는다. 단 `AbortError`(사용자 취소)는 실패가 아니라 조용히
끝낸다.

### 서버 — 스트리밍 zip

`GET /api/quests/by/{slug}/attachments.zip` (+ campaigns / library).

- `tokio::io::duplex` 로 writer↔reader 를 잇고 별도 task 에서 zip 을 쓴다.
  응답은 그 reader 를 흘려보낸다 — **어느 쪽도 전체를 들고 있지 않다**
  ([[BUG-188]] 의 1.5GB 첨부, 폰은 메모리가 더 빠듯하다).
- **무압축(store)** — 첨부는 png/zip/mp4 등 이미 압축된 것이 대부분이라
  재압축은 CPU 만 쓴다.
- zip 안에서도 [[REQ-002]] 의 allowlist 검증을 그대로 통과시킨다 — 새 경로로
  우회되면 안 된다.
- 사이드카엔 있으나 디스크에서 사라진 파일은 건너뛴다.

의존성 추가: `async_zip`(tokio), `tokio-util`(io, compat).
`EntryStreamWriter` 는 futures 계열 `AsyncWrite` 라 tokio `File` 을 `compat()`
로 감싸 `futures_util::io::copy` 로 흘린다.

## 검증

**서버 (curl)**

| 확인 | 결과 |
|------|------|
| 헤더 | `application/zip`, `attachment; filename="DEV-007-attachments.zip"` |
| 스트리밍 | `transfer-encoding: chunked` (Content-Length 없음) |
| 무결성 | `zipfile.testzip()` **OK** |
| 내용 | `seed-note.md` 86B / `preview.png` 70B / `config.json` 49B, 전부 `compress_type=0`(store) |
| 첨부 없는 문서 / 없는 문서 | 404 |

**프론트 (브라우저 실측, 첨부 3개)**

| 경로 | 결과 |
|------|------|
| 폴더 쓰기 정상 | 3개 파일 기록, zip 다운로드 0건 |
| **모바일 재현**(폴더 API 제거) | 다운로드 **정확히 1건** = `DEV-007-attachments.zip` |
| 폴더 쓰기 실패(권한 거부) | zip 1건으로 자동 대체, 오류 표시 없음 |
| 폴더 선택 취소 | 아무 일도 일어나지 않음 |

`cargo test -p openguild-server` 210 passed, 프론트 418 passed,
`svelte-check` 0 errors.

## 테스트 방법

1. **폰에서** `http://<LAN IP>:3100` 접속 → 첨부 2개 이상인 퀘스트 →
   '전체 다운로드' → **zip 1개**가 받아지고, 풀면 첨부가 전부 있는지.
2. 데스크톱 Chrome/Edge 에서 `http://localhost` 접속 → 폴더를 고르면
   **압축 없이** 파일들이 그 폴더에 저장되는지.
3. 폴더 선택 취소 시 오류가 안 뜨는지.
4. **데스크톱 앱 회귀** — 폴더 선택 후 일괄 복사가 그대로 동작하는지.

### 후속 — zip 파일명에 길드 이름 포함 (사용자 요청)

`DEV-007-attachments.zip` 만으로는 여러 길드에서 받은 묶음이 다운로드 폴더에
섞였을 때 구분이 안 된다. `{길드}_{문서}_attachments.zip` 로 바꿨다.

이름은 **서버가 정한다.** 프론트에서 길드 이름을 다시 조회하면 `await` 가 끼어
제스처가 끊기고 이름 규칙이 두 곳으로 갈라진다. 그래서 앵커에 `download` 속성을
**일부러 붙이지 않는다** — 같은 출처에서는 그 속성이 서버
`Content-Disposition` 을 덮어쓰기 때문이다.

한글 길드명 때문에 두 가지를 더 처리했다:

- **RFC 5987** — `filename=` 만 쓰면 비ASCII 가 브라우저마다 다르게 깨진다.
  `filename*=UTF-8''<percent-encoded>` 를 함께 내고, 구형 클라이언트용으로
  ASCII 로 접은 이름을 `filename=` 에 남긴다.
- **파일명 안전화** — 길드 이름은 사용자가 정하므로 `/ \ : * ? " < > |` 와
  제어문자를 `_` 로 바꾼다. 한글은 그대로 둔다(파일명으로 정상이다).

#### 검증

| 길드 이름 | `Content-Disposition` |
|-----------|----------------------|
| `test-guild` | `filename="test-guild_DEV-007_attachments.zip"` |
| **`우리 길드`** (한글+공백) | `filename="______DEV-007_attachments.zip"; filename*=UTF-8''%EC%9A%B0%EB%A6%AC%20...` → 브라우저 해석 **`우리 길드_DEV-007_attachments.zip`** |

한글 길드명으로 받은 zip 도 무결성 OK / 항목 3개. 프론트 실측: 요청 1건,
`download` 속성 없음(서버 파일명 사용). server 210 passed.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)

## Successors
- (없음)
<!-- openguild:auto-end -->
