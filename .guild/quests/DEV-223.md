+++
quest_id = "DEV-223"
title = "[gui] [[ 자동완성 UX — 빈 [[ 트리거 + auto-close ]] 중복 방지"
status = "open"
urgency = 3
prerequisites = []
created_at = "2026-07-04T12:34:59+09:00"
updated_at = "2026-07-04T12:34:59+09:00"
deleted = false
+++

사용자 피드백(2026-07-04) 2건 — 둘 다 editor-links.ts / textarea-wikilink.ts 의 [[ 컨텍스트 자동완성(DEV-173 신설) 코드.

1. **빈 [[ 에서도 자동완성**: 현재 [[ 만 치면 제안 안 뜸(1자 이상 입력 필요 — 소음 방지 명목). 사용자는 [[ 입력 즉시 전체 후보(최신/전체 목록)가 뜨길 기대. CM(editor-links)은 explicit 호출만 허용 중 → activateOnTyping 시에도 빈 partial 허용으로 변경. textarea(wikiMatch)도 partial.length<1 가드 제거. 후보 많을 때 정렬 기준(최근 수정순? id 역순?) 결정 필요.

2. **auto-close ]] 중복**: CM 편집기는 closeBrackets 로 [[ 입력 시 ]] 가 자동 삽입되는데, 자동완성 apply 가 'slug]]' 를 붙여 [[slug]]]] 가 됨. apply 를 함수형으로 바꿔 **커서 뒤에 이미 ]] 가 있으면 생략**(또는 닫힘 포함 범위를 치환). textarea 는 auto-close 가 없어 현행 유지 — CM 만 수정.

관련: DEV-173(원 구현), DEV-220(bare 제거 — 같은 파일, 순차 작업 권장).

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
