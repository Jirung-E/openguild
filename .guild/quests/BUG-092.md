+++
quest_id = "BUG-092"
title = "[gui] reindex 후 보드 grid snap 점 표시 안 됨 — stale 캐시"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-06-28T15:43:16+09:00"
updated_at = "2026-06-28T15:44:29+09:00"
deleted = false
+++

reindex(Nav 의 ⟲) 또는 레인 순서 변경(swapLane) 후 grid snap 점 표시(toggleGridSnap 켜진 상태)가 사라짐. 원인: buildLaneDivs() 가 lane-col DOM 을 lanesEl.innerHTML=" 로 통째로 재생성하지만, syncLanes() 의 그리기 캐시 gridBgCache(Map, lane index 키)는 안 지워짐. 그래서 새로 생성된(비어있는) dotsEl 에 대해 캐시 히트로 오판(zoom/cols 동일) → 실제 backgroundImage 설정을 건너뜀 → 점이 안 그려진 빈 DOM 만 남음. 테마 변경 시엔 buildCyStyle 가 gridBgCache.clear() 를 호출해 정상이지만 buildLaneDivs 경로엔 빠져있었음. 수정: buildLaneDivs() 시작부에 gridBgCache.clear() 추가.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
