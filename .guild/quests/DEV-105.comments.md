<!-- og-comment id="1" ts="2026-06-06T17:29:55+09:00" author="claude" -->
## 현황 (partial — lane 폭 축소 미포함)

본 quest 의 spec 3 항목 중 2 (노드 hide + 세로 라벨 + 클릭 토글) 만:

### 완료
- ✅ lane 라벨 클릭으로 collapse 토글.
- ✅ collapsed 시 lane 의 모든 노드 hide (display: none).
- ✅ collapsed 라벨이 90도 회전 (writing-mode: vertical-rl).
- ✅ localStorage 영속 (`{guild}.collapsedLanes`).

### 미포함 (별도 작업 — LANE_W 상수 retrofit)
- ❌ lane 폭 자체 축소 (글자 1~2개 들어갈만한 폭). LANE_W / LANE_STRIDE 가 모든 layout 계산의 기반 상수 — variable 화 시 Cytoscape 노드 좌표 / lane bg / grid 시각화 / 모든 곳 retrofit. 큰 작업.

## 사용
1. lane header 의 라벨 클릭 → 토글.
2. collapsed: 노드 사라짐 + 라벨 세로.
3. 다시 클릭 → 펼침. 노드 복귀.
4. 새로고침 → 상태 유지.

## DEV-056 (노드 숨김 옵션) 와 별개
- DEV-056 의 hideSettings.hideGroup / hideSolo / laneHidden — 사용자가 옵션으로 hide.
- DEV-105 의 collapse — lane 토글로 그 lane 만 즉시 hide. 두 메커니즘 독립.

## 한계
- 노드를 다른 lane 으로 이동시키려면 일단 펼침 필요 (lane 자체가 없으므로). 본문의 "lane 폭 축소" 가 이 문제 해결인데, 본 라운드 미포함.
- collapsed lane 에 노드 새로 추가하면 DOM 상 hide 안 됨 — load 시 재계산. 또는 mutation 후 syncCollapsed 호출.

## 테스트
1. 'done' lane 의 라벨 클릭 → 노드 사라지고 라벨 세로.
2. 다시 클릭 → 노드 복귀.
3. 새로고침 → done lane 의 collapsed 유지.
4. 다른 lane 도 동일.

## 자동 검증
- `npm run check` 0 errors 0 warnings.

<!-- og-comment id="2" ts="2026-06-06T17:57:40+09:00" author="" -->
레인 폭 줄이는 작업은 왜 안했냐.
규모가 크든 작든 여기서 언급한건데 왜 따로 분리하려고 하냐.
진행해라.

<!-- og-comment id="3" ts="2026-06-06T18:27:41+09:00" author="claude" -->
## 사용자 명시 (2026-06-06)
"레인 폭 줄이는 작업은 왜 안했냐. 규모가 크든 작든 여기서 언급한건데 왜 따로 분리하려고 하냐. 진행해라."

## 수정
laneWidth(slug) / laneStride(slug) 헬퍼 — collapsed 면 LANE_COLLAPSED_W (40px), 아니면 LANE_W.

- `visibleLaneLeftOfStatus`: 일률 LANE_STRIDE 곱 → 각 lane 의 width 누적.
- `syncLanes` 의 lane-col / lane-hdr: visIdx 곱 → curLeft 누적 + lane 별 width 적용.
- `toggleLaneCollapsed`: 모든 노드의 visualX 재계산 (collapsed 변화로 뒷 lane left 누적 영향) + animate (150ms) + syncLanes.

absToVisualX / visualToAbsX 는 visibleLaneLeftOfStatus 통해 자동 새 width 반영.

## 시각 효과
- collapsed lane: 40px 폭으로 좁아짐. 라벨 세로 회전. 노드 hide.
- 펼침 시 LANE_W (948px) 로 복귀. 뒷 lane 도 누적 자리 갱신.
- 모든 노드 animate 로 부드러운 이동.

## 테스트
1. 'done' lane 의 라벨 클릭 → 40px 좁은 폭 + 세로 라벨 + 노드 사라짐.
2. 그 lane 뒤의 다른 lane 들이 왼쪽으로 당겨짐 (148px 회수 X — 정확히 (948-40)).
3. 다시 클릭 → 원래 폭 복귀.
4. 새로고침 → 상태 유지.

## 자동 검증
- npm run check 0 errors 0 warnings.

<!-- og-comment id="4" ts="2026-06-06T20:20:55+09:00" author="" -->
접혔을때 이름 출력 안됨. 이로 인해 다시 펼치기가 불가능. 근데 다른버튼(레인위치이동, 레인정렬버튼 등)은 또 표시가 되고있어서 어색. 이름은 출력하고 다른버튼은 출력하지 않아야한다.
또, 보드 확대/축소시 이름 표시줄은 확대/축소가 안돼는건지 노드 표시 영역과 크기가 안맞는다.

<!-- og-comment id="5" ts="2026-06-07T01:14:40+09:00" author="claude" -->
## fix2~7 (2026-06-07): 접힘 / 헤더 정리 / 영속 / hide 회귀

### fix2 — 접힘 lane label only + zoom 비례 + 보드설정 메뉴 통합 (e8bb991)
- `.lane-hdr.collapsed > :not(.lane-label) { display: none }` — 접힘 시 settings 영역 (cols-sel, arrange-group) 자동 숨김. 라벨만 표시.
- 헤더의 `◀ ▶` 제거 → 기존 '숨김 설정' 모달을 '보드 설정' 으로 확장: 첫 열에 ◀/▶ reorder.
- 토글 버튼: `⚙ 보드 설정` (icon 변경).

### fix3 — lane 헤더 크기 zoom 무관 (fd83f5a 공통)
- fix2 가 zoom 비례로 width / left + height / font-size 모두 적용했지만 UI overlay 로서 부자연.
- syncLanes 가 left / width 만 `* zoom + pan`, height / fontSize 는 CSS 고정값 사용.

### fix4 — collapsed lane 영속 복원 + 긴 이름 헤더 안으로 (af1f6a4)
- `loadCollapsedLanes` / `saveCollapsedLanes` 가 정의돼 있지만 onMount 의 영속 상태 복원 블록에서 `collapsedLanes = loadCollapsedLanes()` 호출이 빠져 있어 새로고침 시 매번 빈 set.
- `.lane-label.collapsed` 의 `max-height: 60px` 가 38px lane-hdr 보다 커서 긴 이름이 위로 overflow → `.lane-hdrs` 의 `overflow: hidden` 에 위쪽 잘림.
- 28px + ellipsis 로 가둠 (fix6에서 다시 변경).

### fix5 — 레인별 설정 ⚙ 토글 (e533540)
- cols-sel + arrange-group 이 자주 안 쓰는데 헤더 폭 크게 차지.
- `lanesSettingsOpen: Set<string>` (slug별 영속).
- `[label] [⚙] [cols-sel] [arrange-group]` — `.lane-hdr.settings-open` 일 때만 cols/arrange 표시.
- aria-expanded + title 동적.

### fix6 — collapsed label ellipsis 제거 + 위 정렬 (441181a)
- fix4 의 `max-height: 28px` + `text-overflow: ellipsis` 가 긴 이름을 '...' 으로 축약 → 사용자 피드백: 별로.
- `align-self: flex-start` + `padding-top: 4px` — 헤더 위쪽에 anchored, 위로는 안 새고 긴 이름은 아래로 자연스럽게 연장 (lane-hdrs 가 board 전체 inset).

### fix7 — 접었다 펴면 hideGroup / hideSolo 무시되던 버그 (ee61263)
- `toggleLaneCollapsed` 가 펼침 시 그 lane 의 모든 노드를 무조건 `'element'` 로 복구.
- 펼침 시 `computeHiddenIds()` 호출 → 그 결과 set 기반으로 노드별 display 결정.
- 접힘일 때만 lane 전체 hide.

### 자동 검증
npm run check 0 errors / 0 warnings.
