+++
quest_id = "DEV-220"
title = "[gui] cross-link 를 [[]] 명시형 전용으로 — bare 인식/자동완성 제거"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-07-04T11:48:17+09:00"
updated_at = "2026-07-05T15:15:14+09:00"
deleted = false
+++

사용자 결정(2026-07-04): (1) 본문 렌더에서 bare 추적번호(DEV-001 등)의 자동 링크 인식 제거 — [[..]] 로 감싼 경우에만 링크. (2) 일반 문자열 타이핑 중 자동완성 팝업(bare XXX-NNN 트리거) 제거 — [[ 컨텍스트에서만.

## 구현 (2026-07-04)
- MarkdownView: CROSS_LINK_RE/TEST 에서 bare 대안 제거 — [[..]] 단일 형태(비ASCII slug 허용, DEV-173 유지). rewrite 루프 단순화.
- editor-links(CM — 본문/메모/규칙 편집기): bare 경로(BEFORE_CURSOR + selfRef/prefix 매칭) 전부 제거, wikiContextCompletion([[ 안)만 위임.
- textarea-wikilink(댓글): 동일 — bare 분기 제거, [[ 컨텍스트만.
- 테스트: bare 케이스 기대값 반전(제안 안 함).
- CHANGELOG Unreleased 에 동작 변경 명시(기존 본문의 bare 참조는 더 이상 링크 안 됨 — 의도).

## 테스트 방법
1. 본문에 bare `DEV-001` 만 쓰고 저장 → 렌더에서 **일반 텍스트**(링크 아님).
2. `[[DEV-001]]` → 링크 정상(파랑/미존재 빨강), [[규칙slug]]/한글 slug 회귀.
3. 편집기/댓글에서 `DEV-` 타이핑 → **팝업 안 뜸**. `[[DEV` → 팝업 뜸 + 선택 삽입.
4. cd gui/frontend && npm run check && npm test -- --run.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
