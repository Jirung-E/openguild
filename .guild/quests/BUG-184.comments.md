<!-- og-comment id="1" ts="2026-08-01T15:52:22+09:00" author="claude" -->
## 테스트 방법
1. macOS 에서 `just test-rust` (또는 `cargo test -p openguild-gui --lib`)
   전체 통과 확인 — 이미 16 passed 확인함(코드리뷰 세션에서).
2. Windows/Linux 에서도 회귀 없이 통과하는지(canonicalize 는 non-symlink
   경로에선 원본과 동일해 no-op이라 영향 없을 것으로 예상).
