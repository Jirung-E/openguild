<!-- og-comment id="1" ts="2026-08-04T23:48:44+09:00" author="claude" -->
반영한 곳:

## 스킬 (agent 용 — CLI 진입점)

- `reference/rules.md` — **Tags** 절 신설. `rule tag list/add/remove/set`,
  공백 구분 인자 허용, 로컬·원격 모두 지원, frontmatter 가 진리원임을 명시.
- `reference/library.md` — **Tags** 절 신설(같은 내용), 첨부 절 제목 추가.
  더불어 **첨부 바이트는 백업/스냅샷에 포함되지 않는다**는 사실을 명시했다
  (BUG-188 이후 실제 동작인데 스킬에 안 적혀 있었다 — 에이전트가 "백업했으니
  안전" 이라고 잘못 안내할 수 있는 지점).
- `SKILL.md` — 치트시트의 `quest tag` 옆에 library/rule 태그 두 줄 추가.

## CHANGELOG

- Unreleased 요약에서 "첨부 64 MB 로 올림" → **스트리밍으로 상한 자체 제거**로
  수정(DEV-337 이후 사실과 달랐다).
- Added: 스트리밍 업로드, 대기 목록·취소(전체/개별), gzip 압축, CLI 도서관·규칙
  태그, 상위 퀘스트 지정 UI.
- Changed: 목록 API slim, 보드 최소 zoom 계산.
- Fixed: 인덱스 재적재, 데스크탑 길드 열기, 모바일 길게 눌러 선택, 스크롤바 2건.

## 코드 주석

`server/src/routes/attachments.rs` 의 `MAX_ATTACHMENT_BYTES` 주석이 아직도
"index.db blob 백업 때문에 상한을 둔다" 고 말하고 있었다 — 그 근거는 BUG-188
에서 사라졌고 지금 남은 이유는 body 버퍼링 방식이라는 점, 무제한 업로드는
`/api/attachments/stream` 이라는 점으로 고쳤다. (admin 이 "왜 64MB냐" 고 물은
바로 그 주석이다.)

검증: cargo test 709 passed, clippy 통과, npm run check 0 errors, vitest 358 passed.
