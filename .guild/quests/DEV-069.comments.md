<!-- og-comment id="1" ts="2026-06-12T00:56:00+09:00" author="claude" -->
## markdown 본문 로컬 이미지 표시 (DEV-087 논의에서 합류, 2026-06-12)

현재 quest/campaign 본문의 `![](로컬경로)` 는 안 뜸 — WebView 의 frontend origin (`http://tauri.localhost`) 에서 `file://` 로드가 cross-origin 차단되기 때문. 웹 URL (`https://...`) 만 동작.

해결 = 본 quest 범위에 포함:
1. Tauri **asset protocol** 활성화 (DEV-087 배너와 인프라 공유) — `convertFileSrc(absPath)` 로 `asset://` URL 생성.
2. `MarkdownView` 렌더 후처리 hook — `<img src>` 가 웹 URL 이 아니면 `.guild/` 기준 상대 경로로 해석해 asset URL 로 변환.
3. 권장 reference 형식: `![alt](attachments/foo.png)` — `.guild/attachments/` 기준.

백업 관련 (본문의 "백업시 db 저장" 요구에 대한 제안): 첨부를 DB blob 으로 넣는 대신 `.guild/attachments/` 를 **git tracked** 디렉토리로 두는 것을 권장 — 댓글 파일과 동일한 보장 수준, snapshot (index.db copy) 범위 밖이지만 git 으로 복원 가능. DB blob 은 index.db 비대화 + reindex 비용 문제. 이견 있으면 회신.

업로드 UI / drag-drop / 로컬(quest 별) 첨부 / 승격은 후속 DEV-097 에서.

<!-- og-comment id="2" ts="2026-06-13T01:46:20+09:00" author="admin" -->
클립보드에 이미지가 있을때 본문에 Ctrl+v로 붙여넣기가 가능해야한다.

<!-- og-comment id="3" ts="2026-06-13T01:48:09+09:00" author="admin" reply_to="1" -->
이거 저번에 말했던건데, git은 선택사항이지, 무조건 사용하는걸 전제로 하지 않음. 즉, 첨부파일도 db 백업이 되어야한다는거임.

<!-- og-comment id="4" ts="2026-06-13T01:50:28+09:00" author="admin" reply_to="2" -->
드래그&드랍으로도 가능해야한다.

<!-- og-comment id="5" ts="2026-06-13T01:52:34+09:00" author="claude" reply_to="3" -->
(#2 #3 #4 종합 답)

#3 수용 — git tracked 제안 철회. 저장 설계를 "파일 진리원 + DB blob 캐시" 로 변경 (댓글/메모의 DEV-102 패턴과 동일 원칙):

1. **백업**: migration 으로 `attachment_blobs(rel_path PK, bytes BLOB, mtime)` — 파일 write 시 blob 도 UPSERT. snapshot (index.db copy) 에 자동 포함 → git 없이도 복원 가능.
2. **self-heal**: reindex 가 양방향 sync — 디렉토리의 새/변경 파일 → blob 갱신, blob 만 있고 파일이 사라진 경우 → 파일 복원. snapshot restore 후 reindex 한 번이면 첨부 파일까지 돌아옴.
3. **#2 Ctrl+V**: 본문 편집기 (CodeMirror — quest/campaign detail) 에 paste handler — 클립보드 이미지면 `.guild/attachments/{ts}-{rand}.{ext}` 저장 + 커서에 `![](attachments/...)` 삽입.
4. **#4 drag&drop**: 같은 handler 의 drop 버전 — 이미지 파일 드롭 시 동일 처리.

v1 범위 외 (후속): 브라우저 (HTTP) 모드 업로드, 메모 편집기 paste, 첨부 삭제 UX (지금은 파일 지워도 self-heal 이 복원 — 삭제하려면 blob 도 지워야 함, 관리 UI 는 DEV-097).

<!-- og-comment id="6" ts="2026-06-13T11:22:14+09:00" author="claude" -->
구현 완료 (#2/#3/#4 반영).

**저장/백업** — 파일이 진리원(`.guild/attachments/{nanos}-{rand}.{ext}`) + `attachment_blobs` BLOB 캐시(migration 0018). git 선택사항이므로(#3) blob 이 snapshot 에 합류해 git 없이도 복원 가능. reindex 의 `sync_attachment_blobs` 가 양방향 self-heal: 새/변경 파일→blob, blob 만 남음(파일 소실)→복원.

**업로드 UX** (GUI, Tauri):
- #2 클립보드 이미지 Ctrl+V paste
- #4 파일 drag&drop
- `editor-attach.ts` 의 CodeMirror extension → base64 → `save_attachment` invoke → 커서에 마크다운 삽입(업로드 중 placeholder). quest/campaign 상세 편집기 양쪽 적용.

확장자 화이트리스트: png/jpg/jpeg/gif/webp/bmp/svg/mp4/webm/pdf.

v1 범위 외(→DEV-097): 브라우저(server) 모드 업로드, 첨부 삭제 UX, 메모 편집기 paste.

테스트: core attachments 2종(save+blob, self-heal 복원), 전체 워크스페이스 cargo test / svelte-check / clippy -D warnings 통과.
