<!-- og-comment id="1" ts="2026-07-11T16:55:15+09:00" author="claude" -->
admin 추가 요청: [[DEV-229]] 의 서버 설정 파일(openguild-server.toml)도 ~/.openguild/ 를 탐색 경로에 포함할 것 — 현재 탐색(--config 명시 / exe 옆 / cwd)에 `~/.openguild/openguild-server.toml` 추가(우선순위: --config > cwd > exe 옆 > ~/.openguild).

frontend dist(서버가 호스팅하는 html/js)는 이전 대상 아님 — 사용자 데이터가 아니라 서버 바이너리와 버전이 결합된 프로그램 자산이라, ~/.openguild 에 두면 exe 업데이트 후 낡은 dist 가 남아 미묘한 버전 불일치를 만든다. 설치 폴더 유지 + 특수 배치는 config 의 frontend_dist 로 지정하는 것으로 충분(admin 논의 확정).

<!-- og-comment id="2" ts="2026-07-11T17:47:57+09:00" author="claude" -->
구현 완료:

- core `user_dirs` 모듈 신설: `openguild_home()`(~/.openguild, 없으면 생성, 테스트는 OPENGUILD_HOME env 격리) + `sync_bundled_docs()`(설치 폴더 docs/*.md → ~/.openguild/docs — 첫 실행 복사 + 원본이 더 새 것일 때만 갱신). GUI 시동이 best-effort 호출(개발 실행처럼 docs/ 없으면 no-op).
- recents: `~/.openguild/recents.json` 으로 이전. 구 경로(ProjectDirs 3중 중첩)에 파일 있고 새 경로 없으면 1회 copy(구 파일 보존 — 다운그레이드 안전). OPENGUILD_RECENTS_DIR 테스트 override 유지.
- 서버 config 탐색에 `~/.openguild/openguild-server.toml` fallback 추가(--config > exe 옆 > cwd > ~/.openguild — 기존 우선순위 유지, 뒤에만 추가).
- NSIS 복사 대신 첫 실행 복사 채택 — 인스톨러 스크립팅 없이 앱 업데이트 후 문서 갱신도 자동으로 따라옴.
- AGENTS_OPENGUILD_USAGE 머리에 새 위치 안내. user_dirs 단위 테스트 2건.
