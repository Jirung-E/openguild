<!-- og-comment id="1" ts="2026-08-04T00:58:55+09:00" author="claude" -->
원인 1 수정 — `routes/rules/+page.svelte`, `routes/library/+page.svelte`:

`loadList()` 에 `mutated` 인자를 추가하고 `loadQuestIndex(mutated)` 로 바꿨다.
이 함수는 마운트 시에도, 생성/삭제/이름변경 후에도 호출되는데 이전엔 구분 없이
`force=true` 라 페이지 진입마다 memo 를 무시하고 4종을 통째로 다시 받았다.
마운트 경로는 이제 memo 되는 `loadQuestIndex()` 를 타고, 실제 변경이 있었던
호출부(생성/삭제/이름변경, 각 4곳)만 `true` 를 넘긴다. 저장/제목변경 직후의
직접 `loadQuestIndex(true)` 호출은 진짜 변경이므로 그대로 뒀다.

## 재측정 (같은 방법, 라우트 이동 16회)

문서 페이지 왕복(/rules ↔ /library) — 이 수정이 겨냥한 경로:

|                | 수정 전 | 수정 후 |
|----------------|--------|--------|
| 수신량         | 18.38MB | **0.41MB** (45배 감소) |
| 힙 증가        | +40.5MB | **+3.3MB** (12배 감소) |
| /api/quests 호출 | 16회 | **0회** |

## 남은 몫 — 원인 2 로 이어짐

목록 페이지 왕복(/?view=list ↔ /rules)은 아직 16회에 9.14MB / +37.1MB,
`/api/quests` 8회(= 목록 진입마다 1회, 1.13MB). 이건 인덱스가 아니라
**목록/보드/홈 페이지가 자기 데이터로 퀘스트 전량을 매번 받는** 것이라
성격이 다르다. description 은 목록 렌더에 쓰이지 않으므로 경량 응답
(원인 2)으로 같이 해결된다. 잔여 보존 재측정은 그 뒤에 하는 게 맞다.

<!-- og-comment id="2" ts="2026-08-04T01:20:08+09:00" author="claude" -->
원인 2 수정 — 목록 응답에서 본문 제외 (`?slim=true`).

## 왜 인덱스 적재가 컸나 (admin 질문)

인덱스 **자체**는 작다 — 메모리에 남는 건 `Map<ID, {title, kind}>` 531개,
50KB 남짓. 큰 건 그걸 만들려고 받는 **응답**이었다:

- `/api/quests` 는 목록 전용 API 인데 `description` 을 전부 싣는다.
  531건 × 평균 1,095자 = 약 580KB 가 본문.
- 응답 JSON 문자열 827K자 → UTF-8 **1,156,414 바이트**. 한글이 3바이트라
  글자 수보다 바이트가 40% 더 크다.
- 정작 목록·보드·홈·태그·캠페인 상세·검색 팔레트·cross-link 인덱스 중
  **본문을 읽는 곳은 하나도 없다**(검색 팔레트는 미리보기를 열 때
  `questsApi.get(id)` 로 따로 받는다).

## 구현

- `ListQuery.slim` 추가. 쿼리스트링이 그대로 `ListQuery` 로 역직렬화되므로
  서버(`Query<ListQuery>`)와 Tauri(`list_quests` 커맨드) 양쪽이 동시에 지원됨
  — transport 분기 추가 불필요.
- `services::quests::list` 가 slim 이면 행의 `description` 을 None 으로.
  SELECT 에서 빼지 않은 이유: `search` 가 본문을 봐야 하고, SQL 분기를 늘리면
  필터 조합마다 경로가 갈라진다. 로컬 SQLite 읽기는 싸고 비용은 네트워크다.
- `QuestRow.description` 에 `skip_serializing_if` — slim 응답은 필드 자체가
  없다(널이 아니라 부재). TS `Quest.description?` 로 반영.
- 프런트 8개 호출부 전부 `list(true)` / `listRecent(true)`.
- CLI 는 본문을 출력·검색에 쓰므로 `slim: false`.

## 측정

- `/api/quests` 응답: **1,156,414 → 258,404 바이트** (-78%).
- 라우트 이동 16회(/?view=list ↔ /rules): 수신 9.14MB → **2.25MB**,
  힙 증가 +37.1MB → **+8.6MB**.
- 이동 100여 회 후 힙: 수정 전 260MB(계속 상승 중) → **약 100MB**.

## 남은 것

이동당 약 1MB 씩은 여전히 쌓인다(수정 전 2.5MB). GC 후 바닥값도 조금씩
오르므로 순수 보존 문제가 남아 있다 — 별도 조사 대상.

부수 발견: **서버가 압축을 전혀 하지 않는다**(`content-encoding` 없음,
tower-http 의 `compression` feature 미사용). 한글 JSON 은 gzip 이 잘 먹는
데이터라 별도 퀘스트로 등록한다.

<!-- og-comment id="3" ts="2026-08-09T20:54:02+09:00" author="claude" -->
데스크톱 앱 실측 (admin 이 직접 사용, 나는 프로세스 메모리만 관측):

| 프로세스 | 기준선(실행 직후) | 7분 사용 후 | 증가 |
|---|---|---|---|
| renderer | 95.6 MB | 236.4 MB | +141 MB |
| browser(main) | 134.6 MB | 163.1 MB | +28 MB |
| gpu-process | 81.4 MB | 128.9 MB | +47 MB |
| Rust 본체 | 42.1 MB | 52.5 MB | +10 MB |

renderer 의 Private 은 157 MB 로 WorkingSet(231 MB)보다 낮다 — 차이의 상당
부분은 공유 페이지/캐시다.

## 이 퀘스트의 범위 정리 (admin 결정)

**증명된 것 — 이 퀘스트가 고친 것:**
라우트 이동마다 cross-link 인덱스를 강제 재적재하던 문제. 이동 16회 기준
수신량 18.4 MB → 0.4 MB, `/api/quests` 호출 16회 → 0회, 응답 자체도
1,127 KB → 258 KB(slim), gzip 까지 합치면 34 KB.

**증명되지 않은 것 — 여기서 분리한다:**
데스크톱 renderer 메모리가 줄었는지는 **비교 기준이 없어 판정 불가**다.
원래 보고된 354 MB 는 얼마나 오래 쓴 뒤의 값인지 모르고, 이번 측정도
"수정 전 같은 조건" 이 없다. 7분에 +141 MB 가 붙는 것도 그냥 넘길 수치는
아니지만, 원인이 이 퀘스트가 고친 재적재와 다를 수 있다(캐시, 다른 코드 경로).

같은 퀘스트에 묶어두면 판단이 흐려지므로 **메모리 증가는 별도 퀘스트로
분리**했다. 이 퀘스트는 수신량 감소를 근거로 닫는다.
