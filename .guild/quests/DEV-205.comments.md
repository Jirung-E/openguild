<!-- og-comment id="1" ts="2026-07-03T22:35:06+09:00" author="claude" -->
[계획] 진행 계획 (2026-07-03) — 대형 스윕이라 모듈 단위 분할이 핵심

## 범위 확정
- **GUI 전역**: Svelte 컴포넌트의 한글 하드코딩 → `locale.ts` `t()` 사전 이전. (DEV-015 인프라 재사용)
- **core/cli/server 메시지**: 이번 스윕에서 **제외 권장** — CLI 에러/println 은 agent 파싱·문서(AGENTS_OPENGUILD_USAGE 예시)와 결합돼 있어 다국어화 비용>효과. 사용자 확인 후 확정.

## 모듈 분할 (화면 단위 — 각각 독립 검증 가능)
1. Nav/공통 컴포넌트(ConfirmDialog, 배지류) + Welcome
2. 홈(Home) + 캠페인 목록/상세
3. Quest List + Quest Board(툴바/컨텍스트 메뉴/모달)
4. Quest 상세(댓글/메모/첨부/이력 포함 — 문자열 최다)
5. 설정 + Admin + rules
→ 각 모듈 = 별도 sub-quest 로 등록해 진행 권장 (한 브랜치 대형 diff 방지).

## 작업 규칙 (모듈 공통)
- 사전 키 네이밍: `화면.요소` (기존 locale.ts 관례 따름 — 착수 시 확인).
- 문자열 추출 시 보간(`{n}개`) 은 t() 파라미터 치환 사용.
- REQ-001(편집창 언어 일관성)을 4번 모듈에 포함 처리.
- 검증: 각 모듈 후 한/영 토글로 해당 화면 전수 확인 + svelte-check.

## 사전 준비(선행 소작업)
- 하드코딩 문자열 자동 탐지: 컴포넌트 내 한글 리터럴 grep 스크립트(check:no-hex 처럼 scripts/ 에 추가, CI 게이트는 스윕 완료 후) — 진행률 측정/누락 방지 도구.

예상 규모: 대형(모듈당 중형×5). 착수 결정 시 sub-quest 분리부터.

<!-- og-comment id="2" ts="2026-07-12T13:26:10+09:00" author="claude" -->
[범위 확정] 착수 결정 (2026-07-12)

사용자 확인으로 계획 코멘트의 미확정 항목 2건 확정:

1. **진행 방식**: DEV-205 한 퀘스트로 순차 진행 (모듈별 sub-quest 분리 안 함).
   5개 모듈을 순서대로 처리하고 모듈마다 커밋.
   - 모듈1: Nav/공통 컴포넌트(ConfirmDialog·배지류) + Welcome
   - 모듈2: 홈(Home) + 캠페인 목록/상세
   - 모듈3: Quest List + Quest Board
   - 모듈4: Quest 상세 (댓글/메모/첨부/이력) — REQ-001 포함 완료분 제외 나머지
   - 모듈5: 설정 + Admin + rules
2. **core/cli/server 포함**: GUI 외에 CLI 에러/println, 서버 응답 메시지도 i18n 대상.
   - CLI: `--locale`/env 명시 선택 옵션 설계 (localStorage 없음)
   - server: 응답 메시지 언어를 요청 헤더/쿼리로 수신 설계

선행 완료: REQ-001 (퀘스트/캠페인 상세 편집창 라벨 통일) — 사전 키
detail.edit/delete/back, quest.section.* 추가. 모듈4 편집창 라벨 부분 선반영됨.
