<!-- og-comment id="1" ts="2026-07-11T11:10:10+09:00" author="claude" -->
수정 완료. current_guild_name 이 LaunchInfo state 를 받아 mode != "guild" 면 빈 문자열 반환 — Nav 는 빈 이름이면 배지를 숨기므로({#if guildName}) Welcome/Uninit 에서 이름 자체가 안 보임. 기존엔 "frontend 가 launch_mode 확인하고 쓰라"는 주석 계약뿐이라 Nav 가 어겨도 막을 게 없었음 → 백엔드 강제로 전환.

<!-- og-comment id="2" ts="2026-07-11T13:53:57+09:00" author="admin" -->
다른 길드에 들어갔다가 다시 웰컴 페이지로 돌아온 후 설정창에 들어가면 이전에 들어갔던 길드 이름이 표시되고있음.  
이런 경우에도 동일하게 아무런 이름도 표시되지 않아야함

<!-- og-comment id="3" ts="2026-07-11T16:29:23+09:00" author="claude" -->
후속 수정(#2 보고: 다른 길드 방문 후 Welcome 복귀 시 이전 길드 이름 잔존): Rust LaunchInfo.mode 는 한 번 guild 가 되면 Welcome 복귀(순수 frontend 라우팅)로 안 풀리는 stale 상태 — DEV-207 이 설정 페이지에서 이미 겪은 패턴. Nav 는 onMount 1회 조회라 라우팅 변화를 못 따라갔음.

수정: DEV-207 의 guildContextActive 세션 플래그를 반응형 스토어로 확장(mark* 가 sessionStorage 와 스토어 동시 갱신 — 마운트 순서 무관), Nav 가 이를 구독해 활성 전환마다 이름 재조회 / 비활성(Welcome 마운트) 시 즉시 숨김. 부수 효과로 A 길드 → B 길드 전환 시 이름이 낡는 문제도 함께 해소(전환마다 재조회).
