<!-- og-comment id="1" ts="2026-07-06T22:11:54+09:00" author="claude" -->
## 구현 확인 (2026-07-06)
- global.css: `button, input, select, textarea { font: inherit }` —
  '본문 추가'(campaign .link) 포함, font-size 명시 없는 컨트롤 전부 해결.
- WorklogSummaryCard: 히트맵 셀 11px→0.6875rem, 간격 3px→0.1875rem.
- QuestNodeConveyor: uiScale 구독 — effCardW/effCardH/effGap 을 이미지
  width/height + slot/spacer flex-basis(inline) + marquee 폭/속도 계산에
  일관 적용 (SVG 는 벡터라 확대 손실 없음).
- CampaignConveyor: 동일 패턴 (effCardW/effGap). LOOP_SPACER_PX 상수
  제거(spacer 도 effCardW).
- 잔여 px 는 min/max 경계(모달 min-width, 콤보박스 max-height 등) —
  내용이 rem 이라 스케일 시 자연 확장되므로 이번 범위 제외.

<!-- og-comment id="2" ts="2026-07-06T22:53:06+09:00" author="claude" -->
후속(admin 피드백): 홈 퀘스트 노드가 캠페인 카드(200px) 대비 과하게 큼
(보드 노드 폭 284px 그대로였음).

- makeQuestNodeSvgUrl 에 layout 폭 파라미터 추가 — 글자 크기는 유지한 채
  제목 truncation(MAX_PX = W-24)/배지 위치만 지정 폭에 맞춰 재배치
  (전체 축소 스케일이 아니라 글자가 작아지지 않음).
- QuestNodeConveyor CARD_W 284 → 200 (CampaignConveyor 와 동일 폭).
  보드 노드는 기본값(284) 그대로 — 영향 없음.
