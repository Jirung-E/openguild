+++
quest_id = "DEV-016"
title = "길드 규칙 (Guild Rules) UI"
status = "testing"
urgency = 3
prerequisites = []
created_at = "2026-05-15T16:13:10Z"
updated_at = "2026-05-31T21:34:47+09:00"
deleted = false
+++

팀 컨벤션 / 그라운드 룰 길드별 문서화. docs/planning.md 의 '길드 규칙' 메모 참조

## 구현 (2026-05-31)

### 설계
- 파일이 진리원 — `.guild/rules.md` (frontmatter 없는 plain Markdown). 자유
  문서. 부재 = "아직 미설정".
- DB 캐시 불필요 (단일 파일, search 등 불필요).
- git tracked.

### Backend (core / server / Tauri)
- `core::repo::rules::{read, write}` — Option<String> / atomic write.
- `core::repo::GuildPaths::rules_path()` — `.guild/rules.md`.
- `core::ops::rules::{get_rules, set_rules}` — set 시 journal append.
- `server/routes/rules.rs` — `GET/PUT /api/rules` (`{ content: string|null }`).
- `gui/src/commands.rs::{get_rules, set_rules}` — Tauri 핸들러. HTTP 와
  동일 `RulesResponse { content }` shape (transport 통일).

### Frontend
- `lib/api/rules.ts::rulesApi.{get,set}`.
- `lib/api/transport.ts` — `/api/rules` GET → `get_rules`, PUT → `set_rules`.
- `routes/rules/+page.svelte` — 신규.
  - 파일 없으면 "아직 작성된 규칙이 없습니다. [지금 작성]" 안내.
  - 읽기 모드: `MarkdownView` (Quest/Campaign 본문과 동일 톤).
  - 편집 모드: CodeMirror (markdown + oneDark) — Quest Detail 동일 패턴.
  - 사용자 height 영속화는 `openguild.questEditorHeight` 키 공유.
- `Nav.svelte` — Admin 다음에 "Rules" 링크.

### 검증
- `cargo test --lib -p openguild-core repo::rules` → 3/3 통과 (missing /
  roundtrip / empty allowed).
- `cargo build` (workspace 전체) → OK.
- `npm run check` → 0 errors. `npm test` → 131. `npm run build` → OK.
- end-to-end (사용자 확인 필요): `/rules` 진입 → "[지금 작성]" → 본문 입력 →
  저장 → reload 시 markdown 렌더. Tauri 는 rebuild 후 동일 흐름.

### 후속 (이 quest 범위 외)
- CLI `openguild rules show / edit` 명령 (현재 GUI-only).
- 다중 규칙 파일 지원 (e.g. `.guild/rules/{topic}.md`).
- 길드 onboarding 시 기본 규칙 템플릿 제공.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
