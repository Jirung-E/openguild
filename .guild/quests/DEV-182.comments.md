<!-- og-comment id="1" ts="2026-07-03T22:40:03+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03)

## (1) 댓글 edited_at — 소형
- `og-comment` HTML marker 에 `edited_at="ISO"` 속성 추가(옵션) — parser 하위호환(없으면 미편집).
- core comments 파서/라이터 + update_comment 시 기록. DB 캐시 컬럼 추가(migration — 빈 컬럼만, BUG-102 교훈).
- UI: ts 옆 "(편집됨)" + title 툴팁에 편집 시각. CLI `comment list/show` 에도 표시.
- 외부편집 귀속 불가(파일 mtime 은 파일 단위) — 본문 확정대로 생성 ts 유지, edited_at 은 정식 경로(CLI/GUI/HTTP) 편집만 기록.

## (2) 규칙 created_at/updated_at — 설계 결정 필요
**(A) frontmatter 도입 추천**: quest 패턴과 일관 + BUG-080 식 mtime write-back 재사용. 단 —
- 기존 규칙 파일은 frontmatter 없음 → 파서는 "없으면 None" 허용(하위호환), 첫 저장 시 삽입. created_at 소급은 git log 1회 조회로 백필(선택) 또는 미상 처리.
- ⚠️ 규칙 본문 맨 위 `# 제목` 과 frontmatter 공존 — DEV-173 이 규칙 제목을 첫 # 헤딩에서 뽑으므로 frontmatter 파싱이 그걸 깨지 않게(파서가 +++ 블록 제거 후 본문 전달).
(B) mtime/git 산출은 restore/checkout 에 취약(BUG-103 과 같은 함정) — 기각 권장.
- rules list/상세 UI + `rules list` CLI 출력에 updated_at 컬럼.

예상 규모: (1) 소형 / (2) 중형. (2)는 rules 파일 포맷 변경이라 BUG-103 수정(내용 비교 게이트) 이후 진행 권장 — 같은 write-back 메커니즘 공유.
