<!-- og-comment id="1" ts="2026-07-03T22:24:58+09:00" author="claude" -->
[계획] 구현 계획 (2026-07-03 작성) — 착수 전 방향 결정 1개 필요

현황 재확인: `--json` 은 이미 pretty-print(2-space). 따라서 "보기 좋게" 요청의 실체는 (a)가 아니라면 재확인 필요.

**추천안**: `--json` 은 pretty 유지(현행, 사람+agent 겸용) + **`--compact` 플래그 추가**(한 줄 JSON — 파이프/jq/로그 수집용). 반대 방향(json 을 compact 기본화)은 기존 스크립트/에이전트 호환성 깨질 수 있어 비추천.

구현:
1. `cli/src/main.rs` 전역 옵션에 `--compact`(requires = "json") 추가.
2. 출력 헬퍼 단일화: 현재 각 arm 이 `serde_json::to_string_pretty` / `json!` 매크로 직접 호출 — `fn print_json<T: Serialize>(v: &T, compact: bool)` 하나로 수렴(= DEV-209 의 출력 일관성 조사와 겹침 — **DEV-209 조사 후 같이 하면 중복 작업 방지**).
3. 문서: AGENTS_OPENGUILD_USAGE §6 갱신.

착수 시 사용자에게 "compact 플래그 방향 맞는지" 한 번만 확인하고 진행 (본문 취지가 pretty 요청이었다면 이미 충족 상태라 close 가능성도 있음).

<!-- og-comment id="2" ts="2026-07-10T17:14:29+09:00" author="claude" -->
구현 완료. 착수 확인 결과 admin 답변 "전부" — 추천안(--compact)과 사람용 렌더(--table) 둘 다 진행:

- 전역 `--compact`(--json 필수, clap requires): 한 줄 JSON — 파이프/jq/JSONL 수집용. `--json` 기본은 pretty 유지(기존 스크립트/에이전트 호환).
- 출력 헬퍼 단일화: 30곳의 serde_json::to_string_pretty 직접 호출을 json_str() 하나로 수렴(계획 2번 항목) — 전 명령 공통 적용.
- `quest list --table`: 헤더+컬럼 정렬 사람용 표(색상은 line 렌더와 동일 규칙). 한글 제목은 가변폭이라 마지막 컬럼에 배치해 패딩 문제 회피. --tree/--id-only/--count 와 clap 상호배타, --json 과는 수동 검증(전역 인자는 clap conflicts 대상 불가 — debug assert 가 subcommand 스코프에서 못 찾음).
- 문서: AGENTS_OPENGUILD_USAGE §1.3/§2.2/§6 갱신. CLI 테스트 3건 추가.

참고: debug 빌드 CLI 가 `quest list` 만으로도 stack overflow 나는 기존 문제 발견(release 는 정상, 본 퀘스트와 무관) — 별도 확인 필요.
