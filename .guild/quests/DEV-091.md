+++
quest_id = "DEV-091"
title = "[docs] AGENTS_OPENGUILD_USAGE 갱신 — campaign / due / history / search CLI"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-05-31T14:21:14+09:00"
updated_at = "2026-06-23T01:30:44+09:00"
deleted = false
+++

audit follow-up. AGENTS_OPENGUILD_USAGE.md 에 누락된 CLI: campaign (DEV-011, 9 subcommands), quest due (DEV-076), quest history (DEV-013), quest search (DEV-037), quest move (DEV-044 canonical). 0 'campaign'/'due' keywords → agent 가 가이드 보고도 모름.

## 구현 (2026-05-31)

### 2.2 Quest CRUD 보강
- `list` 의 filter / sort / search / pagination / id-only / count 플래그
  실제 시그니처 명시 (이전엔 `[--json]` 만 적혀 있어 agent 가 활용 불가).
- `search` (DEV-037) 명령 추가 — `list --search` 단축.
- `show <slug> --field <NAME>` 옵션 명시.

### 2.3 상태 변경 명확화
- `move` 가 ⭐ 정식 명령. `status <slug> <STATUS>` 형식은 ⚠ deprecated 임을
  명시 (이전 문서는 status 변경법을 status 인자로만 안내).

### 2.5 기한 (DEV-076) 신설
- `quest due <slug>` 조회 + `--desired` / `--required` 설정 +
  `--clear-desired` / `--clear-required` 해제.
- 형식 (YYYY-MM-DD) + 상호배타 + 빈 문자 정규화 규칙.
- required_due 가 Home 임박 / Overdue 기준임을 명시.

### 2.6 변경 이력 (DEV-013) 신설
- `quest history <slug>` — status / type 변경 audit.

### 2.7 Campaign (DEV-011) 신설
- `campaign list / show / new / delete / start / end / link / unlink /
  checklist {add,check,uncheck,rm}` 전체 시그니처.
- 진행률 = checked/total → Home carousel progress bar / 100% 강조 안내.

### 2.8 백업 / 복원 (이전 2.5)
- 리넘버 only — 본문 동일.

### 검증
- 변경: +77 / -6 (1 file).
- `campaign` 키워드 0 → 12, `due` 0 → 7.
- 코드 변경 없음.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
