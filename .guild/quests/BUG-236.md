+++
quest_id = "BUG-236"
title = "recent guild 목록에 openguild-welcome-placeholder tmp 경로가 등록됨"
status = "done"
urgency = 3
prerequisites = []
created_at = "2026-08-17T14:28:55+09:00"
updated_at = "2026-08-22T19:53:01+09:00"
deleted = false
+++

## 증상

GUI 의 "최근 guild(recent guild)" 목록에 `%TMPDIR%/openguild-welcome-placeholder`
같은 임시 경로가 정식 항목으로 계속 등록됨.

## 원인

`openguild-welcome-placeholder` 는 guild 를 안 연 Welcome/Uninit 상태에서
쓰는 in-memory DB 용 고정 tmp 경로(`gui/src/lib.rs`). BUG-041 당시 "in-memory
라 디스크에 아무 흔적도 안 남긴다"는 의도로 설계됐는데, `core/src/store.rs`
의 `Store::open_in_memory` 가 여전히 `.guild/`(+`backups/`) 마커 디렉터리를
실제 디스크에 만들고 있었다.

여기에 최근 추가된 "뒤로가기로 guild 히스토리 복원"(DEV-355) 기능이 겹치며:
1. Welcome 상태에서 페이지를 돌아다니면 브라우저 히스토리에 이 placeholder
   경로가 찍히고,
2. 나중에 실제 guild 를 연 뒤 뒤로가기(트랙패드 스와이프 포함)로 그 히스토리
   지점에 도달하면 `open_guild_in_current_window` 가 이 경로로 호출되는데,
3. `.guild/` 마커가 디스크에 없어야 정상 거부(`has_guild_marker`)되어야
   하는데 1번 버그 때문에 마커가 실제로 존재해 체크를 통과해버리고,
4. 진짜 디스크 DB 가 만들어지며 `recents.json` 에 정식 등록됨.

## 수정

`Store::open_in_memory` 에서 `.guild/`(+`backups/`) 디렉터리 생성 코드를
제거 — index/journal 모두 sqlite in-memory(`mode=memory&cache=shared`)라
디렉터리 자체가 애초에 불필요. 이제 이 경로엔 디스크 흔적이 전혀 안 남아
`has_guild_marker` 체크가 정상 작동하고, 히스토리 복원 경로를 타도 recents
에 등록될 수 없다.

## 참고 (이번엔 손 안 댐)

`current_guild_path` Tauri 커맨드(`gui/src/commands.rs`)가 launch mode 를
체크 안 하고 항상 placeholder 경로를 반환하는 지점도 있음 — `current_guild_name`
처럼(BUG-136) launch mode 가드를 추가하는 방어책도 가능하나, 프론트엔드
호출부 5곳(guildIdentity.ts / banner.ts / guild-storage.ts / QuestBoard.svelte
/ settings +page.svelte)의 동작을 다 검토해야 해서 별도 후속 과제로 남김.

## 테스트 방법

`cargo test -p openguild-core --lib store::` 통과 확인(자동). 다만 실제
회귀 경로(Welcome 에서 페이지 이동 → 뒤로가기로 stale 히스토리 진입 →
recents 오염 여부)는 GUI 조작이 필요해 자동화 범위 밖 — 수동 확인 필요:
1. GUI 를 guild 없이 시작(Welcome).
2. 설정 등 다른 화면으로 몇 번 이동.
3. 실제 guild 를 하나 연다.
4. 트랙패드 뒤로가기 스와이프(또는 뒤로가기)로 여러 번 되돌아가 Welcome
   시점 히스토리까지 진입.
5. recent guild 목록에 `openguild-welcome-placeholder` 항목이 새로 안
   생기는지 확인.

---

## 2차 수정 (2026-08-21) — 1차로는 안 끝났다

사용자가 **최신 빌드에서도 계속 보인다**고 보고. 확인해 보니 1차 수정은
동작하지만 문제를 끝내지 못했다.

### 진단

| 확인 | 결과 |
|------|------|
| placeholder 의 `.guild/` 디렉터리 mtime | **8/21 09:10 그대로** → 새로 안 만듦(1차 수정 정상) |
| `recents.json` 의 placeholder 항목 | **당일 19:21** → **계속 재등록됨** |

즉 1차 수정은 "앞으로 마커를 만들지 않게" 만 했는데, **수정 이전 빌드가 이미
만들어 둔 마커가 디스크에 남아 있었다.** 그래서:

1. 히스토리 복원(DEV-355)으로 placeholder 경로 재방문 →
2. `open_guild_in_current_window` 의 `has_guild_marker` 검사가 **잔재 덕분에
   통과** →
3. `recents::add` 로 다시 등록.

마커를 아무도 지우지 않으므로 **사용자 입장에선 영원히 고쳐지지 않는다.**

### 수정

1. **경로 자체로 거부** — `is_welcome_placeholder(p)` 를 마커 검사 **앞에** 둔다.
   잔재가 있든 없든 안전하다. 마커 유무에 기대던 것이 1차 수정의 약점이었다.
2. **시동 시 잔재 정리** — `cleanup_welcome_placeholder_leftovers()`:
   - `recents.json` 에서 placeholder 항목 제거
   - 예전 빌드가 남긴 `.guild/` 마커 디렉터리 제거
   실패해도 조용히 넘어간다 — 정리 때문에 앱이 못 뜨면 훨씬 나쁘다.
3. **경로 정의를 한 곳으로** — `welcome_placeholder_path()`. 문자열이 흩어지면
   한쪽만 고치는 사고가 난다. 판별은 `canonicalize` 후 비교해 mac 의
   `/var` ↔ `/private/var` 표기 차이를 흡수한다.

### 검증

`cargo test -p openguild-gui` **19 passed** (신규 3건):
placeholder 판별 / 진짜 길드는 오인 안 함 / 잔재 마커 실제 삭제.

실제 환경에서도 확인:

| | 수정 전 | 수정 후 |
|---|---|---|
| `.guild` 마커 잔재 | 존재 (8/21 09:10) | **없음** |
| recents 의 placeholder 항목 | 1건 (당일 19:21) | **0건** |

## 남은 것 (이번에도 손 안 댐)

`current_guild_path` 는 여전히 launch mode 를 확인하지 않고 placeholder 경로를
그대로 반환한다(`current_guild_name` 은 BUG-136 에서 가드가 붙었다). 프론트
호출부 5곳의 동작을 검토해야 해서 별도 과제로 남긴다. **이번 증상(recents 오염)
과는 무관하다** — 그쪽은 위 2단계로 막힌다.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)

## Successors
- (없음)
<!-- openguild:auto-end -->
