<!-- og-comment id="1" ts="2026-07-10T11:02:33+09:00" author="admin" -->
기획 변경: 기본적인 표시는 현재와 같게 하되, 마우스 호버시 상태별 표시로 바뀌도록. 마우스가 바를 벗어나면 다시 원래대로 돌아옴. 추가로 마우스 호버일때 팝업으로 각 상태 갯수 표시

<!-- og-comment id="2" ts="2026-07-10T12:13:57+09:00" author="claude" -->
구현 완료. 댓글 #1(기획 변경) 반영: 기본은 기존 단일 채움 그대로, hover 시에만 상태별 stacked + 카운트 팝업으로 전환, mouseleave 시 원복.

- server: CampaignSummary/CampaignDetail 에 quest_status_counts(상태별 카운트, sort_order 순) 추가 — summarize()/fetch_detail 공유 헬퍼.
- CampaignCard.svelte: hover 시 progress-bar 를 상태별 세그먼트로 교체 + position:fixed 팝업(카드가 overflow:hidden 이라 BUG-125 emoji 팝업과 동일하게 fixed+좌표계산으로 클리핑 회피).
- 캠페인 상세 페이지 진행바도 동일 hover 동작 적용.
- 완료 캠페인(quest_done===quest_total)은 hover 해도 기존 초록 단색 유지(의미 없는 정보라 skip) — 댓글의 "완료 시 단색 유지 고려" 반영.
- 체크리스트 진행바는 변경 안 함(제안대로).

<!-- og-comment id="3" ts="2026-07-10T22:03:41+09:00" author="claude" -->
후속 수정(admin 보고: 홈에서 팝업 잘림): 캠페인 상세에서는 정상인데 홈에서만 잘리던 원인 — 홈 캐러셀(.track 의 translateX)/컨베이어(will-change: transform)가 조상에 transform 을 두면서 position:fixed 의 containing block 이 뷰포트가 아닌 그 조상으로 바뀜(CSS 명세) → 좌표 틀어짐 + overflow:hidden 클리핑. 상세 페이지는 transform 조상이 없어서 멀쩡했던 것.

수정: 공용 `use:portal` 액션(utils/portal.ts) 신설 — 툴팁을 document.body 직속으로 이동시켜 getBoundingClientRect 뷰포트 좌표가 그대로 유효해짐. Svelte scoped CSS 는 요소에 hash class 가 붙어 있어 reparent 후에도 적용됨.
