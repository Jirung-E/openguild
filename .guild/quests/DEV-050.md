+++
quest_id = "DEV-050"
title = "frontend Quest Detail 페이지에 parent 카드 표시"
status = "done"
urgency = 4
prerequisites = []
created_at = "2026-05-22T22:26:34+09:00"
updated_at = "2026-05-23T12:39:56+09:00"
deleted = false
+++

DEV-047 에서 백엔드 `QuestDetail.parent: Option<QuestRow>` 와 frontend
`Quest.parent` 타입은 노출했지만 frontend 화면엔 미반영. sub-quests /
prerequisites 와 동일 카드 형태로 표시.

## 구현 (2026-05-23)

### Quest Detail 페이지
- `<section>` 추가 (description 이후, sub-quests 앞에 배치):
  - 라벨 "Parent" (sub/prereq 와 동일 톤).
  - `<a href="/quests/{slug}">` 로 부모 quest 페이지 링크.
  - badge: type / status — 기존 sub/prereq 카드와 동일 markup.
- `{#if detail.parent}` — root quest 면 섹션 자체 생략.

### 섹션 라벨 색
- `Parent` → `#7ee787` (초록).
- `Sub-Quests` → `#3dc9b0` (청록).
- `Prerequisites` → `#a371f7` (보라).
- QuestBoard 의 다중-선택 하이라이트 / CLI `quest show` 의 섹션 라벨과
  일치하는 팔레트.

## 회귀

- root quest (parent 없음) → 섹션 자체 미렌더.
- backend / API 변경 없음 (DEV-047 의 parent 필드 그대로 사용).

## 검증

- `npm test -- --run` → 107 passed.
- `npm run check` → 0 error / 0 warning.
- `npm run build` → success.
- 라이브: DEV-008 같이 parent (DEV-007) 가 있는 quest 페이지 열면 Parent
  카드 표시.

## 후속 (별도 quest 후보)

- parent 변경 UI — 현재 frontend 에서 parent 분리 (× 버튼) 만 가능. 다른
  parent 로 지정하는 UI 가 sub-quests 의 "+ 기존 지정" 같은 형태로 필요.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
