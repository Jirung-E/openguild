<!-- og-comment id="1" ts="2026-06-13T19:47:55+09:00" author="claude" -->
## 테스트 스텝 (사용자)
1. 앱 시작(특히 첫 부트스트랩 또는 대량 외부 변경 후) 시 reindex 진행 표시가 뜨는지.
2. 완료 후 정상 화면 진입 + 데이터 최신 반영.

<!-- og-comment id="2" ts="2026-06-13T20:00:28+09:00" author="admin" discussion="true" resolved="true" -->
안되고있음(앱을 켜자마자 admin 페이지에서 체크해보면 reindex 안된것들이 보인다.)

<!-- og-comment id="3" ts="2026-06-13T20:02:05+09:00" author="admin" reply_to="2" -->
BUG-049에서 안하기로 결정한건가?

<!-- og-comment id="4" ts="2026-06-14T15:00:17+09:00" author="claude" reply_to="2" -->
"안 하기로 결정"한 건 아니고, 구현은 돼 있습니다. gui/src/lib.rs 가 시동 시 incremental::sync_on_open 을 호출(BUG-049 자동 동기화 + DEV-121 Phase 1 incremental). 그런데 두 가지 때문에 admin 에서 "안 된 것처럼" 보입니다:

1. drift 표시기 오탐: incremental sync 는 quest 데이터(cached_mtime)는 갱신하지만 app_meta.last_indexed_at 을 안 올림. admin drift 체크는 last_indexed_at 기준이라, 실제로 quest 가 갱신됐어도 "drift 있음"으로 표시됨 → 데이터는 최신, 표시만 stale.
2. 범위가 quest 파일만: 외부에서 status/type/campaign/댓글 '내용'을 수정(신규·삭제 아님)하면 incremental 이 안 잡아 ⟲ 필요. (신규/삭제 quest 는 full reindex fallback 으로 잡힘.)

DEV-121 제목의 "splash/진행 표시"는 의도적으로 폐기(Phase 1 으로 시동 <50ms 라 불필요 — 본문 기록).

→ 정리 방향 3안 중 선택 필요(채팅으로 질문 드림): A) 시동 시 drift 있으면 full reindex(항상 정확, 표시기 clean, 대형 길드만 약간 느림 — BUG-049 원안) / B) incremental 을 모든 파일 타입으로 확장 + last_indexed_at 갱신(빠르고 정확, 작업 큼 = Phase 1b) / C) incremental 유지 + drift 표시기를 cached_mtime 기준으로(오탐만 제거, 비-quest 수정은 여전히 ⟲).

<!-- og-comment id="5" ts="2026-06-14T15:11:24+09:00" author="claude" reply_to="3" -->
확인 결과 + 진짜 원인 발견했습니다.

1) "시동엔 mtime만 보고 내용은 상세에서만 읽기"(pure-lazy)는 기록상 '결정'된 게 아닙니다. 실제 구현된 설계는 Phase 1(시동 시 mtime 비교 → 변경된 quest 파일만 re-parse) + Phase 2(DEV-137: 상세 진입 시 그 파일 lazy refresh)이고 둘 다 구현돼 있습니다.

2) 진짜 원인: sync_on_open 이 앱 부팅의 '초기 길드'(lib.rs)에서만 호출됐고, Welcome 화면에서 길드를 여는 경로에서는 호출이 빠져 있었습니다. 그래서 런치 → Welcome → 길드 열기(가장 흔한 흐름)에선 시동 sync 가 아예 안 돌아 admin drift 가 남았던 겁니다. (배너/첨부 asset scope 누락과 같은 swap 누락 패턴.) → 고침(fc377b6): 두 swap 함수에서 Store::open 직후 sync_on_open 호출.

3) pure-lazy 로 바꾸는 건 권장하지 않습니다 — 시동에서 변경 파일을 안 읽으면 외부 편집한 quest 의 제목/상태가 '상세에 들어가기 전까지' 리스트/보드에서 stale 로 남습니다. 이건 DEV-095에서 원하셨던 "외부 편집이 리스트에 반영" 과 정반대라 오히려 퇴보입니다. 지금 설계(시동 re-parse + 상세 lazy)가 그 요구를 충족합니다.

남은 한계(정직히): campaign/status/type/댓글 '내용' 외부수정은 Phase 1 범위 밖이라 여전히 ⟲ 필요(신규/삭제 quest 는 full reindex fallback 으로 잡힘). 이건 별도 Phase 1b 후보.

→ 재빌드 후: Welcome 으로 길드 열고 quest 파일 외부 편집 → admin drift 가 clean 인지 + 리스트에 반영되는지 확인 부탁드립니다.

<!-- og-comment id="6" ts="2026-06-14T18:45:13+09:00" author="claude" -->
[정정] 앞 댓글에서 "pure-lazy는 결정 안 됨"이라 표현한 건 제 오해/과한 해석이었습니다. 사과드립니다.

정한 내용("시동엔 파일 수정시간만 확인 → 바뀐 것만, 파일 내용은 상세 진입 때 읽기")은 이미 퀘스트화 + 구현돼 있습니다:
- 시동 mtime 체크 → 바뀐 파일만 re-parse = DEV-121 Phase 1 (incremental::sync_changed_quest_files). 안 바뀐 파일은 내용 안 읽음.
- 상세 진입 때만 그 파일 읽기 = DEV-137 (refresh_quest_if_stale, get_quest_by_slug 에서 호출). ← 이게 그 퀘스트.

admin drift 가 남았던 건 설계 미구현이 아니라, Welcome 으로 길드 열 때 sync_on_open 호출이 빠진 swap 누락 버그였고 fc377b6 에서 고침.

<!-- og-comment id="7" ts="2026-06-14T18:58:06+09:00" author="claude" -->
## 테스트 스텝 (갱신 — 댓글 #1 대체)

주의: "시동 reindex 진행 표시(splash)"는 의도적으로 폐기됨(시동 <50ms 라 불필요). 진행 표시는 **안 뜨는 게 정상** — splash 를 찾으려 하지 말 것.

실제 검증(Welcome-swap 수정 fc377b6 반영):
1. 앱 실행 → Welcome 화면에서 길드 열기.
2. 그 전/후에 외부 에디터나 CLI 로 quest 파일 하나 수정(제목 또는 status 변경).
3. 길드 진입 직후, ⟲ 안 눌러도 그 변경이 리스트/상세에 반영돼 있는지.
4. admin 페이지 drift 체크가 clean 인지(수정한 quest 가 drift 로 안 뜸).
5. (한계 — 정상 동작) campaign/status/type/댓글 '내용'만 외부수정한 경우는 시동 sync 범위 밖 → 여전히 ⟲ 필요.

<!-- og-comment id="8" ts="2026-06-14T19:43:01+09:00" author="admin" reply_to="7" -->
4번은 안되는거같다. (파일을 보면 제대로 보이는데, drift 체크에는 걸림)  
DEV-137로 인해 이게 정상일 가능성이 있는지?

<!-- og-comment id="9" ts="2026-06-14T19:54:49+09:00" author="claude" reply_to="8" -->
정확히 보셨습니다 — 데이터는 맞는데(파일/리스트/상세 정상) drift '체크'만 걸리던 건 표시기 오탐이었습니다. "DEV-137로 인해 정상"이라기보단, DEV-137(및 ops/시동 sync)이 데이터+cached_mtime 은 갱신해도 global last_indexed_at 을 안 올려서, 이미 최신인 quest 가 drift 로 잘못 잡힌 버그였습니다.

고쳤습니다 → BUG-067 (bb6fc61):
- detect_drift 가 이제 global last_indexed_at 대신 그 quest 행의 cached_mtime 과 비교(파일 mtime > cached_mtime 일 때만 fresh).
- ops 의 write_quest_file 이 파일 쓸 때마다 cached_mtime 을 동기화(모든 mutation 경유).
→ 우리 write 경로 밖에서 파일이 실제로 더 새로 바뀐 경우(진짜 외부 편집)만 drift 로 잡히고, 동기화된 quest 는 clean.

테스트 스텝 #4 갱신: 재빌드 후 다시 보면, 외부 편집한 quest 를 상세 진입(lazy refresh)하거나 시동 sync 후엔 admin drift 가 clean 으로 떨어져야 정상입니다. (sibling 댓글/메모는 아직 per-row mtime 이 없어 last_indexed_at 기준 유지 — 이건 별도.)
