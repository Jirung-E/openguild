+++
quest_id = "BUG-053"
title = "[core] reindex 가 file→DB sync 시 status 되돌림 + quest_history 안 적힘 — silent revert"
status = "open"
urgency = 2
prerequisites = []
created_at = "2026-06-06T00:28:30+09:00"
updated_at = "2026-06-06T00:28:30+09:00"
deleted = false
+++

사용자: 'GUI 에서 상태 바꾼 quest 들이 되돌아오는 경우가 있다, 기준 모르겠음'.

## 재현 확인
BUG-046 의 quest_history 에 testing→done 이 두 번 기록. 두 기록 사이에 done→testing 의 reverse 가 history 에 없음. 즉 DB 가 reverse 됐는데 그게 change_status mutation 이 아님.

## 원인
- 사용자가 GUI 에서 변경 → DB + file 갱신, working tree 에 변경분 stage 안 됨.
- 누군가 (또는 git restore / branch checkout) file 을 옛 commit 의 status 로 되돌림.
- 다음 시동시 drift::auto_resync (BUG-049) 가 file mtime 새것 감지 → reindex → DB 가 file 의 옛 status 로 sync.
- reindex 는 quest_history 안 적음 → 사용자는 무슨 일인지 모름.
- GUI 에서 본 변경이 사라짐.

## 영향
- AGENTS.md 의 '직접 편집 금지' 정책으로 막혀야 하는데 GUI 변경 후 git restore 등 정상적 git 작업이 같은 효과.
- BUG-051 (BUG-040 file/DB drift) 와 같은 family.

## 해결 후보
1. reindex 가 status 변경 시 quest_history 에 op='reindex_status_sync' 같은 entry 추가 — at least 흔적.
2. drift::auto_resync 호출 시 fresh_files 의 status 변경을 stderr / GUI banner 로 명시 알림 (현재는 tracing::info! 만).
3. write_quest_file 시 frontmatter 와 DB 의 status 가 다르면 경고 + 명시적 우선순위 선택 prompt.
4. change_status 의 BUG-011 early return — file 도 검사해서 file status != DB status 면 file 갱신 강제.
5. snapshot 의 시점이 사용자 변경 직전 + 직후 둘 다 — 'pre-revert' snapshot 자동.

## 우선 검증
- BUG-040 의 history 도 같은 양상인지.
- 이번 라운드 BUG-046 / DEV-068 / DEV-070 / DEV-101 working tree 변경분이 사용자 GUI 변경 결과인지 사용자 확인.

## 관련
- BUG-049 (시동시 자동 reindex) — 본 issue 의 trigger.
- BUG-051 (BUG-040 file/DB drift 결정 대기) — 같은 family.
- DEV-095 (auto drift 감지 + reindex) — 본 기능의 기반.

<!-- openguild:auto-begin — 아래는 자동 생성. 직접 수정하지 마세요. -->
## Sub-quests
- (없음)

## Prerequisites
- (없음)
<!-- openguild:auto-end -->
