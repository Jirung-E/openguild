<!-- og-comment id="1" ts="2026-07-11T21:30:36+09:00" author="claude" -->
구현 완료:

1. `render_table()` 공용 헬퍼 — 헤더/컬럼 폭/우측정렬/행수 footer. 원칙: 한글 등 가변폭 텍스트는 마지막 컬럼(패딩 없음), 색은 패딩 후 colorize(ANSI 가 폭 계산에 안 섞이게). quest list 의 기존 --table 도 이 헬퍼로 리팩토링.
2. --table 확장: campaign list / type list / status list / library list / quest deleted (+기존 quest list). comments 횡단은 본문이 멀티라인이라 표에 부적합 — 제외(사유 기록).
3. 일관성 장치 3종: ① 공용 헬퍼(render_table/json_str) ② guild rule `cli-list-command-options` 신설(옵션셋 + 구현 규약 + 강제 장치 명문화) ③ `all_list_commands_accept_table_flag` 테스트 — 알려진 목록형 명령 전수의 --table 파싱을 검사, 새 목록 명령 추가 시 이 목록에 넣는 것이 규칙.
4. AGENTS_OPENGUILD_USAGE 에 공통 옵션 명시.
