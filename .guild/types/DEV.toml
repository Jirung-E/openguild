# WARNING: [counter] is an auto-managed field. Do not edit it by hand.
# last_number is a monotonic counter that keeps issued quest IDs from being reused.
#   - Lowering it can hand out a duplicate ID and corrupt data.
#   - Raising it skips numbers and makes tracking harder.
# On startup it is validated against the max number of the actual quest files.

prefix = "DEV"
color = "#4A90D9"
description = "일반 개발 작업"

[counter]
last_number = 365
