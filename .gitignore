# Rust
target/

# 테스트용 guild 파일 (server/build.rs 자동 생성, repo root 에 작성됨)
/test.guild
/*.db

# DB 자동 백업 (server backup task, repo root 기준)
/backups/
# audit log (mutation HTTP 요청 기록)
/audit.log

# Node.js / Svelte (gui/frontend/)
gui/frontend/node_modules/
gui/frontend/.svelte-kit/
gui/frontend/build/
gui/frontend/dist/

# 환경 변수
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/
*.suo
*.user

# Claude Code 로컬 설정
.claude/
