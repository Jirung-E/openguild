# 모든 텍스트 파일은 저장소에 LF 로 저장. Windows working dir 에서는 자동 변환.
# 매 커밋마다 "LF will be replaced by CRLF" 경고가 나는 걸 막는다.
* text=auto eol=lf

# 바이너리는 명시
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.svg    text eol=lf
*.db     binary
*.sqlite binary
*.lock   text eol=lf
